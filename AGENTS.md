# BackwaterGenetics Project — Agent Memory

> **AGENTS.md File Maintenance:** This file must use **LF-only line endings** (Unix-style). Never write CRLF () line endings to this file. When editing via tools that write raw bytes (e.g., Python scripts, bash), always open/write in text mode or explicitly convert  → . The Error 0x80070006: The handle is invalid. tool may silently introduce CRLF if the file already contains them — if you must use the edit tool and then verify, run: `python3 -c "print(open('AGENTS.md','rb').read().count(b'
'))"` and strip any introduced CRLF immediately.

## Project Overview
Fisheries population monitoring for PIT-tagged Razorback Sucker (XYTE) and Bonytail (GIEL) in two backwater habitat sites on the lower Colorado River system: Yuma Cove Backwater and Imperial Ponds California (IPCA 1–6). Analyses cover post-stocking survival, minimum known survivor counts, annual mark-recapture population estimates, and reproductive contribution from parentage genetics (Dowling lab). Managed by Brian Kesner, Marsh & Associates.

> **Geography note (corrected 2026-08-17):** Yuma Cove Backwater is **not** near Yuma, Arizona. It is a cove on **Lake Mohave** (the reservoir between Hoover and Davis dams on the Colorado River, between Boulder City, NV and Laughlin, NV), closed off from the mainstem reservoir by an earthen berm built across the mouth of the cove — hence "backwater." Only IPCA (on the Imperial National Wildlife Refuge) is actually near Yuma, AZ.

---

## Species Codes
| Code | Common Name |
|------|-------------|
| XYTE | Razorback Sucker (RASU) |
| GIEL | Bonytail (BONY) |

---

## Study Locations
| Location Name | LID | Species | Surface Area | Source |
|---|---|---|---|---|
| Yuma Cove backwater (Lake Mohave, NV — see geography note above; not near Yuma, AZ) | 592 | XYTE | 2.6 acres (1.1 ha); ~13 ft deep at lake elevation 643 ft, varies with reservoir elevation | Marsh et al. 2024, Appendix 1.3 |
| IPCA (Pond 1) | 1043 | XYTE | 8.8 acres (3.6 ha) | Marsh et al. 2024, Appendix 1.2 |
| IPCA (Pond 2) | 1044 | GIEL | 12.5 acres (5.1 ha) | Marsh et al. 2024, Appendix 1.2 |
| IPCA (Pond 3) | 1045 | XYTE | 14.2 acres (5.7 ha) | Marsh et al. 2024, Appendix 1.2 |
| IPCA (Pond 4) | 1046 | XYTE | 8.6 acres (3.5 ha) | Marsh et al. 2024, Appendix 1.2 |
| IPCA (Pond 5) | 1047 | GIEL | 21.3 acres (8.6 ha) | Marsh et al. 2024, Appendix 1.2 |
| IPCA (Pond 6) | 1048 | GIEL | 8.8 acres (3.6 ha) | Marsh et al. 2024, Appendix 1.2 |

> **Note:** Pond assignments verified from source data (July 2026); Ponds 1, 3, 4 = XYTE; Ponds 2, 5, 6 = GIEL.

> **Surface areas (2026-09-09):** All 7 areas were gleaned from the full Marsh et al. 2024 paper (`documents/Maturation of an off-channel.pdf`, Appendices 1.2–1.3), not just the summary — the summary only had the 3 BONY pond sizes. All six IPCA ponds are design surface areas ~12 ft deep. `data/BWPondAreas.csv` has been updated with real values (previously all `NA`, which forced `Area = 1` for every modeled pond in `model/YCB3S_data.R`). The IPCA design totals (74.2 acres for 6 ponds) match the current site closely; earlier proposed sizes in the 2005 design workshop (Pond 1 ~8.9 ac, Pond 3 14.3 ac, Pond 4 10.2 ac, Pond 5 17.2 ac, Pond 6 9.2 ac) were adjusted slightly during final construction. YCB's 2.6-acre size is much smaller than any IPCA pond and varies with Lake Mohave's reservoir elevation (it is a natural berm-isolated cove, not an engineered pond).

### Site Background (from Marsh et al. 2024)

**Yuma Cove Backwater.** A natural off-channel habitat: wind-driven gravel/cobble accumulation formed a semipermanent berm isolating the cove from Lake Mohave; the berm was artificially enhanced in 1999 and again in autumn 2010 during low lake elevation, and can breach during high westerly winds at near-maximum lake elevations. Water source is passive percolation through the berm; substrate is coarse gravel/cobble, sand, silt, organic debris. Located on the Arizona shoreline of Lake Mohave, river mile 24.5 upstream of Davis Dam, within Lake Mead National Recreation Area.

**Imperial Ponds (IPCA).** A wholly artificial pond complex near river mile 59 upstream of the International Boundary, part of USFWS Imperial National Wildlife Refuge, near Yuma, AZ. Originally 4 "DU" (Ducks Unlimited) ponds set aside for native fish/waterfowl (totaling 45 surface acres, 18 ha); reworked in 2007 into the current 6-pond configuration (74.2 acres total, 30 ha). All ponds ~12 ft (3.7 m) deep; hummocks were added to Ponds 3, 4, and 6. Water source is individual groundwater pumps per pond (an earlier river-water/wedge-wire-screen system failed to exclude nonnative fish eggs/larvae and was replaced). Substrate: riprap (Pond 1 west shoreline), limited gravels at boat ramps/added spawning substrate, local soils and silt. Ponds 1, 3, 4 stocked with RASU; Ponds 2, 5, 6 stocked with BONY.

---

## Key Analysis Parameters (set in DataWrangling.R)
- `SurvivalDAL = 90` — minimum days at large post-release to be classified a "survivor"
- `SizeClass2 = 350` mm TL
- `SizeClass3 = 500` mm TL
- Fiscal year (FY): October 1 – September 30; `ScanFY = year + 1` when `month > 9`
- Yuma Cove data filtered from `2013-01-01`; IPCA ponds from `2016-01-01`

---

## Analysis Pipeline (run in order)
| Step | Script | Output |
|---|---|---|
| 1 | `DataWrangling.R` | `data/ReportingData.RData` (includes all known survival objects) |
| — | `KnownSurvival.R` | *(Deprecated — wrangling moved to DataWrangling.R; script retained for standalone PNG generation only)* |
| 3 | `PostStockingSurvival.R` | 3 PNG survival curve figures |
| 4 | `BWMarkRecaptureEstimates.R` | 3 PNG estimate figures |
| 5 | `BackwaterGrowth.R` | Growth summaries (no saved PNGs currently) |
| 6 | `GeneticsAndSurvival.R` | 2 PNG offspring contribution figures |
| 7 | `BackwaterMCR.R` | CJS model results (Pond 1 only; requires MARK software) |
| 8 | `PopulationMonitoring.qmd` | Full 7-location report (HTML or DOCX); regenerates all PNGs and adds 2 cross-site comparison figures. Requires steps 1-2 first. Does **not** source scripts 3-6; models and genetics run inline. Includes an **Introduction** section (species conservation status, OCH concept, site histories, BONY pond sizes) and a **Data Sources and Monitoring Infrastructure** subsection in Methods. Narrative paragraphs for each location draw on `2025Report_IP_PopulationMonitoring.md`, `Marsh_et_al_2024_Summary.md`, and related document summaries. |
| — | `ScanningEffectiveness.qmd` | Standalone PIT scanner effectiveness report (HTML). Requires step 1 only. Covers effort reliability filtering, monthly detection probability, seasonal patterns, per-sub-effort detection rates, and a diagnostic section identifying tagged fish with zero passive antenna contacts. |
| `ChapmanValidation.qmd` | Standalone Chapman estimator validation report (HTML). Does **not** require a pipeline step — loads `data/ReportingData.RData` directly. Tests bootstrap CI coverage against a fixed known true population (N = 65: 2013 YCB XYTE stocking cohort fish confirmed alive by a passive contact in 2019 or later). Two analyses: (1) annual estimates using the standard Jan–Feb mark / Oct–Apr capture protocol for census years 2014–2018; (2) sub-annual rolling 30-day mark and 30-day capture windows (back-to-back, stepping monthly) for the same period. All M, C, R drawn exclusively from the 65 confirmed survivor fish. True N is fixed at 65 for every window. |
| `ChapmanValidationIP.qmd` | Standalone Chapman estimator validation report (HTML) for IPCA Pond 1. Does **not** require a pipeline step — loads `data/ReportingData.RData` directly. Tests bootstrap CI coverage against a fixed known true population (N = 102: 2016 IPCA Pond 1 XYTE stocking cohort fish confirmed alive by a passive contact on or after January 1, 2022). Two analyses: (1) annual estimates for census years 2017–2021; (2) sub-annual rolling 30-day windows for the same period. Notable finding: in 2017–2020 R = C for the confirmed cohort (near-perfect detection), so the Chapman formula returns N̂ = M exactly with a degenerate (zero-width) bootstrap CI; 2021 and the sub-annual windows show real variation, including a mid-2019 scanner gap. See `Chapman Validation Process` section below for the general methodology shared with `ChapmanValidation.qmd`. |
| `IPGIELScannerDiagnostics.qmd` | Standalone PIT scanner diagnostics report (HTML, self-contained) for the three IPCA Bonytail ponds (2, 5, 6), written 2026-09-04 to judge data sufficiency for the joint robust-design model. Loads `data/ReportingData.RData` directly (`StudyBWEffort`, `StudyBWContacts`, `StudyBWAnalysis`); no pipeline step required. **Effort splitting:** each `StudyBWEffort` deployment is expanded to calendar days, hours per day = overlap of Deploy–Retrieve with that day × `ScanTimeHrs/DeployedHrs`, then summed by pond × month (reproduces `ScanTimeHrs` within 3 h); contacts are attributed by their own `Date`. **Known-alive denominator:** deliberately *not* `TotalCountIPGIEL` (its 90-DAL rule excludes freshly stocked fish, so proportion detected exceeded 1 after stockings); instead a fish is known alive in a month if `first_date` ≤ month end and `MaxScanDate` ≥ month start, which bounds proportion at 1 and extends to Mar 2026. Figures: monthly scan hours + coverage, FY × month coverage heatmap, 4-row metric panel (known alive, unique fish, proportion detected, contacts/100 h), seasonal boxplots, rate-vs-N scatter, sufficiency heatmap (usable = ≥50% coverage and ≥5 fish), plus a pond × FY table. Key findings: coverage nearly complete FY2018+ in all three ponds; weak spots are population crashes (Pond 5 FY2021 0–1 fish alive; Pond 2 FY2021 2–38) and one effort gap (Pond 2 FY2023 scanned 1 of 7 Oct–Apr months, pond at 0 fish before 2023 restock); detection lowest Dec–Feb (~0.5), highest Mar–Apr/Oct (0.8–0.95); Pond 6 is the cleanest for modeling. Companion XYTE report: `XYTEScannerDiagnostics.qmd`. |
| `XYTEScannerDiagnostics.qmd` | Razorback Sucker companion to `IPGIELScannerDiagnostics.qmd` (written 2026-09-04) covering YCB (LID 592) and IPCA Ponds 1, 3, 4; same methods, figures, and table. Loads `data/ReportingData.RData` directly. Differences from the GIEL version: sites are labeled `Site` (YCB / Pond 1 / Pond 3 / Pond 4); each site has its own `start_month` (YCB 2013-02, IP 2016-12) and `start_fy` (YCB 2014, IP 2017) so IP panels are not padded with pre-2017 months; effort figure uses free y-scales because YCB runs many more units; summary table filters to FY2017+ (model window) while the FY table shows YCB back to FY2014. Key findings: **YCB has a near-total scanner gap FY2021–FY2022** (1 of 14 Oct–Apr months scanned) — a structural hole for the robust-design model; Pond 1 FY2022 had only 3 of 7 months ≥50% coverage and median proportion detected 0.03 (scanner problem, not a crash); Ponds 3 and 4 collapsed to ~25 and 4–10 known-alive fish in FY2023–FY2024 before FY2025 restocking; otherwise IP coverage is essentially complete FY2018+. Seasonal detection for XYTE is highest Jan–Mar (IP ponds 0.9+, spawning period) and lowest Oct–Nov (0.45–0.85), the reverse of the Bonytail pattern. Output `XYTEScannerDiagnostics.html` in the project root. |
| `BONYIPMDiagnostics.qmd` | Standalone data-sufficiency report (written 2026-09-19), self-contained HTML (`BONYIPMDiagnostics.html`), for the Sound Science LLC proposed bonytail IPM rather than for our own robust-design model — companion in purpose to `IPGIELScannerDiagnostics.qmd` but judging a different, annualized/staged/covariate model structure. Loads `data/ReportingData.RData` and `data/BWDieOffEvents.csv` directly; no pipeline step or model fit required (same "no NIMBLE run" method already used in `model/BONY_ProposedIPM_Critique.md`/`_916_Critique.md`, which this report complements with live-computed tables/figures rather than static ones). Scope: IPCA Ponds 2, 5, 6 only (open-water CJS and CHLP are outside this project's data). Sections: site inclusion check (confirms Yuma Cove and IP1 have **zero** GIEL records and IP3 has 2 — a stronger statement than "low recapture"); annual (Oct–Apr) occasion detection sufficiency per pond × FY (a new metric quantifying how much less information a single annual occasion carries vs. our monthly robust-design primaries); die-off timing vs. FY boundaries (IP5 crash spans FY2020, IP2 crash sits within FY2022 well after its Oct start); age/origin stage sufficiency (reproduces `model/BONY_ProposedIPM_916_Critique.md` §2's wild-hatched recruit counts live — verified identical, e.g. Pond 2 FY2019 155, FY2024 301); size-at-tagging histograms (confirms no bimodal age-0/1 split); growth-curve identifiability (re-embeds `model/GIEL_GrowthTrackComparison_ProbCurves.png`, restates the `Linf`-below-observed-max finding); origin composition per pond-year (few pond-years have both stocked and wild-hatched fish tagged in the same season); F/genetics and environmental-covariate data-gap sections (factual, cross-referencing `AGENTS.md` Open TODOs and `model/BONY_ProposedIPM_Critique.md` §3); combined pond × FY sufficiency summary table. **Note:** rendering this file required installing the `kableExtra` R package (was missing from the R library despite being used by several other project `.qmd` files) — now installed. |
| `model/YCBSizeDensityEDA.qmd` | **Phase 0 of the three-stage density-dependent model** (plan `.posit/assistant/plans/2026-09-04-1335-ycb-three-stage-density-dependent-robust-design-model.md`; written 2026-09-04). Self-contained HTML (`model/YCBSizeDensityEDA.html`); loads `../data/ReportingData.RData` and `../data/BWRobustDesign_data.RData` (for `Fish` removals and `LengthWeight`), so render from `model/` (`cd model && quarto render YCBSizeDensityEDA.qmd`). Uses `mgcv`, `ggrepel`, `patchwork`. Sections: recruit TL at Oct–Dec nettings by FY; re-sighting ≥365 d vs TL at tagging (binomial GAM); persistence curves for <300 mm tagged fish by cohort; cohort table (known-alive adult biomass via `LengthWeight`, recruits handled, size split, small-fish survival); growth increments (too sparse for juveniles); IP Ponds 1/3/4 preview. **Findings:** recruit median TL falls from ~300 mm (<130 recruits: FY2014, FY2017, FY2018) to 120–180 mm (300–500 recruits: FY2019, FY2023–FY2025) — strong density-dependent growth. Re-sighting of netting-tagged fish is flat at 1–4 % from 100 to 300 mm, ~10 % at 300–349, 57–70 % at ≥350; so **T1 = 300 mm** was chosen for the S/L split (not 250). Small fish in dense cohorts are scanned after release (83–96 %) but contacts cease 60–120 d later (Dec–Feb): overwinter loss, not tagging mortality or a scanner artefact. The one small cohort (FY2018, 34 small fish) had 81 % survival to 1 yr; FY2017 loss = documented 2018 die-off. Tagged adult biomass is nearly flat (190–270 kg FY2017–FY2025) so **recruit density, not adult biomass, is the survival covariate with contrast**; the effect is near-binary and rests on one surviving cohort. IP Pond 1 small recruits are re-seen at ~35 % (n = 239) — the main low-density contrast for Phase 4. Pond surface areas for YCB and IP1/3/4 still needed (`data/BWPondAreas.csv` planned). |
| `model/YCB3S_data.R` | **Phase 1 of the three-stage density-dependent model** (2026-09-04). Forked from `BWRobustDesign_data.R`; builds `data/YCB3S_data.RData` for `MODEL_PONDS` (currently `1` = YCB; set to `c(1, 2, 4, 5)` for all XYTE in Phase 4). Three alive stages via `StageBreaks` (XYTE `c(300, 350)`: S < 300 ≤ L < 350 ≤ A; GIEL `c(200, 250)` provisional) and `stage_of(TL, species)`. **stobs codes: 1/2/3 = observed S/L/A, 4 = known alive** (was 3 in the 2-stage build); monotone-stage rule drops any observation lower than an earlier one or than `stage0`. `remU` is `[pond, season, 3]`; CSV `Stage == "J"` rows are assigned S or L by `MeanTL` vs the first break. New objects: `RecruitIndex` (pond × season: `n_tagged_juv` = untagged sub-adult first captures at Oct–Dec nettings, `n_csv` = CSV harvested + returned (in pool or not) + morts, `rec_total`, `rec_density = rec_total/Area`, `dens_std` = standardized `log(density + 1)` over netted seasons, `n0`/`nL` = fish measured / fish ≥ 300 for the size split; CSV juveniles with `MeanTL < CSV_SMALL_MAX = 250` count in `n0` as S), `SplitObs` (seasons ≥ 2 with `n0 > 0`), `Wbar[pond, season, stage]` mean weight (g) via `LengthWeight`, `BUILD_INFO` (input mtimes). `eff_std` is floored at `EFF_FLOOR = -3` (Apr 2021 leverage point). Areas read from `data/BWPondAreas.csv`; if any modeled pond has `NA` area, **all** areas are set to 1 with a warning (currently the case — YCB area unknown). **Calendar switch (Phase 3b, 2026-09-08):** set `CALENDAR <- "m7"` (default; Oct–Apr, `N_MOY = 7`, `T = 70`, Apr→Oct `gap = 6`, writes `data/YCB3S_data.RData`) or `CALENDAR <- "m12"` (every month a primary, `N_MOY = 12`, `T = 120`, `gap ≡ 1`, `DET_MONTHS = 1:12`, writes `data/YCB3S_data_m12.RData`) before sourcing; `OUT_FILE` may also be preset. `Primary` gains `summer` (May–Sep), passed as `RD_constants$summer`; `tApr` is `moy == 7` in both calendars; week screening computes `p_season` within pond × season × summer. The m7 build is unchanged by the refactor (verified identical `y`, `K`, `stobs`, `post`, `eff_std`, `Fish`, `Netting`). `RD_constants` adds `NSTAGE`, `dens`, `Area`, `Uk`, `sp_pond/sp_season/sp_n0`, `NSPLIT`; `RD_data` adds `sp_nL`. YCB build: 2,584 fish (S 1,904 / L 193 / A 487 at entry), 69 harvests, 30 netting event-stage rows, only 295 stage re-observations (292 adult), 18,863 known-alive fish-months. Observed recruit density: FY2017 120, FY2018 40, FY2019 484, FY2020 2,887, FY2021 230, FY2022 245, FY2023 1,283, FY2024 366, FY2025 829, FY2026 7; observed pL (≥ 300 mm) 0.31, 0.15, 0.00, 0.00, 0.01, 0.00, 0.00, 0.07, 0.00, 1.00 (n0 = 7). |
| `model/YCB3S_defs.R` | **Phase 2 of the three-stage model** (2026-09-04): `make_3s_inputs()` (collapses histories; pads `sp`/`Uk`/`zero` to length 2 when `NPOND == 1` because NIMBLE collapses length-1 constants to scalars and then rejects `sp[j]`), `rdPond3()` nimbleFunction (4 states S/L/A/Dead; monthly matrix `M` = survive then advance one stage, gap transitions = `M^g`; returns `T × 4`: `N_tag` by stage in cols 1–3, log-lik in `[1, 4]`), `dLLtrick`, `rd3_code`, `make_3s_inits()`, `repair_3s_inits()`, `rd3_configure()`, `run_3s_chain()`. **Verified** against `rdPond()` with S folded into L: identical log-likelihood when all gaps are 1 month; with 6-month gaps `rdPond3` differs slightly (Δll ≈ 4) because `M^g` lets a fish that matures mid-gap survive at the adult rate thereafter, whereas `rdPond` applies juvenile survival for the whole gap then matures — intended. Model: `lphiL = lphiA + dJ + cB·dens`, `lphiS = lphiL + dS` (`dS ≤ 0`), `logit(pLg[j,y]) = a0 + a1·dens[j,y]` with `sp_nL ~ dbin(pLg, sp_n0)`; monthly `lpsiSL`, `lpsiLA`; `lp[j, 1:3]`; untagged pool `U[j, y, 1:3]` with `RecJ[j,y]` split by `pLg[j, y+1]`; netting join by 3 stages. Monitors add `dS, cB, a0, a1, lpsiSL, lpsiLA, lphiL, lphiS, pLg`. |
| `model/YCB3S_NIMBLE.R` | **Phase 2 fit driver.** Loads `data/YCB3S_data.RData`, **stops if any `BUILD_INFO$inputs` file is newer than the build** (stale-data guard), sources `YCB3S_defs.R`, runs 3 parallel chains (30k / 10k burn / thin 10, seeds 6173/2948/8805; ~25 min per chain, compile 0.8 min), saves `data/YCB3S_MCMC.RData` (`rd3_samples`, `rd3_summary`, `rhat3_all`, `rd3_code`, `rd3_Fish`, `rd3_Hist`, `PondTable`, `Netting`, `RecruitIndex`, `SplitObs`, `rd3_settings`) and `model/YCB3S_traceplots.pdf`. Run: `rstudioapi::jobRunScript("model/YCB3S_NIMBLE.R", workingDir = getwd(), importEnv = FALSE)`. First full fit (job `20EBF982`) completed 2026-09-04 14:54 (33 min). **Results:** hyperparameters converged (`mu_phi`, `a0`, `a1`, `lp`, `g_eff`, `U0` R-hat ≤ 1.02) but `dJ`, `cB`, `b_post`, `lpsiLA` mix slowly (n.eff 40–110, R-hat 1.05–1.10) and last-season recruitment (`r[1,9]`, `RecJ[1,9]`, `U[1,10,·]`) has R-hat 1.25. `a1 = −1.84 (−2.19 to −1.50)`: strong density-dependent recruit size, posterior `pLg` matches observed splits for FY2018–FY2025. **`cB ≈ 0.04 (−0.21 to 0.30)` and `dS ≈ 0`** — no density or small-size effect on juvenile survival once season effects (`sigma_phi = 0.93`) are in; `dJ = −2.55` (annual juvenile survival ~0.26 at `mu_phi`). YCB adult survival moved toward the empirical floors (FY2023 0.58 → 0.74 vs floor ≥ 0.81; FY2024 0.81 → 0.92 vs ≥ 0.91). **Concern:** L-stage detection `lp[1,2] = −1.56` is far below S (0.11) and A (−0.36) with fast S→L (`lpsiSL = −0.95`, 28 %/month) and slow L→A (2 %/month) — L may be acting as a low-detection sink; consider constraining or sharing juvenile detection in Phase 3. |
| `model/YCB3S_validation.qmd` | **Phase 3 validation report** for the YCB three-stage fit (first rendered 2026-09-08; output `model/YCB3S_validation.html`, self-contained). **Parameterized by calendar** (`params: calendar: "m12"` default): loads `../data/YCB3S_MCMC[_m12].RData` + `../data/YCB3S_data[_m12].RData`, checks `BUILD_INFO$CALENDAR`, and loads the *other* calendar's fit as `alt` for side-by-side columns in §3.2 (juvenile cohorts) and §4.1 (size split). Render from `model/`: `cd model && quarto render YCB3S_validation.qmd` (m12) or `quarto render YCB3S_validation.qmd -P calendar:m7 --output YCB3S_validation_m7.html`. Helpers `mdiff(t0, t1)` (calendar months between primaries — never use index differences as month counts; under m7 Apr→Oct is 1 index but 6 months) and `t_of("YYYY-MM")`. §3.3 adds a **gap-decomposition table** (m12 only) with direct floors on the Mar-2020 adult cohort over three legs bounded by scanned months. §6 archive table includes fit 7 = m12 plus rows for FY2021/FY2022 survival, `b_post[1,2]`, `g_eff`. Optionally compares to `../data/BWRobustDesign_MCMC.RData` if present. Sections: (1) R-hat / n.eff by node family and scatter; (2) known-alive constraint `sum(N_tag[t,])` vs fish known alive by any contact (note: `count_from` is **not** a `Fish` column in either data build — it lives only in `RD_constants`; the QMD recomputes it as `entry_t + (origin == 2)`); (3) adult annual survival vs re-sighting floors by season, netting-tagged juvenile cohorts vs fraction re-seen, FY2021–22 gap as 24- and 31-month spans, `eps_J` table; (4) posterior predictive checks for `sp_nL` and the netting join `ev_m`, density–size curve; (5) core parameter table with derived quantities, untagged pool, latent `RecJ`; (6) side-by-side comparison of the six archived three-stage fits (`freeLp`, `sharedLp`, `bpostJ`, `epsJ`, `centeredEpsJ`, working no-cB; each `data/YCB3S_MCMC_<tag>.RData`, skipped if missing) on season adult survival, `dJ` and derived juvenile survival, `sigma_phi`/`sigma_J`, 31-month gap product, detection, and passage rates — adult survival is stable across fits 4–6; (7) comparison with the joint 2-stage fit; (8) auto-generated status bullets. Pitfall fixed while drafting: inside `mutate()` after joining `season_tbl`, the `tOct` column masks the `tOct` vector — use `.env$tOct`. |
| `model/BWRobustDesign_data.R` | **Joint robust-design model, step A.** Builds all NIMBLE inputs from `data/ReportingData.RData` (step 1) plus the hand-maintained `data/BWNettingEvents.csv`; saves `data/BWRobustDesign_data.RData`. **Must be re-run after any edit to this script or to `ReportingData.RData`/`BWNettingEvents.csv` before fitting** — see `Joint Robust-Design Model` section. |
| `model/BWRobustDesign_NIMBLE.R` | **Joint robust-design model, step B.** Fits the model (3 parallel chains, 30k iter / 10k burn / thin 10, ~3 h wall time) and saves `data/BWRobustDesign_MCMC.RData` + `model/BWRobustDesign_traceplots.pdf`. Run as an RStudio background job: `rstudioapi::jobRunScript("model/BWRobustDesign_NIMBLE.R", workingDir = getwd(), importEnv = FALSE)`. Sources `model/BWRobustDesign_defs.R`. |

---

## Joint Robust-Design Model (`model/`)

Hierarchical Bayesian model (NIMBLE) estimating monthly survival, detection, tagged abundance, untagged (wild-recruit) abundance, and recruitment jointly for XYTE (YCB, IP1, IP3, IP4) and GIEL (IP2, IP5, IP6), FY2017–FY2026. Modeled on the Lake Mohave joint model in `C:\GIT\NCreasedSupport2025\model`. Plan file: `.posit/assistant/plans/2026-09-03-1136-joint-bayesian-robust-design-model-for-backwaters.md`.

**Files**
| File | Role |
|---|---|
| `model/BWRobustDesign_data.R` | Data prep → `data/BWRobustDesign_data.RData` |
| `model/BWRobustDesign_defs.R` | `make_rd_inputs()` (collapses identical histories to weighted rows), `rdPond()` nimbleFunction (forward–backward over a pond's histories; returns smoothed expected tagged alive by stage `N_tag` and the total log-likelihood), `dLLtrick` zeros-trick distribution, `rd_code` (model), `make_inits()`, `repair_inits()`, `rd_configure()` (block samplers), `run_rd_chain()` |
| `model/BWRobustDesign_NIMBLE.R` | Fit driver (settings at top: `RUN_PONDS`, `N_ITER`, `N_BURNIN`, `N_THIN`, `SEEDS`, `OUT_FILE`) |
| `data/BWNettingEvents.csv` | Hand-maintained untagged netting counts not in NFWG. Columns: `Backwater`, `EventMonth` (YYYY-MM), `Species`, `Stage` (J/A), `HarvestedUntagged`, `ReturnedUntagged`, `ReturnedInPool` (TRUE if returned-untagged fish are taggable size and belong in the untagged pool), `MortsUntagged`, `MeanTL`, `Notes`. Currently YCB rows only (from `documents/Yuma BW summary of events.xlsx`); IP ponds have no untagged-not-tagged counts. |
| `data/BWRobustDesign_data.RData` | `Fish`, `Primary`, `PondTable`, `Netting`, `Stocking`, `Removals`, `RemovedTagged`, `SameEventRemovals`, `LengthWeight`, `y_mat`, `K_mat`, `stobs_mat`, `post_mat`, `eff_wk`, `eff_hrs`, `eff_std`, `RD_constants`, `RD_data`, constants (`START_DATE`, `T_PRIM`, `N_SEC`, `POST_MONTHS`, `StageTL`), helpers `prim_floor()`, `fy_of_date()` |
| `data/BWRobustDesign_MCMC.RData` | `rd_samples` (coda `mcmc.list`), `rd_summary`, `rhat_all`, `rd_code`, `rd_Fish`, `rd_Hist`, `rd_Netting`, `rd_ponds`, `rd_settings` |
| `data/BWRobustDesign_MCMC_dev.RData`, `model/BWRobustDesign_traceplots_dev.pdf` | Short development fit (12k iter). **Fitted on stale data (no known-alive constraints) — do not use for inference.** |
| `data/BWRobustDesign_MCMC_staleData.RData`, `model/BWRobustDesign_traceplots_staleData.pdf` | First 30k-iteration fit, also on stale data (see `Known Code Issues`). Kept only for comparison. |

**Time structure.** Seasons = FY2017–FY2026 (10). Primary periods = calendar months Oct–Apr (7 per season, `T_PRIM = 70`); `gap` = 1 month within season, 6 months Apr→Oct. Secondary occasions = weeks within a month (days 1–7, 8–14, 15–21, 22–end; `N_SEC = 4`). May–Sep contacts are **not** detections but **do** set known-alive constraints. `prim_floor(date)` maps any date to the primary period at or before it (May–Sep → Apr of that FY).

**Fish table (`Fish`, one row per PIT × backwater, 8,009 rows).** `origin` 1 = stocked, 2 = tagged at netting, 3 = established before Oct 2016 (enters at first contact in window, no post-release offset). `stage0` 1 = juvenile / 2 = adult at entry using `StageTL` (XYTE 350 mm, GIEL 250 mm). `entry_t`, `end_t` (= `T_PRIM` or removal month), `removed`, `rem_type` (harvest / transfer / mortality), `last_det_t` (last Oct–Apr detection), `last_alive_t` (from `MaxScanDate`, any contact), `count_from` = `entry_t` + 1 for netting-tagged fish (not counted as tagged-alive in the month they were tagged). Fish tagged and removed in the same netting event are dropped as individuals and counted as untagged harvest (`SameEventRemovals`, 124 fish). `SuspectTransfers` fish are never treated as removed.

**Detection arrays.** `y_mat[i,t]` = weeks with ≥1 own-backwater contact (0–4); `K_mat[i,t]` = weeks available (scanner running per `StudyBWEffort` **or** any contact that week in that pond, fish at large). `stobs_mat[i,t]`: 0 none, 1 observed juvenile (netting TL), 2 observed adult, **3 known alive, stage unknown** (all months from entry through `last_alive_t` not otherwise coded). `post_mat` = 1 for transitions within `POST_MONTHS = 6` of release. `eff_std` = standardized log monthly scan hours (detection covariate).

**Model (`rd_code`).** States J / A / Dead. Survival: `logit(phiA) = lphiA[pond, season] ~ N(mu_phi[sp], sigma_phi[sp])`; juvenile offset `dJ[sp]`; post-release offsets `b_post[sp, origin]` (stocked, netting-tagged; 0 for established), truncated ≤ 0; maturation `lpsi[sp]` (monthly). Detection: `logit(p) = lp[pond, stage] + g_eff[sp] * eff + d_moy[sp, month-of-season]`, binomial over available weeks. Removal (harvest/transfer) ends a history **with the fish alive** — biomass removed, not mortality. Untagged pool per pond × season × stage: `U[j,1,·]` = 0 for IP ponds (renovated, stocked FY2017), `U0[1,·] ~ Unif(0, 5000)` for YCB; annual survival from the same `lphi`, maturation `psiY`, recruits `RecJ = r[j,y] * Atot[j,y]` with `log r ~ N(mu_r[sp], sigma_r[sp])`, `Atot` = tagged + untagged adults in April; `nu[sp]` = unexplained adult appearances; known removals `remU` (newly tagged + untagged harvest) subtracted with `dconstraint(U >= remU)`. Netting join: `ev_m ~ Binomial(ev_n, N_tag / (N_tag + U_available))` per pond × month × stage. Monitors include `N_tag[pond, t, stage]`, `U`, `RecJ`, `r`, `Tal`, `Atot`.

**Validation check (always run after a fit).** For each pond and month, `sum(N_tag[j,t,])` posterior must be ≥ the number of fish known alive by any contact (`Fish |> filter(pond == j, entry_t <= t, end_t >= t, last_alive_t >= t, count_from <= t)`). Known-survivor lines (`TotalCount*`) and Chapman estimates (`BWMarkRecaptureEstimates.R`) are the other comparison baselines.

**Status (2026-09-04).** Data rebuilt 08:19 with known-alive constraints (71,563 code-3 fish-months). A corrected 30k-iteration fit was launched as background job "BWRobustDesign final fit (rebuilt data)" (job id `B9E7CE08`); check `data/BWRobustDesign_MCMC.RData` mtime to see whether it completed. In the stale fits, core hyperparameters converged (R-hat ≤ 1.02) but `N_tag` for IP4 (pond 5), YCB `U0`/`U`, and `RecJ` mixed poorly (R-hat up to 2.3; `U0[1,·]` n.eff 36–83) — re-check these first; a tighter `U0` prior or longer run may be needed. Remaining work: report QMD (`model/BWRobustDesign_report.qmd`, not yet written) with N_tag vs known survivors / Chapman, netting posterior predictive checks, survival and recruitment tables and figures.

---

## Three-Stage Density-Dependent Model — Open Concerns and Next Steps (as of 2026-09-08 15:30)

Plan file: `.posit/assistant/plans/2026-09-04-1335-ycb-three-stage-density-dependent-robust-design-model.md`. Phases 0–3 complete: m7 working fit is the no-cB variant in `data/YCB3S_MCMC.RData` (job `E5E647F7`), validated in `model/YCB3S_validation.qmd`. **Phase 3b (12-month calendar) is fitted: `data/YCB3S_MCMC_m12.RData` (job `FF04A722`, 2026-09-08) — see the last bullets below; this is the preferred fit going forward. Next: re-render the validation report against m12 (point `mc`/`dd` at the `_m12` files), then Phase 4 (IP XYTE ponds).** The bullets below are the chronological fit log.

**Concerns from the first fit**
1. **L-stage detection is implausibly low.** `lp[1,2] = −1.56` (≈ 0.17/week) vs S `0.11` (0.53/week) and A `−0.36` (0.41/week), with fast S→L (`lpsiSL = −0.95`, 28 %/month) and very slow L→A (`lpsiLA = −3.91`, 2 %/month). Only 3 juvenile stage re-observations exist at YCB, so L is nearly unconstrained and appears to act as a low-detection holding state for small fish that stop being scanned. A 300–350 mm fish should be detected at least as well as a 150 mm one.
2. **No density effect on juvenile survival once season effects are in.** `cB = 0.04 (−0.21, 0.30)`, `dS ≈ 0`; all juvenile mortality sits in `dJ = −2.55` (≈ 26 % annual). The FY2018 cohort's survival is absorbed by that season's `lphiA` (0.93), i.e. `dens` and the season random effect are confounded (Phase 0 warned of this — one surviving cohort vs four or five lost).
3. **Adult survival improved but FY2023 still below its empirical floor:** 0.74 (0.69–0.79) vs re-sighting floor ≥ 0.81–0.90 (joint 2-stage model gave 0.58). FY2024 now consistent (0.92 vs ≥ 0.91).
4. **Mixing:** `dJ`, `cB`, `b_post[1,1:2]`, `lpsiLA` have n.eff 40–110 and R-hat 1.03–1.10; final-season recruitment (`r[1,9]`, `RecJ[1,9]`, `U[1,10,·]`) R-hat 1.25; `N_tag` around Oct–Nov 2022 R-hat 1.11–1.16. Species-2 (GIEL) parameters are prior-only in this build and should be ignored.
5. Density-dependent **growth** is well supported and fits: `a1 = −1.84 (−2.19, −1.50)`; posterior `pLg` matches observed size splits FY2018–FY2025.

**Shared-juvenile-detection refit (launched 2026-09-04 15:25, job `794E9847`)**
- `rd3_code` now has `lp[j, 2] <- lp[j, 1]` (S and L share a detection intercept); `make_3s_inits()` gives `lp[, 2] = NA`; `rd3_configure()` blocks only `lp[j, c(1, 3)]`. `rd3_settings$variant` records the change. Output goes to `data/YCB3S_MCMC.RData` / `model/YCB3S_traceplots.pdf` (~35 min).
- The first fit (free `lp[j,2]`) was renamed `data/YCB3S_MCMC_freeLp.RData` / `model/YCB3S_traceplots_freeLp.pdf` for comparison.
- **Result (completed 15:45, 25 min):** shared juvenile detection `lp[1,1] = −0.19` (adult −0.27). Transitions reorganized: `lpsiSL` −0.95 → −3.18 (4 %/month), `lpsiLA` −3.91 → −2.08 (11 %/month) — the L "low-detection sink" is gone. `dJ` −2.55 → −2.98: juvenile annual survival at `mu_phi` drops from ~0.27 to ~0.13–0.14 (what the free fit parked as undetected L fish is now mortality). `cB = 0.00 (−0.26, 0.25)`, `dS ≈ 0` — still no density effect. Season adult survival essentially unchanged (FY2023 still 0.75 vs floor ≥ 0.81). Mixing better: max R-hat 1.11 (was 1.26), only `lphiS/L[1,4]` > 1.1; `b_post`, `dJ`, `cB`, `lpsiLA` n.eff 170–310 (was 40–110). `N_tag ≥ known alive` holds for every month in both fits. This is the working variant going forward.
- **FY2023 adult-survival diagnosis (2026-09-04 16:00).** `lphiA[1,7]` (0.75) is below the empirical floor because the season effect is shared across stages and `dJ`/`b_post` are constants, so an unusually fast juvenile crash can only be absorbed by the season's `lphiA`. The Nov 2022 netting tagged 283 small fish whose expected S count went 283 → 186 → 14 in two months (FY2019's 484 juveniles persisted ~3 months before collapsing, and FY2022's vanished with the scanner off, so those seasons were not pulled down). Profile log-likelihood over `lphiA[1,7]` at posterior means (compiled `rdPond3`, history weights `w` zeroed for the FY2023 juveniles): full data peaks at 0.75 and is −27 at 0.85; without the 283 juveniles, or adults-only, it peaks at 0.83–0.85 (floor on known-alive adults Oct 2022 → Oct 2023 is 150/171 = 0.88; the model's N_tag trajectory 201 → 166 with 9 removals is consistent with that). Posterior correlations of `lphiA[1,7]` with `dJ`/`b_post` are weak (|r| < 0.1) — the juvenile data pin it rather than trade off. **Fix candidates:** stage-specific post-release offset for netting-tagged juveniles (`b_post` by origin × stage), or a season-specific juvenile deviation `eps_J[j,y]` on `lphiL`. Same mechanism likely explains FY2017 (0.81 vs floor 0.92; 113 juveniles, none survived 90 d).
- **Juvenile post-release offset refit (launched 2026-09-04 16:05, job `35D7B71F`).** `rdPond3` gains a `bpostJ` argument: S and L use `lphiS/L + b_postJ[sp, origin]` during post-release months, A uses `lphiA + b_post[sp, origin]`. Prior `b_postJ[s, o] ~ T(dnorm(-1, 1), , 0)`, `b_postJ[s, 3] <- 0`; added to inits, monitors, `core_pars`, and the survival-offset `RW_block`. When `b_postJ == b_post` the code reduces exactly to the previous variant. The shared-lp fit was archived as `data/YCB3S_MCMC_sharedLp.RData` / `model/YCB3S_traceplots_sharedLp.pdf`; the new fit writes `data/YCB3S_MCMC.RData`. **On completion check:** `lphiA[1,7]` (FY2023) and `lphiA[1,1]` (FY2017) vs floors 0.88 / 0.92, `b_postJ[1,2]` vs `b_post[1,2]`, whether `dJ` moves toward zero (juvenile mortality shifting from a constant offset into the post-release window), and mixing of the now 9-parameter offset block.
- **Result of the `b_postJ` refit (completed 2026-09-04 16:44, 41 min): no effect.** Season adult survival unchanged to two decimals (FY2017 0.81, FY2023 0.75); FY2023 S trajectory identical (283 → 185 → 14). `b_postJ[1,2] = −0.68 (−1.17, −0.20)` ≈ `b_post[1,2]`; `dJ` −2.98 → −3.16 with `dJ + b_postJ` ≈ constant — the two are on an unidentified ridge because almost no netting-tagged juvenile survives past the 6-month post-release window, so only their sum is informed. Mixing degraded: `dJ` R-hat 1.49, `b_postJ[1,2]` 1.45, 105 nodes > 1.1. **Diagnosis:** a constant post-release offset cannot fix FY2023 because the problem is a *season-specific* juvenile crash rate (FY2023 juveniles died within 1–2 months; FY2019's persisted 3 months); any stage offset that is constant across seasons leaves `lphiA[j,y]` as the only season-varying term. **Next fix:** season-specific juvenile deviation `lphiL[j,y] = lphiA[j,y] + dJ + cB·dens + eps_J[j,y]`, `eps_J ~ N(0, sigma_J)` (or a month-of-season juvenile survival effect to represent overwinter loss — both FY2019 and FY2023 cohorts crashed in Dec–Feb). Recommend reverting `b_postJ` to `b_post` (set `b_postJ[s,o] <- b_post[s,o]`) when adding `eps_J`, to remove the ridge. Fit archived as `data/YCB3S_MCMC_bpostJ.RData` / `model/YCB3S_traceplots_bpostJ.pdf`.
- **Cohort trajectory check (2026-09-05).** Plotted model S-stage `N_tag` vs cohort fish detected for every YCB netting cohort: juveniles hold at 80–95 % detection for 1–3 months, then lose nearly everything in a single month between Dec and Feb (FY2017 Dec→Jan, FY2019 Jan→Feb, FY2023 Dec→Jan, FY2024 Feb→Mar, FY2025 Dec→Jan); FY2018 is the only surviving cohort; FY2021/FY2022 have no detections (scanner off). Timing varies by ±1 month, so a month-of-season effect would smear — a season-specific deviation was chosen.
- **`eps_J` refit (launched 2026-09-05 10:25, job `2E8895C1`).** `rd3_code`: `b_postJ[s, o] <- b_post[s, o]` (tied; removed from the offset block sampler and `core_pars`), `sigma_J[s] ~ T(dnorm(0, 1), 0, )`, `eps_J[j, y] ~ dnorm(0, sd = sigma_J[sp[j]])`, `lphiL[j, y] <- lphiA[j, y] + dJ + cB·dens + eps_J[j, y]` (S inherits via `lphiS = lphiL + dS`; untagged-pool juvenile survival also uses `lphiL`, so `U`/`RecJ` will move). Inits add `sigma_J`, `eps_J`; monitors add both; `rd3_configure()` adds an `RW_block` over `eps_J[j, 1:NS]`. Writes `data/YCB3S_MCMC.RData` / `model/YCB3S_traceplots.pdf` (the working file was a copy of the sharedLp fit, which remains archived). **On completion check:** `lphiA[1,1]`/`lphiA[1,7]` vs floors 0.92/0.88; `sigma_phi` (should shrink) vs `sigma_J`; `eps_J[1,2]` (FY2018, expected positive) and `eps_J[1,5:6]` (FY2021–22, should be prior-driven ≈ 0); `cB` vs 0; changes in `U`, `RecJ`; mixing of `dJ` (should improve with the ridge removed) and of the `eps_J` block; `N_tag ≥ known alive`.
- **Result of the `eps_J` refit (completed 2026-09-05 11:00, 31 min).** Adult survival released as intended: FY2017 0.81 → 0.91 (floor 0.92), FY2023 0.75 → 0.85 (floor 0.88); FY2019 0.91 → 0.86, FY2025 0.86 → 0.82 (both above floors). FY2021/FY2022 swapped (0.90/0.75 → 0.98/0.65) — scanner off both seasons, so only the product is identified (0.68 → 0.64); **report these two as a 2-year product**. `sigma_phi` rose 0.85 → 1.08 (not shrank) because of that swap. `sigma_J = 1.60 (0.91, 2.55)`; `eps_J` strongly negative for FY2017 (−2.1), FY2021 (−3.6, prior-free crash of 230 undetected juveniles) and FY2023 (−1.2); ≈ 0 elsewhere. `N_tag ≥ known alive` holds every month. **Problems:** (1) `dJ` and mean(`eps_J`) are confounded — per-chain `dJ` −2.41/−2.35/−3.14 while `dJ + mean(eps_J)` is −3.23 in all chains; `dJ` R-hat 2.17, all `eps_J` R-hat 1.2–2.6, `cB` R-hat 1.31 and now `−0.02 (−1.17, 1.30)` (unidentified against `eps_J`, as predicted). Fix: sum-to-zero constraint on `eps_J[j, 1:NS]` (or drop `dJ` and let `eps_J ~ N(dJ, sigma_J)`), possibly `barker` sampler. (2) **Recruitment `RecJ` is not credibly identified in either fit**: point estimates 2,000–20,000 with 95 % CIs spanning two orders of magnitude (e.g. FY2023 13,458 (338, 89,676)); the `eps_J` fit roughly doubled them because lower juvenile survival requires more recruits to match the netting join. Untagged adults `U[1, y, 3]` are stable (130–290) across fits. Recruitment reporting should focus on the untagged juvenile pool at netting or be constrained by `RecruitIndex`; treat `RecJ`/`r` as nuisance until then. Fit saved as `data/YCB3S_MCMC.RData`; sharedLp remains archived.
- **FY2021–FY2022 two-year adult survival (2026-09-08).** Posterior product `phiA[5]^12 · phiA[6]^12` (Oct 2020 → Oct 2022): sharedLp 0.680 (0.626–0.728), epsJ 0.636 (0.578–0.691). The naive floor on adults "known alive Oct 2020" (119/188 = 0.633) is **conditioned on survival** — with the scanner off Oct 2020–Sep 2022 (only Apr 2021, 1 week, 22 h), a fish is known alive at Oct 2020 mostly *because* it was seen after Oct 2022. Use the last well-scanned month instead: **Mar 2020 → Oct 2022 (31 months), 219 known adults, 119 seen after → floor 0.543**, no removals in the window. Model over the same span (`phiA[4]^7 · phiA[5]^12 · phiA[6]^12`): sharedLp 0.663 (0.610–0.710), epsJ 0.619 (0.564–0.672). Since scanning was continuous and adult detection ≈ 0.4/week from Oct 2022 onward, the floor here is close to realized survival, so the model is likely ~0.08 too high across the gap — hierarchical shrinkage of `lphiA[1,5:6]` toward `mu_phi` with no in-gap detections. Report the gap as a single 2-year (or 31-month) survival, not by season. **Data issue found:** Apr 2020 (t = 28) has 77.7 scan hours and `K_mat` = 4 weeks for 1,026 fish but **zero** contacts of any fish — scanner deployed but apparently not functioning. The model treats this as a fully scanned month with no detections; `K_mat[, 28]` should probably be set to 0 in `YCB3S_data.R` (or effort rows with zero contacts flagged generally). Check other months with effort > 0 and no contacts before the next data build.
- **Correction on the FY2021–FY2022 "gap" (2026-09-08, BK).** The scanner was *not* off Oct 2020–Sep 2022; there was no **Oct–Apr** scanning (one week, 30 Apr 2021), but submersible units ran 30 Apr–4 Aug 2021 (~2,300 h, 200 fish in May 2021), 10–18 May 2022 (~760 h, 239 fish) and 3 Aug–22 Sep 2022 (~2,200 h, 222 fish). May–Sep contacts are not detection occasions in the robust design; they enter only as known-alive constraints mapped by `prim_floor()` to April of the same FY, so within-gap Oct→Apr survival is partly informed but the summer-to-summer interval is unused. Adults seen May–Jul 2021 and re-seen from May 2022 give a direct ~12-month floor of 0.846 (187/221); only 173/218 adults seen May 2022 were seen again after Oct 2022 (~20 % summer-2022 loss, consistent with the low FY2022 estimate). **Possible improvement:** add scanned summer months as extra primary periods (`M^g` already handles irregular gaps). Wording in `YCB3S_validation.qmd` corrected from "scanner off" to "no Oct–Apr scanning"; always describe it that way.
- **Summer (May–Sep) scanning is the norm, not the exception (inventory 2026-09-08).** Prorating `StudyBWEffort` deployments to calendar months from Oct 2016: all six IP ponds were scanned in every May–Sep month of FY2017–FY2025 (Pond 2 FY2022: 3 of 5), and YCB in 5 summer months most years (0 in FY2020, 4 in FY2021, 3 in FY2022, 4 in FY2025, 3 so far in FY2026). Across ponds, **38–45 % of all scan hours and 33–45 % of fish-months (unique fish × month) fall in May–Sep** and are therefore not detection occasions in either robust-design model (they enter only as known-alive constraints via `prim_floor()`). IP1 summer months typically contact 150–216 fish in FY2017–FY2020 and 20–150 later; YCB 50–260. Extending primary periods to all scanned months (12/season instead of 7) is a data-build change that would roughly double the detection information and remove the summer-to-summer blind spot; it would also make `d_moy` a 12-level month effect and lengthen `rdPond3` runs proportionally.
- **Summer vs Oct–Apr detection at IP Pond 1 (2026-09-08).** Weekly detection (fish contacted / fish known alive that month; weeks with ≥ 100 prorated scan hours and ≥ 20 fish alive, FY2017–FY2026): May–Sep median 0.45 (IQR 0.24–0.70; 171 weeks) vs Oct–Apr 0.59 (0.38–0.75; 244 weeks). By month, summer medians (May 0.58, Jun 0.47, Jul 0.45, Aug 0.37, Sep 0.34) lie inside the Oct–Apr range (Oct 0.28, Nov 0.33, Dec 0.53, Jan–Mar 0.65–0.75, Apr 0.42), so a shared `lp` with `d_moy` extended to 12 levels is plausible. Differences: effort slope is steeper in summer (2.2 vs 1.4 logit per log-hour, p = 0.03), residual SD after month + FY + effort is larger (1.24 vs 0.90 logit), and FY effects correlate only r = 0.46 between seasons — so if summer months are added as primary periods, consider a season-specific `g_eff` (or wider detection overdispersion) rather than a separate `lp`. Denominator is outcome-conditional (fish never seen again are excluded), which biases both seasons upward similarly.
- **Scanner-week screening added to `YCB3S_data.R` (2026-09-08).** All-pond scan of Oct–Apr effort weeks with zero contacts of any fish: 11 whole months (YCB Apr 2020; IP1 Nov 2021 [+ Oct 2021 with 1 contact, 127 fish alive — the documented FY2022 Pond 1 scanner failure]; IP3 Oct 2023 and Oct 2024 with ~25 fish alive and season detection already 0.1–0.25/week, so ambiguous; IP3 Oct–Nov 2016 and IP5 Oct–Dec 2020, Oct 2021, Jan 2022 with 0–1 fish alive, harmless) plus ~68 isolated weeks (many at 0.4–30 h, i.e. partial deployments; a few full-hour dead weeks: IP5 Mar 2022 wk 1 [189 h, 301 fish], IP3 Nov 2024 wk 1 [166 h, 724 fish], IP6 Oct 2023 wk 1, IP2 Mar 2021 wk 1). Rule implemented after the `ContactWeeks` loop, before the median-hours fill: a week is dropped from `eff_wk` (→ `K_mat`) and its hours removed from `eff_hrs` if it has no contact of any fish **and** either (a) hours < `MIN_WK_FRAC = 0.25` × pond median weekly hours, or (b) ≥ `MIN_ALIVE = 20` tagged fish known alive (`entry_t ≤ t ≤ end_t`, `MaxScanDate ≥ month_start`) and `P0 = (1 − p_season)^known_alive < P0_MAX = 1e-3`, where `p_season` = median weekly proportion of known-alive fish detected over that pond-season's weeks with contacts. Weeks with any contact are never dropped. Dropped weeks are saved as `DroppedWeeks` in `YCB3S_data.RData`. YCB result: 17 weeks (14 low-effort, 3 dead) — Mar 2020 wk 4, all of Apr 2020 (now K = 0, `eff_std` = 0), Oct 2023 wk 2–4, Nov 2023 wk 1, Mar 2024 wk 2–3, Apr 2024 wk 1, Nov 2025 wk 2, Feb 2026 wk 2–4, Mar 2026 wk 1. Caveat: rule (b) is outcome-conditional (mild upward bias on detection); IP3 Oct 2024 (P0 = 1.3e-3) and the IP3 FY2023 low-detection weeks (P0 0.04–0.12) are deliberately kept. For Phase 4 the whole IP1 FY2022 season (median proportion detected 0.03) needs a separate decision. `data/YCB3S_data.RData` was rebuilt 2026-09-08 with this rule. **The same block was ported verbatim to `model/BWRobustDesign_data.R`** (saves `DroppedWeeks`; `data/BWRobustDesign_data.RData` rebuilt 2026-09-08 11:00): 72 weeks dropped all-pond (56 low-effort, 16 dead-scanner: YCB Mar 2024 wk 2–3 and Nov 2025 wk 2; IP1 all 8 weeks of Oct–Nov 2021; IP2 Mar 2021 wk 1; IP3 Nov 2024 wk 1, Nov 2025 wk 2; IP5 Mar 2022 wk 1; IP6 Oct 2023 wk 1); 8 pond-months now have K = 0 (YCB Apr 2020, IP1 Oct–Nov 2021, IP3 Oct–Nov 2016, IP5 Oct–Dec 2020). The IP1 Oct 2021 "1 contact" was an `NA`-PIT record, so the month is treated as dead. **`data/BWRobustDesign_MCMC.RData` predates this rebuild** — refit the joint model before using it.
- **Centered `eps_J` refit (2026-09-08, job `B1FC72D8`, 51 min; saved `data/YCB3S_MCMC.RData`, previous fit archived as `data/YCB3S_MCMC_epsJ.RData` / `model/YCB3S_traceplots_epsJ.pdf`).** `rd3_code` now has `eps_raw[j, y] ~ dnorm(0, sd = sigma_J[sp[j]])` and `eps_J[j, y] <- eps_raw[j, y] - mean(eps_raw[j, 1:NS])` (sum-to-zero by centering; `make_3s_inits()` supplies `eps_raw`, `rd3_configure()` blocks `eps_raw[j, 1:NS]`, monitors unchanged). Fitted on the screened data. **Results:** the `dJ`/`eps_J` confounding is resolved — per-chain `dJ[1]` means −3.08/−3.13/−3.07 (was −2.41/−2.35/−3.14), R-hat 2.17 → 1.09, `dJ[1] = −3.09 (−3.63, −2.62)`; nodes with R-hat > 1.1 fell 105 → 28. `eps_J` values: FY2017 −1.7, FY2021 −3.3, FY2023 −0.6, FY2022 +1.4, FY2020 +1.7 (wide), others ≈ 0–0.5; `sigma_J = 1.63`, `sigma_phi = 1.08`. **Still unresolved:** `cB[1] = −0.20 (−1.74, 0.91)`, R-hat 1.54, n.eff 22 — density and `eps_J` share the season index so `cB` is unidentified against the deviations (expected); `eps_J[1, 2]` (FY2018) and `eps_J[1, 7]` (FY2023) R-hat 1.3–1.5 and `dJ` n.eff 54. A sensitivity run with `cB = 0` (or a `barker` sampler on the offset block) is the next step. **Adult survival is essentially unchanged by both the constraint and the screened data:** FY2017 0.91, FY2018 0.89, FY2019 0.86, **FY2020 0.959 (0.934–0.977) vs 0.957 before**, FY2021 0.98, FY2022 0.65, FY2023 0.85, FY2024 0.91, FY2025 0.82, FY2026 0.97 (partial). **FY2021–22 product 0.632 (0.576–0.685) vs 0.636 before; Mar 2020 → Oct 2022 0.617 (0.563–0.669) vs 0.619** — still ~0.07 above the 0.543 floor, so zeroing Apr 2020 did not move the gap estimate; that residual is hierarchical shrinkage, not the dead month. `N_tag ≥ known alive` holds every month.
- **Model-structure decision (BK, 2026-09-08): no direct density effect on juvenile survival.** Rationale: density already acts on survival through recruit size — `logit(pLg) = a0 + a1·dens` (`a1 = −1.84`, well identified) sets how many recruits enter at stage L vs S, and survival is stage-specific (`dJ`, `dS`) with the S/L→A threshold being where survival changes; a separate `cB·dens` term is redundant with that pathway and, with one cohort per season, unidentifiable against `eps_J`. `eps_J` is retained for event-driven season crashes (FY2017 die-off, FY2021 scanner-off cohort loss) — it, not `cB`, is what released adult survival from the juvenile crashes. Implementation in `rd3_code`: `cB[s] <- 0` (deterministic; `lphiL` expression unchanged), `cB` removed from `make_3s_inits()`, from the survival-offset `RW_block` in `rd3_configure()`, from `rd3_monitors`, and from `core_pars` in `YCB3S_NIMBLE.R`. Note that `dS ≈ 0` and Phase 0 re-sighting are flat 100–300 mm, so the size→survival link is effectively binary at the adult threshold; `lpsiSL`/`lpsiLA` remain constant (only 3 juvenile stage re-observations, so a density-dependent maturation rate is not attempted). **No-cB refit launched 12:10 as job `E5E647F7`** (writes `data/YCB3S_MCMC.RData` / `model/YCB3S_traceplots.pdf`; the centered-`eps_J` fit archived as `data/YCB3S_MCMC_centeredEpsJ.RData` / `model/YCB3S_traceplots_centeredEpsJ.pdf`). **Result (completed 13:02, 43 min): the cleanest fit so far and the working variant going forward.** Mixing: max R-hat 1.25 (was 1.55), nodes > 1.1 fell 28 → 9, and all 9 are final-season/last-recruitment quantities (`U[1,10,·]`, `RecJ[1,9]`, `r[1,9]`, `eps_J[1,10]`, `lphiS/L[1,10]` — FY2026 is partial and has n0 = 7) plus `N_tag[1,29,2]`. `dJ[1] = −3.15 (−3.57, −2.66)`, n.eff 54 → 111; `eps_J` R-hats all ≤ 1.10 for FY2017–FY2025 (FY2018 1.47 → 1.06, FY2023 1.31 → 1.02) with n.eff 106–309; `sigma_J = 1.51`, `sigma_phi = 1.05`, `a1 = −1.83` unchanged. Season adult survival unchanged to 2–3 decimals (FY2017 0.91, FY2018 0.90, FY2019 0.85, FY2020 0.96, FY2021 0.97, FY2022 0.65, FY2023 0.85, FY2024 0.91, FY2025 0.82, FY2026 0.97). `eps_J`: FY2017 −1.6, FY2021 −3.0, FY2023 −0.8, FY2018 +0.8 (now clearly positive: the one surviving cohort), FY2020/FY2022 +1.4. `N_tag ≥ known alive` every month. Saved as `data/YCB3S_MCMC.RData` / `model/YCB3S_traceplots.pdf`.
- **Phase 4 primary survival covariate: adult density, not fall recruit density (BK hypothesis, 2026-09-08).** Rationale: recruit size at fall netting is the *outcome* of first-summer competition, so `rec_total` is a proxy for what already happened; the model's juvenile survival (`dJ`, `eps_J`) is entirely post-netting, when 150–300 mm fish share the adults' resource base. Fall harvest also makes `rec_total` nearly irrelevant to post-netting density (FY2020: 2,887 handled, 2,877 harvested, ~10 tagged returned; FY2023: 1,283 handled, 1,000 harvested). Implied recruit-to-adult probability from the no-cB fit is nearly flat across observed densities (0.07 at ≥ 300 recruits → 0.11 at 40 → 0.20 at 7; bounded by passage probabilities `pSL` 0.21 (0.07–0.44) and `pLA` 0.30 (0.13–0.54)), so the fitted size pathway explains only a small part of the FY2018-vs-dense-cohort contrast; the rest sits in `eps_J`. **YCB cannot test the hypothesis:** modeled adult abundance (tagged `N_tag` Oct + `U[1,y,3]`) ranges only ~2× (270–545; biomass 360–700 kg) vs 400× for recruits; FY2018 (the surviving cohort) had mid-range adults (~480, 546 kg), and the lowest-adult seasons FY2024–FY2026 still produced small recruits and Feb–Mar juvenile crashes. Recruit size at fall tracks recruit number, not adults (FY2017 120 recruits/418 adults → largest recruits; FY2024–25 ~300 adults → `pL` 0.07/0.00), so **keep `a1` on recruit density for the size split** and put adult density on post-fall survival. **Planned implementation:** `lphiL[j,y] = lphiA[j,y] + dJ + cA·adultDens[j,y] + eps_J[j,y]` with `adultDens` = adult biomass per unit area (`Atot × Wbar[,,3] / Area`, latent) or, simpler and fixed, standardized log known-alive tagged adults per hectare in October; requires `data/BWPondAreas.csv`. Contrast comes from IP Ponds 1/3/4 vs YCB (IP1 small recruits re-seen ~35 % vs 1–4 % at YCB). Consider the same covariate on `lpsiSL`/`lpsiLA` only if IP ponds add juvenile stage re-observations (YCB has 3).
- **Phase 3 validation report written and rendered (2026-09-08, `model/YCB3S_validation.qmd`) against the no-cB fit.** Results: known-alive constraint holds all 70 months; adult survival within CI of floors in every well-scanned season (FY2017 0.908 vs floor 0.917, FY2018 0.895/0.893, FY2019 0.854/0.821, FY2023 0.851/0.875, FY2024 0.914/0.931); FY2025 floor 0.689 is truncated by the data end (only ~5 months re-sighting), FY2020/FY2021 floors (0.86/0.84) are conditioned on surviving the gap; Mar 2020 → Oct 2022 span 0.626 (0.574–0.679) vs floor 0.548. Juvenile cohorts: model-implied survival to next Oct is 0.11–0.20 for FY2019/FY2022/FY2024 vs re-seen 0.006/0.02/0.08 — the model over-predicts small-fish survival in the dense cohorts and matches FY2018 (0.31 vs 0.375). Recruit size split PP: 4 of 9 seasons p < 0.05 (FY2019 0/484 vs expected ~8; FY2024 26/366 vs ~8; FY2021, FY2022 slightly) — the single-slope logistic in log density is too smooth. Netting join: 1 of 11 adult rows p < 0.05 (Oct 2016, 14/27 tagged vs PP 3–13; interacts with `U0`). Convergence: 9 nodes R-hat > 1.1, all FY2026/last-recruitment quantities except `N_tag[1,29,2]` and `eps_J[1,3]` (1.10). Remaining from the Phase 3 checklist: sensitivity refits (`a1 = 0`; WAIC or held-out `nL`) not done.
- **Next: Phase 3b — 12-month calendar (plan file, sketched 2026-09-08).** Set `N_MOY = 12` (`T = 120`, `gap ≡ 1`), drop the `pmin` in `prim_floor()`, remove the three `month(Date) %in% c(10:12, 1:4)` filters, keep `tApr = Primary$t[Primary$moy == 7]`, `NMOY = 12` in `RD_constants`; `d_moy` becomes 12-level automatically; optional season-specific `g_eff`. Build to `data/YCB3S_data_m12.RData` so the current build stays intact; fix `tOct` derivation in `YCB3S_validation.qmd`. Expect ~1.7× run time. First-refit checks: `N_tag ≥ known alive` all 120 months, FY2021/FY2022 `lphiA` separation vs floors 0.85 / ~0.80, summer `d_moy` at the Oct/Nov/Apr level. Port to `BWRobustDesign_data.R` afterwards.
- **Phase 3b data build done (2026-09-08 15:45): `data/YCB3S_data_m12.RData`** built with `CALENDAR = "m12"` alongside the untouched m7 build. m7 → m12: fish-months with K > 0 81,841 → 135,877 (+54,036 summer), fish-months with a detection 10,440 → 15,474, detection-weeks 21,930 → 33,245, known-alive code-4 months 18,863 → 28,565; same 2,584 fish, 295 stage observations, 30 netting rows (May 2017 netting now lands in May, not April); 15 fish now enter in a summer month, 285 have `last_alive_t` in a summer month, none removed in summer. **FY2021/FY2022 are now informed directly:** May–Aug 2021 (728/708/739/90 h; 198/173/144/9 fish) and May, Aug, Sep 2022 (761/1,686/169 h; 238/221/187 fish) vs 26 and 0 fish-detection-months under m7. Weekly detection among known-alive fish is similar across seasons (Oct–Apr 0.40, May–Sep 0.39; by month Oct 0.51 … Mar 0.26, Apr 0.29, May 0.55, Jun 0.37, Jul 0.24, Aug 0.28, Sep 0.48), supporting a shared `lp` with 12-level `d_moy`. Screening dropped 20 weeks (3 new: Aug 2017 wk 1 dead at 326 h/221 alive; May 2020 wk 1 and May 2025 wk 4 low-effort); 26 pond-months have K = 0 (14 under m7); `eff_std` floor now also catches Oct 2023 (27 h). `YCB3S_NIMBLE.R` now has a `CALENDAR` setting ("m7"/"m12") that derives `DATA_FILE`, `OUT_FILE` (`data/YCB3S_MCMC[_m12].RData`) and `TRACE_FILE`, and stops if the loaded `BUILD_INFO$CALENDAR` does not match; `rd3_settings` records `CALENDAR`/`DATA_FILE`. `YCB3S_validation.qmd` `tOct` is now `Primary$t[Primary$moy == 1]` (the old `tApr + 1` is May under m12) but the QMD still loads the m7 files.
- **First m12 fit (job `FF04A722`, completed 2026-09-08 16:41, 59 min; `data/YCB3S_MCMC_m12.RData`, `model/YCB3S_traceplots_m12.pdf`). Same model code as the no-cB m7 fit; `d_moy` 12-level automatically.** Convergence: hyperparameters R-hat ≤ 1.04; 26 nodes > 1.1 (max 1.29) — final-season recruitment (`U[1,10,·]`, `r/RecJ[1,9]`, also `r/RecJ[1,4]`, `U[1,5,1]`) and `N_tag` in Jan–Jun 2021 and Jan–Jul 2023 (1.11–1.15). `N_tag ≥ known alive` holds in all 120 months (also at the 2.5 % quantile). **Season adult survival now separates in the gap:** FY2021 0.865 (0.821–0.904) vs floor 0.840, FY2022 0.779 (0.730–0.824) vs 0.791 (m7 had 0.97 / 0.65); `sigma_phi` 1.05 → 0.56 (0.29–1.00); other seasons unchanged within 0.01 except FY2020 0.958 → 0.933 and FY2026 0.97 → 0.94. `dJ` −3.15 → −2.70, `b_post[1,2]` −0.67 → −1.23 (netting-tagged post-release penalty larger, base juvenile penalty smaller), `sigma_J` 1.51 → 1.09, `lp[1,1]` 0.00 → 0.09, `g_eff` 0.20 → 0.13, `a1`/`lpsi` essentially unchanged. **Gap decomposition against direct floors (Mar-2020 adults, no removals):** Mar 2020 → May–Jul 2021 model 0.883 vs floor 0.809 (157/194); May–Jul 2021 → May–Sep 2022 model 0.814 vs 0.815 (128/157) — matched; May–Sep 2022 → ≥ Oct 2022 model 0.901 vs 0.711 (91/128); whole 31-month span 0.647 (0.598–0.694) vs 0.485 (94/194; m7 gave 0.615). So the model is right where both ends are scanned and too high across stretches unscanned on one side (Apr 2020 → Apr 2021 has zero scanning; the late-summer-2022 loss cannot be localized by a season-constant monthly rate, which spreads it over Oct 2021–Sep 2022). Report those two stretches with the direct floors, not the model. **`d_moy` (Oct = 0):** Nov −0.30, Dec −0.37, Jan −0.35, Feb −0.67, Mar −0.99, Apr −0.80 (identical to m7), May +0.22, Jun −0.54, Jul −1.15, Aug −0.91, Sep −0.04 — summer within the Oct–Apr range; adult weekly detection 0.23 (Jul) to 0.53 (May). **Season-specific `g_eff` not warranted:** observed vs model detection of known-alive adults agrees by month within 0.06; logit residual slope on `eff_std` is 0.15 (SE 0.19) Oct–Apr vs −0.09 (SE 0.27) May–Sep, residual SD 1.16 in both. Large month residuals are year-specific in both seasons (summer 2017 observed 0.03–0.16 vs expected 0.25–0.56 at 1,150–1,500 h; May 2022 0.92 vs 0.54; Jul 2018/Aug 2018 0.08 vs 0.24–0.28) — a pond-month detection random effect would be the relevant extension, not a summer slope.
- **Validation report re-rendered against m12 (2026-09-08 17:10, `model/YCB3S_validation.html`).** Two **month-count bugs fixed** in the QMD that affected the earlier m7 report: `months = t_next - entry_t` (§3.2) and `phi^(t_oct20 - t_mar20)` (§3.3/§6.2) used primary-index differences, which under m7 gave 6 instead of 11 months for a Nov netting → next Oct and 2 instead of 7 for Mar → Oct 2020. Corrected juvenile-cohort comparison (survival to next Oct, post-release rate for 6 months then S rate, maturation ignored), m12 / m7 model vs re-seen: FY2018 0.18 / 0.20 vs **0.375** (the one surviving cohort is now *under*-predicted), FY2019 0.085 / 0.060 vs 0.006, FY2022 0.016 / 0.044 vs 0.020, FY2024 0.095 / 0.096 vs 0.079, FY2025 0.025 / 0.016 vs 0.003; the earlier note that the model gave "0.11–0.20 for dense cohorts" was inflated by the span bug — the real over-prediction is FY2019 and FY2025 (~0.03–0.08 above), with FY2024 matched. Size-split PP identical across calendars (4 of 9 seasons p < 0.05: FY2019, FY2021, FY2022, FY2024) — that sub-model does not see the calendar. Gap legs (QMD adult definition, 221 adults): Mar 2020 → May–Jul 2021 model 0.883 vs floor 0.819; May–Jul 2021 → May–Sep 2022 0.814 vs 0.840; May–Sep 2022 → ≥ Oct 2022 0.901 vs 0.757; whole 0.647 vs 0.548. Netting join 0 of 30 rows p < 0.05 (Oct 2016 adult row no longer flagged). Status bullets otherwise as in the m12 fit entry above.
- Later candidates: longer run or `barker` sampler for the survival-offset block; sensitivity with `sigma_phi` fixed smaller to test whether `cB` becomes identifiable; sensitivity with `cB = 0`.
- **Resolved 2026-09-09:** surface areas for all 7 backwaters (YCB + IP1–6) gleaned from Marsh et al. 2024 (see `Study Locations` table and `documents/Maturation of an off-channel.pdf`) and written to `data/BWPondAreas.csv` (previously all `NA`, forcing `Area = 1`). **All existing `YCB3S_data*`/`YCB3S_MCMC*` builds (including the current v11 four-pond fit) predate this change and used `Area = 1` (recruits per pond, not per acre) — `dens`/`dens_std`/`a1` and any future adult-density covariate must be treated as provisional until the data are rebuilt with real areas.** Re-run `model/YCB3S_data.R` (all builds it produces) before the next fit or sensitivity run that reports density effects.
- **Phase 4 data build (2026-09-08 17:15): `data/YCB3S_data_xyte_m12.RData`** — `YCB3S_data.R` now defaults `MODEL_PONDS <- c(1, 2, 4, 5)` (override before sourcing; `MODEL_PONDS <- 1L` reproduces the YCB-only files) and derives a pond tag in `OUT_FILE` (`""` for YCB-only, `_xyte` for all XYTE, `_p<ponds>` otherwise). Other edits: `RecruitIndex$netted` is now pond-specific (was season-only, so a YCB netting marked IP seasons as netted); `Wbar` falls back to a species-level mean when a pond has no fish of a stage (IP4 has never handled a stage-S fish); reconciliation adds a per-pond detection table and prints `Wbar` for every pond. **Regression check passed:** pond-1 `y`, `K`, `stobs`, `post` identical to `data/YCB3S_data_m12.RData`; `eff_std` and `dens_std` differ because standardization is pooled across ponds (areas still all `NA` → `Area = 1`, so `dens` is recruits per pond). Build summary: 5,071 fish (YCB 2,584 / IP1 866 / IP3 1,007 / IP4 614), 81 netting rows, `NSPLIT` 20 (IP1 6 rows, IP3 3, IP4 2 — the IP3/IP4 rows have n0 ≤ 3), 611 stage observations (IP1 has 13 S + 10 L re-observations vs YCB's 3; IP3/IP4 adult only), 79,479 known-alive fish-months, 82 weeks dropped by screening (39 low-effort, 43 dead), 11 pond-months with no available week beyond the YCB ones. **IP-specific facts:** no tagged removals at any IP pond (the only IP removal record is the Pond 3 → 5 hopper `003C06F28D`, a GIEL excluded as a species mismatch along with `003D796065`); stockings by season match known events (IP1/3/4 300 each FY2017; FY2025 restock 100/700/302); netting-tagged recruits IP1 313 S + 64 L (FY2019–FY2024, `pL_obs` 0.04–0.31), IP3 7, IP4 12; no `origin = 3` fish at IP. **Screening notes:** IP1 FY2022 under m12 is much less of a hole than the m7 diagnostics implied — Oct–Nov 2021 dead (dropped), Jan–Feb 2022 no effort, Dec 2021 (277 h, 12/183) and Mar 2022 (23 h) weak, but Apr–Sep 2022 detect 0.23–0.82 of known-alive fish at full effort, so the season was kept whole. IP3 summer 2023: May had 26/26 detected, so `p_season` = 0.71 and 14 zero-contact weeks Jun–Sep (25 fish alive, 1 fish/month seen) were dropped as dead — borderline, since IP3 summer FY2024 detection was equally low (0–0.21) without being dropped; left as is. **Driver updated and first four-pond fit launched (2026-09-08 17:18, job `311558E8`, "YCB3S all-XYTE m12 fit").** `YCB3S_NIMBLE.R` now has `PONDS <- c(1, 2, 4, 5)` alongside `CALENDAR`; both derive `DATA_FILE`/`OUT_FILE`/`TRACE_FILE` via the same pond tag + calendar suffix as the data script, and the script stops if `BUILD_INFO$MODEL_PONDS`/`CALENDAR` do not match. Output: `data/YCB3S_MCMC_xyte_m12.RData`, `model/YCB3S_traceplots_xyte_m12.pdf`; `rd3_settings$PONDS` recorded. Same model code as the no-cB m12 YCB fit (no adult-density covariate yet — areas still `NA`). Pre-launch uncompiled build: 2,420 unique histories, initial logProb −203,665, 0 non-finite data nodes, 78 samplers. Expect ~3–4 h (YCB m12 took 59 min). **On completion check:** `N_tag ≥ known alive` for all 4 ponds × 120 months; per-pond `lp[j, ]` and `lphiA[j, y]` vs known-survivor floors (IP1 FY2022 especially); `lpsiSL`/`lpsiLA` now informed by IP1's 23 juvenile re-observations; `a1` with IP1's six size-split rows added; IP3/IP4 collapse seasons FY2023–FY2024; `eps_J` for IP ponds (should be ≈ 0 where no juveniles were tagged); mixing of `U`/`RecJ` for IP ponds (`Uk = 1`, so `U[j,1,·] = 0`).
- **Four-pond m12 fit result (job `311558E8`, completed 2026-09-08 21:15, 236 min; `data/YCB3S_MCMC_xyte_m12.RData`).** `N_tag ≥ known alive` holds in all 480 pond-months (also at the 2.5 % quantile). **R-hat caveat:** 473 nodes > 1.1 but almost all are `N_tag` months pinned by the known-alive constraint (within-chain SD ~1e-8, so R-hat is a floating-point artifact — ignore `N_tag`/`Tal` R-hats in this fit); real mixing problems are `dJ[1]` (R-hat 1.31, n.eff 14; chain 3 mean −1.58 vs −1.84/−1.82), `lpsiSL[1]` 1.18, `b_post[1,2]` 1.15, `eps_J[2,3]` (IP1 FY2019) 1.33. **Adult survival vs direct floors (adults at entry alive in Oct → seen ≥ next Oct)** agrees within ~0.05 in most pond-seasons (IP1 FY2018–FY2021, FY2023; IP3 FY2018–FY2021, FY2023–24; IP4 FY2018–FY2021), but **FY2022 was a catastrophic year at all three IP ponds**: only 7/105 (IP1), 23/204 (IP3) and 4/169 (IP4) original adults alive in Oct 2021 were seen after Oct 2022 (floors 0.07 / 0.11 / 0.02), and the model gives 0.40 / 0.29 / 0.24 — well above, because a season-constant monthly rate plus shrinkage to `mu_phi` (`sigma_phi` 0.56 → 1.33) cannot represent a late-summer die-off (IP1 known-alive fell 175 → 144 → 79 → 69 in Jul–Oct 2022). Report FY2022 IP survival from the floors, not the model; consider a month-of-season or event-specific survival term if this matters. IP1 FY2024 is under-estimated (0.78 vs floor 0.90); YCB unchanged from the YCB-only fit within 0.03 (FY2023 0.82 vs floor 0.88). **Parameter shifts vs the YCB-only m12 fit:** `dJ[1]` −2.70 → −1.75; stocked post-release offset `b_post[1,1]` −3.02 (−3.27, −2.78), now informed by the IP stockings; `b_post[1,2]` −1.22 unchanged; `lpsiSL` −3.2 → −1.39 (20 %/month), `lpsiLA` −2.08 → −2.38; `a1` −1.83 → −2.72 (but `Area = 1`, so cross-pond "density" is recruits per pond and IP1's 17–71 recruit seasons sit at low `dens_std` — not interpretable until areas are in); `g_eff` 0.13 → 0.415; `sigma_J` 1.09 → 2.73 with `eps_J` taking large values in pond-seasons with **no** tagged juveniles (IP1 FY2018 −5.3, IP4 FY2017 +3.0, FY2023 −3.0, FY2024 +2.3; YCB FY2026 +8.1) — there it is driven only by the untagged pool/netting join and should be constrained (e.g. `eps_J = 0` where no juveniles were tagged, or a tighter `sigma_J` prior). Detection: juvenile `lp` YCB −0.17, IP1 −1.35, IP3 −2.54 (7 juveniles, uninformed), IP4 +0.39; adult `lp` YCB −0.51, IP1 +0.34, IP3 −0.83, IP4 −0.18 — the IP1 juvenile–adult gap (−1.35 vs +0.34) is the "low-detection juvenile" pattern again, now with 313 S fish behind it. `U0[1,3]` (YCB untagged adults at start) 286 (182–390); IP `U0` are prior-only as intended. Validation QMD not yet parameterized for the four-pond fit.
- **Known die-off event term added (2026-09-09, BK-confirmed cause, not data-derived).** IP1/IP3/IP4 had a documented near-total mortality event in late summer/fall 2022 (Bonytail ponds unaffected; YCB had no known adult die-off, only the already-modeled FY2018 juvenile loss via `eps_J`). New `data/BWDieOffEvents.csv` (`Backwater`, `Species`, `StartMonth`, `EndMonth`, `Cause`, `Notes`) records windows pinned from the known-alive series (continuous full-effort scanning throughout, so these are deaths, not detection gaps): IP1 Aug–Oct 2022 (175→144→79→69, ~40% survived), IP3 Jun–Sep 2022 (204→160→80→60→40→26, ~13%), IP4 Jun–Sep 2022 (149→97→26→14→6, ~3%). **`YCB3S_data.R`** reads the CSV into `DieOffsRaw`, builds `Evt[pond, t]` (0/1 primary-month indicator, via `prim_floor()`) and `nEvt[pond, season]` (calendar-month count per FY, computed directly from dates so it is exact regardless of `CALENDAR`); both added to `RD_constants` and the saved `.RData`; `BWDieOffEvents.csv` added to the `BUILD_INFO` mtime guard. Rebuilt `data/YCB3S_data_xyte_m12.RData` confirms the windows land as expected: IP1 2 months FY2022 + 1 (October) in FY2023, IP3 4 months FY2022, IP4 4 months FY2022. **`YCB3S_defs.R`**: `rdPond3` gains `evt`/`dE` arguments; a fixed offset `dEt <- dE * evt[t]` is added to `phS`/`phL`/`phA` inside the per-month loop (applies to all three stages alike). `rd3_code` adds `dE[j] ~ T(dnorm(0, sd = 3), , 0)` (pond-specific, survival-reducing-only prior; ponds with no recorded event are prior-only) and passes `evt = Evt[j, 1:T], dE = dE[j]` into `rdPond3`; the untagged-pool annual survival (`phiUS/UL/UA`) now splits each season's 12 months into `12 - nEvt[j,y]` baseline months and `nEvt[j,y]` event months at the `dE`-adjusted rate, so `U`/`RecJ` see the same event discount as the tagged model. `dE` added to inits (`-runif(NP, 0.2, 1)`) and `rd3_monitors`. **Caveat noted in code:** this construction assumes `gap == 1` (true for `CALENDAR = "m12"`); under `m7` a 6-month Apr→Oct gap that only partly overlaps an event would apply `dE` to the whole gap. **Pre-launch check:** uncompiled 4-pond model builds with finite initial logProb (−221,095 with the event term vs −203,665 without — the event nodes and their likelihood raise the deviance as expected since dE conditions on data previously left to lphiA), 0 non-finite data nodes, 82 samplers (4 more than the no-event build, one per pond's `dE`). Previous fit (no event term) archived as `data/YCB3S_MCMC_xyte_m12_noEvt.RData` / `model/YCB3S_traceplots_xyte_m12_noEvt.pdf`. **Refit launched 2026-09-09 09:18, job `40FFEA44` ("YCB3S all-XYTE m12 fit with dE die-off term"), writes `data/YCB3S_MCMC_xyte_m12.RData` (overwriting the no-event fit).** On completion check: `dE[2:4]` (IP1/IP3/IP4) should be large and negative, `dE[1]` (YCB) should sit near its prior (no event data); FY2022 `lphiA[j,7]` for IP ponds should now sit close to the pre-/post-event floors rather than being pulled toward `mu_phi`; `sigma_phi` should shrink back down for species 1 now that the FY2022 outlier is absorbed by `dE` rather than `lphiA`; check mixing of the new `dE` block and whether `N_tag ≥ known alive` still holds in the event months.
- **Job `40FFEA44` result — this is "v10" (completed 2026-09-09 14:56, 218 min, `data/YCB3S_MCMC_xyte_m12.RData`).** Windows: IP1 Aug–Oct (2 months FY2022 + 1 = Oct in FY2023), IP3/IP4 Jun–Sep (4 months FY2022). `dE` well-identified, none truncated at 0: IP1 −2.56 (−2.91, −2.20), IP3 −2.63 (−3.07, −2.18), IP4 −4.16 (−4.70, −3.66), YCB (prior-only) −2.41 (−6.82, −0.09). Model FY2022 adult survival vs empirical floors (adults known alive Oct 2021 recontacted after Oct 2022): **IP1 0.227 (0.167–0.293) vs floor 0.067 — model far too high**; IP3 0.096 (0.062–0.138) vs floor 0.113 — floor inside the CI, good fit; IP4 0.0075 (0.0026–0.016) vs floor 0.024 — floor just above the CI, mildly too severe. Root cause of the IP1 mismatch (`cond_surv()` monthly diagnostic, conversation of 2026-09-09): true IP1 crash is Jul (surv 0.82) → Aug (0.55, worst) → Sep (0.87); the coded Aug–Oct window drags in a normal-survival October (0.99, borrowing FY2023's higher baseline `lphiA`) while missing the reduced-survival July, diluting the fitted `dE`. IP3/IP4 Jun–Sep already captures each pond's respective worst month (IP3 Jun 0.67, though its single worst month Apr→May 0.50 falls just before the window; IP4 Jun 0.27) and needs no change to fit acceptably.
- **v11 window correction (2026-09-09 16:15+): IP1 shifted to Jul–Sep; IP3/IP4 unchanged at Jun–Sep.** `data/BWDieOffEvents.csv` IP1 row edited (`StartMonth`/`EndMonth` 2022-07/2022-09); v10 outputs archived as `data/YCB3S_data_xyte_m12_v10.RData`, `data/YCB3S_MCMC_xyte_m12_v10.RData`, `model/YCB3S_traceplots_xyte_m12_v10.pdf` before rebuilding. `YCB3S_data.R` gained a `DIEOFF_FILE` setting (default `"data/BWDieOffEvents.csv"`; added to `BUILD_INFO` and the mtime guard) so alternate event tables can be swapped in without editing the script. New `data/BWDieOffEvents_ip3MaySep.csv` is a ready-to-use sensitivity variant with IP3 widened to May–Sep (its worst single month, Apr→May surv 0.50, falls just before the working Jun–Sep window) — **not yet fit**; set `DIEOFF_FILE <- "data/BWDieOffEvents_ip3MaySep.csv"` before sourcing `model/YCB3S_data.R` to build it (writes the same `OUT_FILE` as the main build unless also overridden — save under a different name, e.g. `data/YCB3S_data_xyte_m12_ip3MaySep.RData`, to avoid clobbering the working file). Rebuilt v11 data confirms IP1 `nEvt[2,]` is now `3` in season 6 only (was `2` in season 6 + `1` in season 7); IP3/IP4 `nEvt` unchanged (`4` in season 6). **v11 refit launched 2026-09-09 16:18, job `B7414372` ("YCB3S all-XYTE m12 fit, v11 die-off windows"), same settings as v10 (3 chains, 30k/10k burn/thin 10, seeds 6173/2948/8805), writes `data/YCB3S_MCMC_xyte_m12.RData` (overwriting v10; v10 preserved under the `_v10` suffix above).** Expect ~3.5–4 h. **On completion check (v11 vs v10):** `dE[2]` (IP1) should move more negative and the FY2022 `lphiA[2,6]` (now a single-season quantity, no longer split with `lphiA[2,7]`) should sit closer to the 0.067 floor; IP3/IP4 `dE`/`lphiA[·,6]` should be essentially unchanged (same window); re-verify `N_tag ≥ known alive` for all pond-months, especially IP1 Jul–Sep 2022; compare max R-hat / n.eff to v10 (real mixing issues to watch: `dJ[1]`, `lpsiSL[1]`, `b_post[1,2]`, `eps_J[2,3]`); check whether `eps_J[2,6]` (IP1 FY2022) shrinks now that more of the crash is captured by `dE` directly.
- **v11 fit result (job `B7414372`, completed 2026-09-09 21:49, 331 min; `data/YCB3S_MCMC_xyte_m12.RData`).** Note: this build's data (`data/YCB3S_data_xyte_m12.RData`, built 16:17:49) still predates the `BWPondAreas.csv` area correction (written 16:27:04 the same session) — **v11, like all prior YCB3S fits, was fit with `Area = 1` for every pond**; see the `a1` sensitivity check below for what changes once real acreage is used. Convergence: 68 non-`N_tag`/`Tal`/`U` nodes with R-hat > 1.1 (237 raw, but all `N_tag`/`Tal` entries are floating-point artifacts of the known-alive pin — verified chain SDs ~1e-6 to 1e-8 on several flagged nodes); real problem nodes are `dJ[1]` (R-hat 2.92, n.eff 28), `lpsiSL[1]` (n.eff 32), many `eps_J[j,y]` (R-hat up to 2.6), `Atot`/`RecJ`/`r` for IP3/IP4 (R-hat 1.1–1.3, recruitment nuisance parameters as in prior fits); `a0[1]`/`a1[1]`/`dE[·]`/`g_eff`/`lp[·]` all mix well (n.eff 600–6000). `N_tag ≥ known alive` verified by joint posterior quantile (not marginal-per-stage, which falsely flags violations) — **holds for all 480 pond-months, 0 violations**. Key posteriors: `a1[1] = -2.72 (-3.07, -2.38)` (matches a standalone GLM fit on the same Area=1 density almost exactly, confirming the size-split submodel is effectively separable from the rest of the joint model); `dE[1]` (YCB, prior-only) `-2.44 (-6.82, -0.09)`; `dE[2]` (IP1, now Jul–Sep) `-3.08 (-3.58, -2.63)`, more negative than v10's `-2.56` as expected from including July; `dE[3]` (IP3) `-2.64 (-3.09, -2.21)` and `dE[4]` (IP4) `-4.16 (-4.69,-3.67)` essentially unchanged from v10 (same window). **FY2022 realized annual adult survival (baseline monthly `phiA` for the 12-nEvt non-event months × event-adjusted rate for the nEvt event months) vs the direct empirical floor (adults known alive Oct 2021 seen after Oct 2022):** IP1 0.292 (0.225–0.365) vs floor 0.067 — **surprisingly *further* above the floor than v10's 0.227**, despite `dE[2]` moving more negative, because the FY2022 baseline `lphiA[2,6]` itself rose (0.791 monthly^12, vs implied ~0.70 in v10) as other parameters re-equilibrated in the full refit; IP3 0.096 (0.061–0.140) vs floor 0.113 — floor inside the CI, good fit, unchanged from v10; IP4 0.0076 (0.0026–0.016) vs floor 0.024 — still mildly too severe, unchanged from v10. **Conclusion: the IP1 window correction did not fix the FY2022 IP1 mismatch and made the point estimate slightly worse; a season-constant `dE` combined with hierarchical shrinkage of `lphiA[2,6]` cannot fully localize a single-pond crash while the model is refit jointly with 3 other ponds.** All other seasons' baseline adult survival essentially unchanged from the no-event-adjusted pattern reported for the prior fit (YCB FY2017–FY2026 0.78–0.96; IP1 0.75–0.98 outside FY2022; IP3 0.39–0.98, its FY2026 collapse (0.39) reflects partial-season data; IP4 0.66–0.99 outside FY2022). Validation QMD still not parameterized for the four-pond fit.
- **`a1` sensitivity to real pond acreage (2026-09-10).** Rebuilt `model/YCB3S_data.R` (`MODEL_PONDS = c(1,2,4,5)`, `CALENDAR = "m12"`) with the now-populated `data/BWPondAreas.csv` (YCB 2.6, IP1 8.8, IP3 14.2, IP4 8.6 acres) to `data/YCB3S_data_xyte_m12_realArea.RData`, without re-running the 5.5-hour NIMBLE fit. Because `cB = 0` (fixed), pond/season density (`dens_std`) only enters the model through the recruit size-split submodel `logit(pLg[j,y]) = a0 + a1·dens_std[j,y]`, `sp_nL ~ dbin(pLg, sp_n0)` — a likelihood term with no feedback from any other model parameter, so a standalone `glm(cbind(nL, n0-nL) ~ dens_std, family=binomial)` on the 20 `SplitObs` rows is a very close proxy for the joint-model `a1` posterior (confirmed: the Area=1 GLM gives slope -2.78 (SE 0.18) vs the actual v11 joint posterior `a1[1] = -2.72 (-3.07, -2.38)` — a near-exact match). **Result: real acreage substantially attenuates the effect.** Old (Area=1, density = recruits/pond): intercept -0.49 (SE 0.19), slope -2.78 (SE 0.18). New (real acres, density = recruits/acre): intercept -1.45 (SE 0.13), slope **-1.91 (SE 0.12)** — about 31% smaller in magnitude, still highly significant (z = -15.9). The two `dens_std` vectors correlate at r = 0.97 across the 20 pond-season rows (YCB's huge density swings dominate both), but dividing by area pulls the three much-larger IP ponds' already-small recruit counts down further relative to YCB, shifting both the standardization center and the fitted slope. **Practical implication:** `a1`, `dJ`, `dE`, and everything else in every existing `YCB3S_MCMC*`/`YCB3S_data*` file (through v11) should be treated as fit on a "recruits per pond" density scale, not "recruits per acre" — a full data rebuild + refit with real areas is needed before reporting a definitive per-acre `a1`, but this GLM check indicates the sign and significance of the density-dependent size-split effect are robust to the correction; only its magnitude shrinks by roughly a third.
- **v12: IP1 x FY2022 adult-survival deviation term + real acreage (BK request, 2026-09-10).** v11's `dE[IP1]` (Jul-Sep 2022 window) still left modeled FY2022 IP1 adult survival (0.292) far above the empirical floor (0.067) because a season-constant event offset, fit jointly with three other ponds, cannot fully localize a single-pond crash under hierarchical shrinkage. Added a targeted extra deviation on top of the existing machinery rather than restructuring `lphiA` into a literal `base_A[y] + pond_A[j]` decomposition (the current hierarchical draw `lphiA[j,y] ~ N(mu_phi[sp[j]], sigma_phi[sp[j]])` already plays that role for every other cell, and a full re-parameterization risked destabilizing convergence project-wide for a single-cell fix). **Implementation:** new scalar `eps_A_IP1_FY2022 ~ dnorm(0, sd = 0.5)` (fixed SD, not hierarchical — this is a one-off deviation, not a shared class like `eps_J`); new deterministic `lphiA_eff[j,y] <- lphiA[j,y] + eps_A_IP1_FY2022 * IP1FY22[j,y]`, where `IP1FY22[j,y]` is a 0/1 constant (1 only at IP1's pond index x FY2022's season index, computed in `model/YCB3S_data.R` from `PondTable$Backwater == "IP1"` and `2022 - FIRST_FY + 1`; 0 everywhere else, including all YCB-only builds, so no other pond-season is touched). `lphiA_eff` replaces `lphiA` everywhere it previously fed downstream: the `rdPond3` call's `lphiA` argument, `lphiL`'s formula, and `phiUA` (untagged-pool annual adult survival) — `lphiA` itself is unchanged and still monitored for comparison. The existing `dE[j]` die-off event offset (Jul-Sep 2022 for IP1) still applies inside `rdPond3`/`phiUA` on top of `lphiA_eff` for event months, as requested (no change to `dE`/`Evt`/`nEvt` machinery). `rd3_monitors` and `YCB3S_NIMBLE.R`'s `core_pars` gained `eps_A_IP1_FY2022` and `lphiA_eff`; `make_3s_inits()` gained `eps_A_IP1_FY2022 = rnorm(1, 0, 0.2)`; `rd3_configure()` blocks `eps_A_IP1_FY2022` jointly with IP1's `lphiA[j, 1:NS]` RW_block (IP1's pond index found from `which(rowSums(IP1FY22) > 0)`; a no-op in YCB-only builds). **Data rebuilt with real acreage** (`data/YCB3S_data_xyte_m12.RData`, 2026-09-10): `Area` now YCB 2.6, IP1 8.8, IP3 14.2, IP4 8.6 acres (previously all 1) — `dens_std`/`a1` in this build are the first all-XYTE fit on a true per-acre density scale (see the sensitivity note above for the expected ~31% attenuation of `a1`'s magnitude vs the recruits-per-pond scale, though the exact number will differ slightly now that ponds are fit jointly rather than via the standalone GLM proxy). **Pre-launch check passed:** compiled build has 5,071 fish, 2,420 histories, `IP1FY22` sums to exactly 1 (row 2 = IP1, col 6 = FY2022), `lphiA_eff` differs from `lphiA` at exactly that one cell, initial logProb finite after `repair_3s_inits()` (-200,940). Previous fit archived as `data/YCB3S_MCMC_xyte_m12_v11.RData` / `model/YCB3S_traceplots_xyte_m12_v11.pdf`. **v12 refit launched 2026-09-10 08:45, job `069BF914` ("YCB3S all-XYTE m12 fit, v12: eps_A_IP1_FY2022 + real acreage"), same settings as v11 (3 chains, 30k/10k burn/thin 10, seeds 6173/2948/8805), writes `data/YCB3S_MCMC_xyte_m12.RData` (overwriting v11, archived above).** Expect ~4-5.5 h (v10/v11 took 218/331 min). **On completion check:** posterior for `eps_A_IP1_FY2022` (expect strongly negative if it captures IP1's residual FY2022 gap); FY2022 `lphiA_eff[IP1's pond, 6]` / realized annual adult survival vs the 0.067 empirical floor (should now sit much closer than v11's 0.292); whether `dE[IP1]` moves (may partially trade off against `eps_A_IP1_FY2022` since both discount the same season — check their posterior correlation); mixing of the new scalar and of `lphiA[IP1,6]`/`lphiA_eff[IP1,6]`; `a1` and the size-split slope on the corrected per-acre `dens_std` (compare to the GLM sensitivity check's -1.91 (SE 0.12), noting the joint fit's scale will differ somewhat from the standalone-GLM proxy since areas changed the standardization); `N_tag >= known alive` for all 480 pond-months (join with joint quantile as in v11, not marginal); other ponds/seasons unchanged from v11 to confirm the targeted deviation had no side effects.
- **v12 result (job `069BF914`, completed 2026-09-10 12:57, 252 min; `data/YCB3S_MCMC_xyte_m12.RData`).** First fit on real per-acre density (`Area`: YCB 2.6, IP1 8.8, IP3 14.2, IP4 8.6 acres) and with the targeted `eps_A_IP1_FY2022` deviation. **`a1[1] = -1.909 (-2.149, -1.678)`** — matches the standalone-GLM sensitivity check (-1.91, SE 0.12) almost exactly; the size-split submodel is confirmed separable and this is now a genuine per-acre effect (previous builds all used `Area = 1`). **`eps_A_IP1_FY2022 = -0.14 (-1.04, 0.74)` did not fix the IP1 FY2022 mismatch:** realized FY2022 annual adult survival at IP1 (baseline `lphiA_eff` + `dE` over the 3 Jul-Sep event months) is 0.290 (0.223-0.362) vs the empirical floor of 0.067 — essentially unchanged from v11's 0.292; the extra scalar deviation was estimated as prior-driven (CI spans 0) and added no real correction, because hierarchical shrinkage toward the cross-pond mean dominates a single free scalar when fit jointly with 3 other ponds. IP3 (0.097, 0.062-0.140 vs floor 0.113) and IP4 (0.008, 0.003-0.016 vs floor 0.024) unchanged from v11 as expected (their windows/mechanism untouched). Other core parameters essentially unchanged from v11 (`dJ[1] = -1.64`, `lpsiSL[1] = -1.28`, `lpsiLA[1] = -2.41`, `dE[IP1/IP3/IP4]` all clearly negative, `sigma_J[1] = 2.71`). **Convergence got worse, not better:** `dJ[1]` R-hat 3.51 (n.eff 15, worst in the model), several `eps_J[j,y]` R-hat 1.8-2.6; detection/size-split/`g_eff` all still converge well (R-hat <= 1.05). `N_tag`/`Tal`/`U`/`RecJ`/`r` bulk R-hat flags remain the known floating-point artifact of the known-alive constraint, not real mixing failures. **Decision (BK, 2026-09-10): stop extending the three-stage density-dependent architecture as a shared cross-pond model.** v12 is the terminal fit in this line — the IP1 FY2022 crash cannot be adequately localized by any season-and-pond-constant offset once fit jointly with 3 other ponds under hierarchical shrinkage, and juvenile-deviation (`dJ`/`eps_J`) mixing has degraded across the last two refinements rather than improved. `YCB3S_validation.qmd` was never re-parameterized for the four-pond fit and is not planned to be. Future single-pond work (e.g. a closer look at IP Pond 1's FY2022 die-off, or YCB-only refinements) should fork from `data/YCB3S_MCMC_xyte_m12.RData` / `model/YCB3S_data.R` with `MODEL_PONDS <- 1L` or `2L` rather than extending the joint 4-pond model further.

---

## Bonytail (GIEL) Model Structure Assessment (2026-09-10)

Spot-check of whether the YCB three-stage density-dependent architecture should be ported to the three GIEL ponds (IP2, IP5, IP6), or whether the existing joint 2-stage `BWRobustDesign` model (already split at `StageTL` = 250 mm for GIEL) is the right base. Method: built scratch NIMBLE-style inputs via `model/YCB3S_data.R` with `MODEL_PONDS <- c(3L, 6L, 7L)` (IP2/IP5/IP6), `StageBreaks$GIEL = c(200, 250)`, `CALENDAR = "m12"`, saved to `data/GIEL_scratch_data.RData` (not part of the pipeline; safe to delete).

**Findings:**
1. **Size vs. survival supports a 2-stage split, not 3.** Netting-tagged juveniles (n = 1,438) re-sighted >= 365 d later, by size class, controlling for cohort-year FY (FY alone matters: LRT vs. null χ²(6) large): 250-349 mm and >=350 mm both jump up from <250 mm (log-odds +0.97 and +1.21 respectively, both p < 1e-7), but do **not** differ from each other (diff 0.25 +/- 0.20, z = 1.2, p = 0.23). One break at 250 mm (the existing `StageTL`) is what the data support; no evidence for a second break at ~350 mm as in XYTE.
2. **No adult-density signal to fit.** Known-alive adult density (Oct census) ranges 0-29 fish/acre and swings by an order of magnitude year to year, but the swings are discrete crash/rebound events (see die-off confirmation below), not a smooth density gradient - a `cA*adultDensity` survival covariate would have no leverage independent of the known events.
3. **Recruit density too sparse/skewed for a growth-density submodel.** Recruit density (untagged juveniles at fall netting, per acre) is 0 in most pond-seasons and otherwise 0.2-5.7/acre, with one outlier (IP2 season 8 / FY2024, 24.1/acre) an order of magnitude above the rest - not enough contrast to fit a `pLg(density)` size-split slope (the XYTE `a1` term).

**Recommendation:** reuse the existing 2-stage joint robust-design model (`model/BWRobustDesign_data.R` / `BWRobustDesign_defs.R`) for GIEL rather than building a three-stage density-dependent variant; it already matches what the data support (2 stages at 250 mm, no density covariates).

**Die-off events confirmed (BK, 2026-09-10) and added to `data/BWDieOffEvents.csv`.** Marsh et al. 2024 documents BONY die-offs in IP5 (2020) and IP2 (2022) attributed to human error or pump failure (see `documents/Marsh_et_al_2024_Summary.md` line 58). Known-alive series (well-scanned throughout, 700-2,300 scan hrs/month) pin down tighter windows than the paper's calendar-year citations:
- **IP5 (Pond 5), GIEL, 2020-01 to 2020-09**: known-alive fell 126->75->59->56->55->55->52->42->0 (from a post-tagging peak of 145 in Dec 2019); stayed at 0-1 fish through FY2021.
- **IP2 (Pond 2), GIEL, 2022-01 to 2022-07**: known-alive fell 121->85->76->61->55->54->39; scanning dropped to 0 h in Aug-Sep 2022 right as the population hit 0, so the window is truncated at Jul (last well-scanned month with fish present) rather than extended to the true floor.

**`dE` die-off mechanism ported to the joint 2-stage model (2026-09-10).** `model/BWRobustDesign_data.R` now computes `Evt`/`nEvt` from `data/BWDieOffEvents.csv` exactly as `YCB3S_data.R` does (new `DIEOFF_FILE` setting, `season_of_date()` helper added), and saves them into `data/BWRobustDesign_data.RData` (plus a `BUILD_INFO` mtime record covering `model/BWRobustDesign_data.R`, `data/ReportingData.RData`, `data/BWNettingEvents.csv`, `data/BWDieOffEvents.csv`). `model/BWRobustDesign_defs.R`: `rdPond` gained `evt`/`dE` arguments (fixed logit-survival offset applied over the whole gap when `evt[t] == 1`, same "assumes gap == 1 or applies to the whole 6-month Apr-Oct gap" approximation used in the XYTE 3-stage model before its m12 calendar); `rd_code` adds `dE[j] ~ T(dnorm(0, sd=3), , 0)` per pond and the same `nEvt`-based annual discount on the untagged pool (`phiUJ`/`phiUA`) as `rd3_code` uses for `phiUS/UL/UA`; `rd_monitors`/`core_pars` add `"dE"`; `make_inits()` adds `dE = -runif(NP, 0.2, 1)`. `make_rd_inputs(ponds)` now also **remaps species to 1..n present among the chosen ponds** (`NSP <- length(unique(sp[ponds]))`) instead of hardcoding `NSP = 2`, so a single-species pond subset (e.g. the 3 GIEL ponds) doesn't carry a dead, prior-only species slot the way the all-XYTE `YCB3S` builds do for species 2; `make_inits()` and `rd_configure()` were generalized from hardcoded `2` to `cst$NSP`/`model$getConstants()$NSP` to match. `model/BWRobustDesign_NIMBLE.R` gained the stale-data mtime guard that was flagged as missing in `Known Code Issues` (stops if any `BUILD_INFO$inputs` file is newer than the loaded data; warns if `BUILD_INFO` is absent, i.e. a pre-2026-09-10 build), and `RUN_PONDS`/`OUT_FILE`/`TRACE_FILE` are now overridable before sourcing (default `RUN_PONDS <- 1:7`; `RUN_TAG` is `"_giel"` for `c(3,6,7)`, `""` for all 7, else `"_p<ponds>"`).

**Verified working (not yet fit):** `data/BWRobustDesign_data.RData` rebuilt 2026-09-10 with the ported code; `make_rd_inputs(c(3,6,7))` (IP2/IP5/IP6) gives `NSP = 1`, `Evt`/`nEvt` correctly flag IP5 season 4 (FY2020) and IP2 season 6 (FY2022) from the confirmed die-off windows, 2,938 fish / 2,163 unique histories / 49 netting rows. `nimbleModel(rd_code, ...)` builds with a finite initial log-probability (-69,553) after `repair_inits()`, no dimension warnings, and `rd_configure()` builds all 49 samplers cleanly. **To launch the GIEL-only fit:** set `RUN_PONDS <- c(3L, 6L, 7L)` before sourcing `model/BWRobustDesign_NIMBLE.R` (e.g. via `rstudioapi::jobRunScript()`); it will write `data/BWRobustDesign_MCMC_giel.RData` / `model/BWRobustDesign_traceplots_giel.pdf` and not touch the all-7-pond files. Expect roughly the same ~3h wall time as the full 7-pond run scaled down for 3 ponds and 1 species (untested). On completion check: `dE[1]` (IP1... no wait, ponds are IP2/IP5/IP6 in that order) vs the confirmed event magnitude, `lphiA`/`lphiJ` in the event seasons vs the empirical floors from this session, `N_tag >= known alive`, and general mixing (R-hat) given this model has never been fit on GIEL alone before. `data/BWRobustDesign_MCMC.RData` (all 7 ponds, XYTE + GIEL jointly) is now also stale relative to the rebuilt data and the new `dE` mechanism if a joint refit is ever wanted instead.

**GIEL-only fit launched (2026-09-10 13:25).** `RUN_PONDS <- c(3L, 6L, 7L)` set before sourcing `model/BWRobustDesign_NIMBLE.R` via `rstudioapi::jobRunScript()` as a background RStudio job ("BWRobustDesign GIEL-only fit"); will write `data/BWRobustDesign_MCMC_giel.RData` / `model/BWRobustDesign_traceplots_giel.pdf`. Check `rstudioapi::jobList()` or output file mtime for completion before reading results; see the on-completion checklist in the paragraph above.

**GIEL-only fit completed (2026-09-10 14:26, 60 min; `data/BWRobustDesign_MCMC_giel.RData`).** Model indices: pond 1 = IP2, pond 2 = IP5, pond 3 = IP6 (`rd_Fish$pond_new`). **File only stores 9 objects** (`rd_code`, `rd_Fish`, `rd_Hist`, `rd_Netting`, `rd_ponds`, `rd_samples`, `rd_settings`, `rd_summary`, `rhat_all`) — load into a fresh `new.env()` when checking results in a session that has other stale `PondTable`/`RD_constants`/etc. objects from prior XYTE work lying around, or `load()` will silently leave those stale globals in place for anything not in this file (they are not overwritten and will look like they belong to this fit). `rd_summary` covers only 26 core hyperparameters; full posteriors (673 nodes incl. `lphiA`, `N_tag`, `U`, `RecJ`, `Atot`, `Tal`, `d_moy`) are in `rd_samples`. Convergence: 42/673 nodes Rhat > 1.1, all either the known-alive-pin floating-point artifact on `N_tag`/`Tal` or recruitment nuisance parameters (`RecJ`, `r`, `U`) in the last 2-3 seasons; `lphiA`, `dE`, detection, and hyperparameters converge cleanly (Rhat ≤ 1.1). `dE[IP2] = -1.74 (-3.15, -0.55)`, `dE[IP5] = -1.45 (-2.07, -0.87)`, `dE[IP6, no event] = -2.42 (-6.76, -0.09, prior-only)`. Event-adjusted realized annual survival matches the direct empirical floor well for IP5 FY2020 (model 0.041 vs. floor 0/39) but not for IP2 FY2022 (model 0.089 vs. floor 0/14) — same under-correction seen for XYTE IP1 FY2022, and traced to the *event window covering only 7 of 12 months that season* in both cases (IP5's 9/12-month window was matched well) rather than to species or pond-isolation differences. **IP2 also shows chronically low survival in non-event seasons** (floor 0.05-0.09 in FY2018/19/21/25) not explained by the single documented 2022 crash — flagged as worth checking for additional unflagged mortality episodes. Stocked-vs-wild survival split is not testable from this model as built (no origin term on established `lphiA`, only the temporary post-release `b_post` offset) despite adequate origin=2 sample sizes at all 3 ponds (IP2 857, IP5 159, IP6 423). Only 19 of 25 non-event pond-season cells have ≥15 known-alive fish, and even those swing 0.05-0.92 in survival with no apparent smooth pattern — a small, noisy base for any single covariate slope, let alone the proposed IPM's fuller stack. Full write-up of all 6 pre-registered open questions in `model/BONY_ProposedIPM_Critique.md` §8. `model/BWRobustDesign_traceplots_giel.pdf` has the trace plots.

**Classic (frequentist, MLE) annualized CJS cross-check with `marked` (2026-09-11).** A separate, much simpler point of comparison to the Bayesian robust-design model above: `model/GIEL_ClassicCJS.R` fits annual (Oct-Apr occasion, FY2017-FY2026) CJS models to the 2,637 tagged GIEL fish (all 3 ponds pooled, no pond effect), varying Phi/p by time, a "young-for-one-interval-then-adult" size class, and stocked-vs-netting-tagged origin (16-model AIC set); top model `Phi(~time+sizeclass+origin) p(~time)`. See `model/GIEL_ClassicCJS_Summary.md` for full results (`data/GIEL_ClassicCJS.RData`), including R2ucare RELEASE-style GOF tests attributing nearly all pooled-model lack-of-fit to transience (i.e., the size-class effect). **Pond-grouped extension (`model/GIEL_ClassicCJS_PondGrouped.R`, same day):** adds pond (IP2/IP5/IP6) as a `marked` group, builds 12 models from the previous top-2 pooled models up to fully-independent-by-pond Phi and p, and picks the AIC winner — `Phi(~location*time + location*sizeclass + origin) p(~time)` (pond-specific time trends and size effects, but a single shared origin effect and a single shared detection-by-year curve), though 4 nearby models including the literal "fully independent Phi and p" specification are within ~3-4 AIC (statistically tied). No pond was excluded despite 300 stocked GIEL (all at IP2) lacking a recorded tagging length — IP2 retains a fully populated origin x size cross-tab. The pond-specific `location*time` interaction lets the model directly reproduce both known die-off events (IP5 FY2020, IP2 FY2022) as near-zero survival, but with quasi-complete-separation-sized SEs in exactly those cells — flagged as a caution on point estimates, not a convergence failure. A **parametric bootstrap GOF (300 replicates) on the top model itself** (not just the null/pooled model): simulate each real fish's encounter history forward under the fitted model's real Phi/p estimates, refit the identical model, and compute deviance relative to a cohort-saturated (pond x origin x size x entry-occasion) reference; **c-hat ~ 1.37** (mean-based; bootstrap p = 0.007) — mild overdispersion, much improved over the pooled null model's crude c-hat ~ 8.4. See `model/GIEL_ClassicCJS_PondGrouped_Summary.md` for the full AIC table, real-parameter caveats, and bootstrap GOF detail (`data/GIEL_ClassicCJS_PondGrouped.RData`, `model/GIEL_ClassicCJS_PondGrouped_bootGOF.png`).

**IP5-only ANNUAL classic CJS (`model/GIEL_ClassicCJS_RobustDesign_Annual.R`, 2026-09-11) — built after the monthly robust-design version failed.** An earlier attempt (`model/GIEL_ClassicCJS_RobustDesign.R`, not otherwise documented) tried to reproduce the Bayesian joint model's exact monthly robust-design time structure (70 monthly occasions, 60 of 69 within-season transitions fixed to Phi=1 via `marked`'s `fix=1` mechanism, survival concentrated into 9 annual Apr→Oct gaps) for IP5 stocked fish only; `crm()` reproducibly hard-crashed (non-catchable, even under `callr` process isolation) on a large fraction of bootstrap-simulated histories under that design, traced to the within-season closure assumption producing degenerate/boundary outcomes in the small (~18-20 fish) "young" stratum. **Fix:** collapse to 10 ANNUAL occasions (FY2017-FY2026, the 9 between-year intervals only, no within-season transitions or `fix` mechanism at all) — every transition is a real, freely-estimated annual survival parameter. Same IP5 stocked-only fish set and "young-for-first-interval-then-adult" sizeclass logic as the failed monthly version; detection collapses to "any PIT contact in that FY's Oct-Apr window"; same parametric-bootstrap c-hat recipe but each replicate refit isolated in its own `callr` subprocess. 3 Phi structures (const/gap [9-level annual year factor, name is a holdover — NOT a 2-level die-off dummy]/dieoff [true 2-level event/normal dummy]) x 2 p structures (p~time annual, p~1). **Result (`data/GIEL_ClassicCJS_RobustDesign_Annual.RData`, job `E14241EB`, ~7.5 min, 0 crashed/suppressed bootstrap replicates):** top model `Phi(sizeclass + gap) p(.)` ("gap_pdot") by a wide AIC margin (weight 0.997 over `gap_ptime`, ΔAIC 11.8; both `dieoff_*` and `const_*` variants trail by >280 AIC — full annual time-variation beats a single event dummy). Bootstrap GOF: **c-hat 0.91 (mean) / 0.97 (median), bootstrap p = 0.53** — clean fit, no overdispersion (contrast with the pooled all-3-pond model's c-hat ~8.4 and the pond-grouped model's ~1.37). **Top model's own fitted annual survival** (adult sizeclass, `preds_ip5$Phi`), interval-start FY2017-FY2025: 0.897, 0.056, 0.941, **8.3e-6 (degenerate/boundary)**, **0.888 (SE=0, unidentifiable — see caveat)**, 0.846, 0.657, 0.579, 0.460 (plus a separate FY2017 "young" row, 0.958, for the 20 fish tagged <250mm in that cohort). The FY2020 interval (Jan-Sep 2020 die-off) collapses to an essentially-zero boundary estimate, matching the empirical floor exactly: **`floor_2020` check: 300 fish known alive at the FY2020 occasion, 0 ever seen again FY2021+.** **The following FY2021 estimate (0.888, SE=0) is not a credible, data-informed number** — with 0/300 of the original cohort surviving the crash, there is essentially no fish at risk to inform that parameter, and SE=0 is the signature of a boundary/unmoved-from-start value, not a real "recovery" estimate; do not report it as such.
- **A separate, cruder Bayesian companion calculation is also computed within this same script (§7 of `model/GIEL_ClassicCJS_RobustDesign_Annual.R`), and should not be confused with the classic-CJS fit above.** It derives an annualized survival trajectory directly from `BWRobustDesign_MCMC_giel.RData`'s `lphiA[2, y]` (pond_new 2 = IP5, a *monthly* rate) as `plogis(lphiA)^12` per season, with the FY2020 season's `dE[IP5]` offset applied across all 12 months as a simplification (not just the `nEvt[IP5, FY2020] = 9` calendar months actually flagged by the event window). Posterior means (`annual_surv_bayes`): 0.814, 0.154, 0.745, **0.021**, 0.136, 0.783, 0.627, 0.542, 0.419, 0.463 for FY2017-FY2026. **A more precise version, using the correct 9-of-12-month `nEvt` weighting** (`plogis(lphiA)^3 * plogis(lphiA+dE)^9` for FY2020 only, all other seasons unchanged) gives **0.041 (95% CI 0.017-0.077)** for FY2020 instead of 0.021, all `lphiA[2,]`/`dE[2]` R-hat <= 1.05. **This Bayesian companion trajectory is a pond-level, all-fish (stocked + netting-tagged) seasonal rate and is *not* directly comparable, year-by-year, to the stocked-only, freely-time-varying classic CJS Phi above** (e.g. FY2018: Bayesian 0.154 vs classic-CJS 0.056; FY2021: Bayesian 0.136 vs classic-CJS's uninformative 0.888) — the two use different fish subsets, different time indexing (per-season monthly rate vs. per-interval annual Phi), and different die-off mechanisms (a pond-level `dE` offset vs. a fully free, unconstrained yearly parameter). **The one place they are genuinely comparable is the FY2020 crash itself and the shared 0/300 empirical floor**, where both collapse to a near-zero survival estimate (classic CJS 8.3e-6; Bayesian 0.021-0.041) — this is the extent of the "external validation" the two models provide each other; it is not a season-by-season agreement. Full write-up in `model/GIEL_ClassicCJS_RobustDesign_Annual_Summary.md`.
- **Stocked-only Bayesian sensitivity check (2026-09-11), to test whether restricting the Bayesian comparison to stocked-only fish narrows the gap with the classic CJS.** IP5's 600 stocked GIEL (`rd_Fish |> filter(pond_new==2, origin==1)`) form exactly the classic CJS's 2 cohorts (300 tagged 2017-03-21, 300 tagged 2022-03-09) — confirms the two models' populations line up as expected outside the origin/detection differences already noted. Two variants tried: (1) a calendar-precise version tracking each cohort's actual 12 months post-stocking (mixing two `lphiA` seasons, since March-tagged fish span both the stocking FY and 5 months of the next FY) gives first-year survival 0.405 (FY2017 cohort) and 0.710 (FY2022 cohort) — very different from the whole-season figures below, because the FY2017 cohort's first 12 months happen to overlap 5 months of FY2018's poor `lphiA`; this instability shows the calendar-precise framing is highly sensitive to an arbitrary choice (annual CJS occasions have no fixed within-year reference point, so this version is not preferred). (2) The **whole-season `b_post` approximation** (apply the stocked-origin post-release offset `b_post[1,1]` to the *entire* stocking-FY season, mirroring the existing whole-season `dE` convention) is preferred for consistency: FY2017 0.809 (vs 0.814 all-fish), FY2018 0.154 (unchanged), FY2019 0.745 (unchanged), FY2020 0.041 (unchanged), FY2021 0.136 (unchanged), FY2022 0.777 (vs 0.783 all-fish), FY2023-FY2026 unchanged. **Conclusion: restricting to stocked-only fish moves the Bayesian trajectory by ≤0.006 in every season** — because `b_post[1,1]` (stocked-origin post-release offset) has posterior mean only -0.03 logit units (essentially negligible; per the existing note in this model, "no origin term on established `lphiA`, only the temporary post-release `b_post` offset"). **This is a structural, not a data, limitation: the joint model has no mechanism for stocked- and netting-tagged/established-origin fish to have different *long-run* annual survival at IP5** — origin only enters as a small, temporary, imprecisely-estimated 6-month offset. The stocked-only restriction therefore does **not** resolve the year-by-year disagreement with the classic CJS's genuinely stocked-only, freely-time-varying Phi (e.g. FY2018 classic 0.056 vs Bayesian stocked-only 0.154; FY2019 classic 0.941 vs 0.745; FY2021 classic 0.888 [itself unreliable, see above] vs 0.136) — the disagreement traces to model structure (pond-level shared survival vs. fully free annual parameters), not to origin-pooling in the Bayesian fish set. Full write-up (including the true classic-CJS Phi estimates side by side, corrected after an earlier mislabeling) in `model/GIEL_ClassicCJS_RobustDesign_Annual_Summary.md` §7.1.

---

## GIEL Growth-to-250mm-Threshold Side Analysis (`model/GIEL_GrowthSeasonThreshold.R`, 2026-09-12)

Side project (BK request), separate from the joint robust-design / classic-CJS survival work above: estimates the probability that a bonytail first captured/stocked below the 250 mm TL size-class threshold at IP2, IP5, or IP6 grows past 250 mm TL after one Apr-Sep growth season.

**Data limitation.** Of 936 GIEL first tagged/stocked <250 mm TL at these 3 ponds (2017-2024), only **41** have any later physical NFWG capture/stocking record with a measured TL — PIT scanner contacts (`StudyBWContacts`) do not record length, so a "recapture with known growth" is limited to opportunistic physical recaptures, not the full known-alive population. This is the entire usable sample for this analysis; results should be read with that in mind (see caveat below).

**Growing-season exposure.** For each of the 41 recapture pairs, growth is assumed to occur only during Apr 1-Sep 30 (`gs_days`, the intersection of `[Date1, Date2]` with that window each year spanned) rather than raw elapsed calendar days — this matches the joint robust-design / YCB3S models' Apr→Oct between-year gap convention. **12 of the 41 pairs fall entirely within Oct-Mar** (`gs_days = 0`, elapsed 42-120 days) and were dropped: an exploratory plot of increment vs. elapsed days colored by first-capture month showed these winter-only pairs have near-zero growth (5-35 mm), confirming they carry no information about the growing-season rate. The remaining **29 pairs** land at 182, 364, 546, or 728 `gs_days` (1-4 full seasons).

**Model.** Bayesian NIMBLE growth-increment regression fit through the origin (increment = 0 at `gs_days = 0`): `increment ~ Normal(rate[origin] * gs_days + u[pondyear] * gs_days, sigma)`, `u[pondyear] ~ Normal(0, sigma_py)`. Pond and year are combined into a single `PondYear` grouping factor (9 levels, 1-6 fish each) rather than two separate variance components, since per-pond-per-year samples are too sparse to identify both. Origin (capture vs. stocking) is a candidate fixed effect. Stepwise term reduction is via WAIC comparison across all four nested structures (full, origin-only, PondYear-only, neither) rather than classical AIC/LRT on `glm()`, since n = 29-41 was expected to risk separation under plain MLE.

**Results.**
- On raw calendar days (n=36, excluding the 5 pairs >700 days elapsed), linear-in-days clearly beat log1p(days) by WAIC, but neither `origin` nor `PondYear` improved WAIC over a fully-pooled model (all four structures within ~3 WAIC units) — winning (simplest) model: single rate ~0.34 mm/day (~125 mm/year), residual SD 35 mm.
- Restricting exposure to growing-season days only (n=29) **reverses that finding**: `origin` still drops (cost ~0.2 WAIC), but `PondYear` is now clearly retained (WAIC 312.7 vs. 332.0 fully pooled, ~19-unit gap) — once winter noise is excluded, real pond/cohort differences in growing-season growth rate become visible (e.g. the 2017 Pond 6 cohort notably slower, `u = -0.315`; the 2019 Pond 6 and 2021 Pond 2 cohorts notably faster, `u = +0.239`/`+0.249`; most other cohorts near zero). Winning model: population growth rate **0.561 mm/day growing-season time (SD 0.099) → ~102 mm per full Apr-Sep season**, `sigma_py = 0.242` (~40% CV between pond-years), residual SD 46.9 mm, all R-hat ≈ 1.00.
- Posterior-predictive **P(TL ≥ 250 mm after one growth season | TL1)**, marginalizing over the pond-year distribution: 0.50 (TL1=150mm), 0.65 (175mm), 0.78 (200mm), 0.87 (225mm), 0.92 (249mm). Notably lower than the raw-calendar-day model's implied one-year probabilities (0.76-1.00), because that model implicitly credited winter days with growth.

**Caveat.** The 29-41 fish are an opportunistic subset of physically recaptured fish, not a random resample of the 936 tagged <250 mm — if capture probability correlates with size or growth rate, these estimates may be biased, and this cannot be corrected with available data. Per-pond-year sample sizes (1-6 fish) are small; treat the `u[pondyear]` deviations as suggestive, not definitive. Output saved to `data/GIEL_GrowthSeasonThreshold.RData` (`recap_pairs`, `gs_data`, `gs_fit_data`, the four NIMBLE fits `gs_full`/`gs_noorigin`/`gs_nogroup`/`gs_neither`, `gs_waic_tab`, `growth_season_threshold_probs`).

**Refit as a Bayesian von Bertalanffy (Fabens) increment model (2026-09-12, BK request).** Replaced the linear-in-`gs_days` increment model with `increment ~ Normal((Linf - TL1)*(1 - exp(-K*t_years)), sigma)`, `K = exp(logK[origin] + v[pondyear])`, fit on the same 29 growing-season-only rows (`t_years = gs_days/365`). Priors: `Linf ~ Normal(635 mm, sd 50)` (weakly informative around the requested 62-65 cm TL) and `logK[k] ~ Normal(log(0.15), sd 0.5)` (weakly informative, median K = 0.15/yr, ~90% interval ≈ 0.06-0.4/yr, per the requested 0.1-0.2/yr center). Same four-way WAIC comparison (origin × pondyear) as before, now on the VBGF form; `model/GIEL_GrowthSeasonThreshold.R` picks the lowest-WAIC model programmatically rather than hardcoding the winner. **WAIC no longer clearly supports a PondYear term:** "pondyear only" (288.79) beats "fully pooled" (288.82) by only 0.03 — negligible, versus the linear model's ~19-unit gap; origin is still unsupported (+13 WAIC). Winning model reported is PondYear-only but is effectively tied with full pooling. **Key finding — Linf/K are not separately identified from this data:** posterior correlation r = -0.79; `Linf` moves to 556 mm (95% CI 432-691) from its 635 mm prior center, and implied `K` moves to 0.67/yr (95% CI 0.33-1.47/yr) from the requested 0.15/yr center. Mechanism: all recapture windows are short (182-728 days) and all `TL1` (117-249 mm) are far below any plausible `Linf`, so the data constrain only the product `K*(Linf-TL1)` (essentially the increment itself), not the two parameters separately; explaining the observed absolute growth (commonly 100-200 mm per half-season) while holding `Linf` near 635 mm requires `K` well above the requested 0.1-0.2/yr range. **This should be read as a genuine data limitation** (recaptures span too little of the growth curve to pin down the asymptote), not a bug — the `Linf`/`K` point estimates should not be reported as standalone growth-curve parameters; the model is more reliably used for its predicted increments/probabilities. **P(TL >= 250 mm after one growth season | TL1), von Bertalanffy model:** 0.66 (150mm), 0.79 (175mm), 0.88 (200mm), 0.94 (225mm), 0.98 (249mm) — broadly similar in shape to the linear model's 0.50-0.92, modestly higher because larger near-origin increments partly offset the now-explicit size-dependent deceleration. Saved objects unchanged in name (`gs_full`/`gs_noorigin`/`gs_nogroup`/`gs_neither`, now VBGF fits) plus new `gs_winner`, `winning_name`, `winner_use_group`, `LINF_PRIOR_MEAN`, `LINF_PRIOR_SD`, `LOGK_PRIOR_MEAN`, `LOGK_PRIOR_SD` in `data/GIEL_GrowthSeasonThreshold.RData`. `model/GIEL_GrowthSeasonThreshold_Summary.md` (pond-year deviation drivers, die-off cross-reference) describes the earlier linear model's `u[pondyear]` values and has **not** been updated for the VBGF refit — treat its specific deviation numbers as describing the retired linear model, though the underlying recapture-pair data and die-off timing cross-reference remain valid.

**Rescoped to the full size spectrum (2026-09-12, BK request).** Originally scoped to only fish first tagged <250 mm TL (to directly target the size-class threshold probability); rescoped to fit a general growth model from EVERY consecutive pair of measured `StudyBWNFWG` records per fish x backwater across all 3 GIEL ponds, any size (not just tag -> first recapture) — 314 distinct fish contribute 362 growth-increment rows (`recap_pairs`), spanning `TL1` 117-442 mm (vs. 117-249 mm before). `gs_days` in this dataset takes only 0 or exact multiples of 182 (one Apr-Sep season) — fish are captured at discrete netting/stocking events near season boundaries — so the existing `gs_days > 0` filter alone satisfies "must span at least one full growing season, not just winter," no extra threshold needed. **Negative-growth pairs removed:** 11 rows have `TL2 < TL1` (bonytail do not shrink; almost certainly measurement/transcription error), flagged into `NegativeGrowthPairs` and excluded before fitting (only 1 of the 11 would otherwise have entered the model — the rest have `gs_days = 0` and were already excluded). Full list with dates/PITs is in `model/GIEL_GrowthSeasonThreshold_Summary.md` §0 for database correction; highest-priority record is `003D772AA4` (362→258 mm, Dec 2021→Mar 2022, a 104 mm drop far outside plausible measurement noise), the other 9 flagged Pond 6 records are small (1-4 mm) Dec→Mar discrepancies that look like ordinary measurement noise. Same VBGF model/priors as before, refit on 361 rows with longer chains (60k iter/20k burn/thin 10 — the larger `origin + pondyear` model needed this to converge; R-hat was 1.8-1.9 at the original 20k/5k/5 settings, now ≤ 1.05). **WAIC now clearly favors the full `origin + pondyear` model** (3309.3) over pondyear-only (3319.5), origin-only (3328.8), and fully-pooled (3343.9) — a real ~10-35-unit gap, unlike the 29-row fit's negligible one. But almost every pond-year is entirely one origin or the other (only `P5_2022` has both, 36 capture + 141 stocking), so origin and pondyear are severely confounded outside that one cell — read the individual `logK`/`v` estimates with that in mind. `Linf = 417.7 mm (SD 6.3)`, implied `K` 0.7-1.4/yr depending on pond-year — much more tightly identified than the 29-row fit (`Linf` SD dropped from 70 to 6.3), but see the Linf investigation immediately below for why this tight estimate is still not trustworthy as a literal asymptote. `P(TL >= 250mm after one growth season | TL1)`: 0.49 (150mm), 0.57 (175mm), 0.68 (200mm), 0.80 (225mm), 0.92 (249mm) — similar shape to, and only modestly different from, the original 29-row VBGF fit's 0.66-0.98.

**Linf-below-observed-max-size investigation (2026-09-12).** The rescoped fit's `Linf` (417.7 mm) sits below the largest observed post-growth length (`TL2` max = 462 mm) — every posterior draw has `Linf < max(TL2)`. Tested two fixes, neither adopted: (1) **per-pond `Linf`** (3 independent priors, same origin+pondyear `K` structure) — WAIC 3311.7, tied with (very slightly worse than) the single-`Linf` model; Pond 2's `Linf` is essentially prior-only (627, SD 50.8 — that pond has almost no large fish, max `TL1` 296), Pond 5 partially informed (474, SD 27.7), and **Pond 6 alone converges to an even lower `Linf` (410.6, SD 6.6)** than the pooled model — giving the pond with the big fish its own asymptote does not fix the problem, because the same many-small-vs-3-large-fish tension exists within that one pond. (2) **Tight `Linf` prior** (`sd = 15` instead of 50, forcing `Linf` toward 635 mm) — WAIC 3458.7 (149 units worse) and clear systematic residuals: small fish (`TL1` < 200 mm) underpredicted by ~50 mm on average, mid-size fish (300-400 mm) overpredicted by ~17 mm. **Diagnosis:** classic Fabens-estimator bias toward low `Linf`/high `K` when individual fish vary in their own growth trajectory and few recaptures exist near the true asymptote — here only 3 of 361 rows have `TL1 > 400 mm` (all Pond 6, 2022 cohort, still growing 17-42 mm/season at that size), so those few points cannot outweigh hundreds of smaller/faster-relative-growth rows under a shared-`Linf`/`K` structure. **Conclusion: not fixable by reparameterizing `Linf` with the current data.** The single-global-`Linf`, loose-prior model (WAIC-best) is retained, but its `Linf`/`K` should not be read as literal growth-curve parameters — use the model only for interpolated increment predictions within the well-populated size range (~117-350 mm); do not extrapolate near/beyond 400 mm. A real fix would require more recaptures of fish already above ~400-450 mm. Alternative-fit objects saved to `data/GIEL_growth_pondLinf_fit.rds` and `data/GIEL_growth_tightLinf_fit.rds` (not part of the main pipeline).

**Final decision (2026-09-12, BK): move forward with the current model's P(TL >= 250 mm) predictions.** No further `Linf`/`K` restructuring is planned after the §0b investigation (per-pond `Linf` and tight-prior alternatives both fit worse or no better). Adopted predictions, TL1 -> P(TL2 >= 250 mm) (from `growth_season_threshold_probs`): 150 mm -> 0.491, 175 mm -> 0.574, 200 mm -> 0.676, 225 mm -> 0.800, 249 mm -> 0.917. **Standing rule: any report or document that cites these numbers must repeat the `Linf`/`K` identifiability caveat** (the fitted `Linf` = 417.7 mm is a data-driven compromise, not a literal asymptote, informed by almost no fish above 400 mm) -- the canonical caveat language lives in `model/GIEL_GrowthSeasonThreshold_Summary.md` §0c and in the `GIEL_GrowthSeasonThreshold.R` header/section-4 output. The predictions are valid for interpolation (TL1 150-249 mm, well inside the well-populated size range) and should not be extended to TL1 near or above ~400 mm.

**Pond-year deviation drivers and die-off cross-reference (`model/GIEL_GrowthSeasonThreshold_Summary.md`, 2026-09-12).** Unpacked which specific recapture points drive the two most extreme `u[pondyear]` deviations, since several groups have only 1-2 points. **P6_2017 (-0.315, n=3)** is dominated by a single fish (`0015AC37D8`) whose one recapture spans 728 growing-season days (2017-2021, 5 calendar years) at only 0.172 mm/day — a third of the population mean — while the group's other two (one-season, 2017-only) fish are near/above average (0.54-0.60 mm/day); this is one long-exposure fish's multi-year average dominating the fit, not three fish agreeing. **P6_2019 (+0.239, n=1)** is a single fish isolating the 2020 season at 1.10 mm/day, the fastest rate in the dataset — pure individual variation with n=1. **P2_2021 (+0.249, n=2)** is the one small group where both fish agree (0.89, 1.09 mm/day) — a real signal, not a single outlier — and it is the growing season immediately before IP2's 2022 die-off (see below), flagged as a WQ follow-up candidate (fast growth in survivors the year before a crash, possibly reduced competition from an already-declining population). **Die-off timing from `TotalCountIPGIEL` monthly known-alive counts** pins both confirmed GIEL die-offs to a single month each, both landing in the hypothesized summer-heat/early-autumn-DO-crash window: **IP2 fell 54 (Jun 2022) → 39 (Jul) → 0 (Aug 2022)**; **IP5 fell 52 (Jul 2020) → 42 (Aug) → 0 (Sep 2020)**. There is mechanically **no Pond 2 recapture pair covering the 2022 season and no Pond 5 pair covering the 2020 season** in the growth dataset — expected, since both ponds had almost no survivors left to recapture by those seasons, not additional evidence of the crashes. **Pond 6 has no documented die-off and no acute month-to-month drop** in its 2017-2023 known-alive series (smooth gradual decline only, unlike IP2/IP5's single-month collapses), so the P6_2017/P6_2019 outlier points read as individual variation rather than an unflagged Pond 6 event, given currently available data. No water-quality (temperature, DO) data exist yet for any pond; every WQ linkage here is a hypothesis for when that data becomes available, keyed to two specific windows: **IP2 Jul-Aug 2022** and **IP5 Aug-Sep 2020**.

---

## GIEL Age-Based Growth Model (`model/GIEL_VBGF_AgeLength.R`, 2026-09-14)

A second bonytail growth model, complementary to the increment (Fabens) model above: builds
a real age-length dataset (not just paired increments) from two age sources — (1) known
stocking-cohort year class (`StudyBWNFWG$year_class`, populated for the 2017-03-21 cohort,
899 fish, year class 2012, and the 2023-04-12 cohort, 300 fish, year class 2018; both ~5.2 yr
old at release, typical of bonytail hatchery production; the 2022-03-09 stocking, 300 fish,
has no recorded year class and is excluded) and (2) assumed young-of-year fall recruits (a
per-FY 2-component `mclust` mixture on log(TL) among untagged Oct-Dec first-captures with no
year class; FY2018 excluded for too few fish, FY2021 has no fall captures at all). Full
detail, including the mixture-classification log and per-source age ranges, is in
`model/GIEL_VBGF_AgeLength_Summary.md`.

**L0 anchoring (2026-09-14).** The initial fit used an uninformed `t0` intercept, believing no
literature hatch-length value existed for bonytail. BK corrected this: Hamman, R.L. 1982.
"Induced Spawning and Culture of Bonytail Chub." *The Progressive Fish-Culturist* 44(4):
201-203, doi:10.1577/1548-8659(1982)44[201:ISACOB]2.0.CO;2, reports a mean bonytail hatch
length of 6.8 mm TL (range 6.5-7.5 mm). The model was refit with `L0 ~ Normal(6.8, 0.25)` mm
replacing `t0` — this substantially changed the fitted curve (`Linf` 703 → 486 mm) since
anchoring the age-0 intercept at a real point requires faster early growth to reach observed
sizes, pulling the asymptote down. **Standing lesson: always check for a literature hatch/
larval-size value before defaulting to an uninformed `t0` in any future VBGF work on this
project's species** — it's easy to wrongly assume none exists (as the first draft of this
model did).

**Shared-K, release-anchored revision (2026-09-14, BK hypothesis, same day) — current model.**
BK hypothesized that stocked (captive-reared) and wild-recruit bonytail actually grow at the
*same rate* once both are in the pond, and that the L0-anchored model's large origin effect
(K[Stocked] 0.221/yr vs. K[Recruit] 0.540/yr, non-overlapping CIs) was an artifact of blending
~5.2 years of slow hatchery growth with subsequent pond growth into one continuous hatch-
anchored curve for stocked fish, rather than a real pond-growth-rate difference.

*Direct empirical test:* almost every pond-year cell in the increment model's data is
single-origin, EXCEPT IP5 (Pond 5) 2022-23, where the 2022 stocking cohort (released at
TL = 250 mm) overlaps in size and time with 36 previously-established fish. Restricting both
to one-growing-season (182-day) pairs and regressing increment on `TL1 + Origin` (n = 143)
gave a small, non-significant origin effect (-18.3 mm/season, SE 11.3, p = 0.108) — much
smaller than the ~2.4x K ratio implied by the hatch-anchored model, supporting the hypothesis
that the origin effect elsewhere is a pond-year confound, not a true growth-rate difference
(caveat: thin overlap, n=6 near the exact 250mm start size, and the two groups' growing
seasons are one calendar year apart).

*Model fix:* stocked-fish records are now anchored at their OWN release size/date (Fabens-
style reset) instead of an assumed hatch date, and the origin-specific `logK` is dropped in
favor of a single shared `K` (still varying by pond-year/cohort via `v[pondyear]`):
`mu_i = TLrel_i + (Linf - TLrel_i)(1 - exp(-K_i * t_i))` for stocked rows (`t_i` = years since
that fish's release anchor; `TLrel_i` = its release-anchor TL, both known data), vs. the
unchanged `L0`-anchored hatch-age form for recruit rows. Release-anchor caveat: the true
stocking record has a measured TL for the 2012 cohort but NOT the 2018 cohort (no
`total_length` at that stocking event) — for the 2018 cohort the anchor falls back to each
fish's first later measured record (Dec 2023, ~244 d post-release), so growth from true
release to first measurement is unobserved for those 80 of 300 fish; they end up carrying
zero weight for K (an anchor row is a tautology at t=0), rather than misleadingly informing a
blended rate as before. All real K information for stocked-origin fish now comes from just
**89 of 979** release-anchored 2012-cohort fish with any post-release repeat measurement.

*Result: CONFIRMS the hypothesis on the full dataset, not just the one IP5 cell.* WAIC is a
statistical tie between the primary shared-K model (13,646.35) and a sensitivity check
re-adding an origin-specific `logK` (13,646.87, Delta = 0.52) — a reversal from the
hatch-anchored version's ~800-unit gap favoring origin. In the origin-check model, K is
nearly identical between origins (StockedCohort 0.718/yr [0.47-1.01] vs. AssumedRecruit
0.768/yr [0.63-0.91]; P(K[Stocked] < K[Recruit]) = 0.63, ~coin flip). The shared-K primary
model gives `Linf` = 406.1 mm (95% CI 395.9-416.7, much tighter than the hatch-anchored
version's 464-509) and `K` = 0.792/yr (0.68-0.92); residual sigma dropped from 22.7 mm to
17.0 mm (better fit with fewer parameters). Convergence improved too (R-hat <= 1.02 vs.
<= 1.06) — same barker-sampler block over `{Linf, logK}` as the L0 revision (curved
Linf-logK ridge; requires `buildDerivs = TRUE`).

**New caveat from this revision: `Linf` is now below the observed max TL (442 mm) — the
single highest posterior draw across all chains is 427.5 mm, 0% posterior support for
`Linf` >= 442.** This is the SAME identifiability failure already flagged for the increment
(Fabens) model (`GIEL_GrowthSeasonThreshold_Summary.md` §0b) and was avoided by the previous
hatch-anchored fit — it reappeared here likely because real growth information on large/old
fish is thin under release-anchoring (only 89 informative stocked fish; recruit data tops out
at 408 mm). **Read this model's `Linf`/`K` as fitted-compromise values for interpolation, not
a literal asymptote** — the WAIC/origin comparison (the point of this revision) is
well-identified and not undermined by this, but the specific curve parameters should not be
over-reported. Full detail, tables, and all caveats: `model/GIEL_VBGF_AgeLength_Summary.md`.
Three fits are archived for comparison: `data/GIEL_VBGF_AgeLength_t0.RData` (original `t0`
form), `data/GIEL_VBGF_AgeLength_originDiff.RData` (`L0`-anchored, hatch-age, origin-
differentiated), and the current `data/GIEL_VBGF_AgeLength.RData` (shared-K, release-anchored).

**Standing lesson for future stocked-vs-wild growth/survival comparisons on this project:**
when a captive-rearing or hatchery phase precedes release, do not assume one continuous growth
(or survival) regime from origin (hatch/birth) to present for stocked animals — anchor at
release when release size/date are known, and test for a residual origin effect only after
that fix, ideally using any available same-pond/same-time overlap between stocked and wild
individuals as a direct check before trusting a model-based origin comparison.

---

## Cibola High Levee Pond Linf Sensitivity Check (2026-09-14, historical, standalone)

BK asked whether **Cibola High Levee Pond** (LID 10222, Cibola NWR, Reach 4 — a
different location from the backwater sites, but a similarly closed, PIT-scanned pond)
could supply recent large-fish (>400 mm TL) growth-recapture pairs to help anchor the
backwater GIEL growth models' poorly-identified `Linf` (both models above independently
found `Linf` collapses below their own observed max TL, a documented Fabens-bias
artifact of too few large-fish recaptures). **Finding: Cibola cannot supply RECENT
large-fish data** — of 60–71 GIEL growth-recapture pairs there, the 18 involving a fish
>400 mm are all from 2001–2005; only 3 pairs postdate 2015, all small fish; and no GIEL
were physically recaptured (NFWG) at Cibola at all after 2017 (PIT scanning continued
through 2024 but never records length). Per BK's follow-up request, the pre-2005
large-fish pairs were fit anyway as an explicit **historical sensitivity check**, not a
data source for the production growth models: `model/GIEL_Cibola_LinfSensitivity.R`
(single-population Fabens/VBGF, no origin/pondyear grouping, same priors as the
backwater models: `Linf ~ Normal(635, sd 50)`, `logK ~ Normal(log(0.15), sd 0.5)`), 52
usable pairs (TL1 189–504 mm) after dropping 13 negative-growth and 2 winter-only pairs
from the 67 pre-2005 candidates. **Result:** converges cleanly (R-hat ≤ 1.001, unlike
the backwater fits' `Linf`-`logK` ridge); `Linf = 662 mm (95% CI 581–749)`, `K =
0.316/yr (0.237–0.420)`, with **zero posterior support for `Linf` below this dataset's
own observed max (510 mm)** — the opposite identifiability failure from the backwater
fits, and consistent with the hypothesis that the backwater models' ~406–418 mm `Linf`
is an artifact of missing large-fish data rather than a true asymptote. **Caveat: this
result is itself prior-sensitive** — a wide-prior fit (`Linf` sd 300 instead of 50)
gives `Linf = 834 mm (sd 155)` instead of settling back down, so only the *direction*
(backwater `Linf` likely biased low) is robust, not the magnitude. Standing caveats
(data 20–30 years old, different pond/era, unverified Apr–Sep growing-season assumption
for Cibola, no origin/cohort grouping) are detailed in
`model/GIEL_Cibola_LinfSensitivity_Summary.md`; full narrative and code comments in
`model/GIEL_Cibola_LinfSensitivity.R`. **This check does not modify or feed back into**
`GIEL_GrowthSeasonThreshold.R`, `GIEL_VBGF_AgeLength.R`, or their saved `.RData`/summary
files — it is purely an external plausibility check. Output saved to
`data/GIEL_Cibola_LinfSensitivity.RData`.

**Comparison figure (2026-09-14):** `model/GIEL_Cibola_LinfComparison.png` overlays
posterior-median VBGF curves (95% ribbons) for the backwater model and the Cibola
historical fit from a common reference start (TL0 = 100 mm), with jittered rug tick
marks showing each dataset's observed `TL1` values along the left edge. The curves
track closely through ~350–400 mm then diverge sharply as the backwater curve
flattens at its own `Linf` while Cibola's continues rising — visually confirming the
divergence begins exactly where backwater's data support ends.

**Follow-up: does pooling Cibola into the production growth model help? (2026-09-14,
`model/GIEL_GrowthSeasonThreshold_withCibola.R`, output
`data/GIEL_GrowthSeasonThreshold_withCibola.RData`, summary
`model/GIEL_GrowthSeasonThreshold_withCibola_Summary.md`).** BK asked whether Fabens
growth rate (`K`) differs significantly between backwater and Cibola in their
overlapping size range, and if not, whether adding Cibola's data into the production
model helps anchor `Linf`. **Step 1 (site-comparison test, TL1 189–442 mm overlap,
349 backwater + 45 Cibola pairs):** genuinely borderline — WAIC narrowly favors a
pooled `K` (3667.1) over a site-specific `K` (3668.9, Delta = 1.9, below the
conventional ~2-unit threshold), but `K[Backwater] = 0.896/yr (0.651–1.151)` vs.
`K[Cibola] = 0.630/yr (0.390–0.878)`, with the 95% CI on `logK[Cibola] -
logK[Backwater]` (−0.805, +0.007) barely failing to exclude zero and P(K[Cibola] <
K[Backwater]) = 0.974 — a real but underpowered (45 pairs) hint of a difference. BK
decision: treat as not-significant per the project's standard WAIC convention and
pool with a single shared `K` (still varying by cohort/origin) going forward. **Step
2 (combined production model, all 361 backwater + 52 Cibola pre-2005 pairs, n =
413):** WAIC again selects the full origin + group structure (3844.4, same as the
backwater-only model), converges cleanly (max R-hat 1.03). **Result: `Linf` moved up
from 417.4 mm (backwater-only) to 435.5 mm (95% CI 421.5–451.5)** — in the expected
direction, but the identifiability problem is **not resolved**: posterior P(Linf <
max observed size, 510 mm) = 1 in the combined fit too, because `Linf` is shared
across all groups/sites (per the Step 1 pooling decision) and the 361 backwater rows
still numerically dominate the 52 added Cibola rows. Derived P(TL≥250mm after one
growth season) predictions all increased (e.g. TL1=150mm: 0.491 → 0.643; TL1=249mm:
0.917 → 0.985) — see the summary file for the full table. Comparison figure
`model/GIEL_GrowthModel_CombinedVsOriginal.png`. **Status: this combined fit is
offered as a documented alternative, not yet adopted as the sole production model —
the original `GIEL_GrowthSeasonThreshold.RData` (backwater-only) remains the primary
reference until BK decides otherwise.** All caveats from the standalone Cibola check
carry over (20–30-year-old data, different pond/era, unverified growing-season
assumption, borderline/underpowered site test) plus a new one: every Cibola row is
`Origin = "Capture"`, so there is no Cibola stocking-origin data to compare against
backwater's stocked cohorts.

---

## GIEL Size-Tied Maturation Hazard — Design Plan for the Joint Robust-Design Model (planned, NOT implemented, 2026-09-14)

**Status: design only — no code has been changed.** BK requested this plan while deliberately holding off on implementation, pending (1) review of the age-based growth model's output and (2) a search for additional data to better anchor `Linf`/`K`. This section exists so a future session can implement the change without re-deriving the reasoning; treat every item below as a proposal, not a completed step.

**Motivation.** The joint 2-stage robust-design model (`model/BWRobustDesign_defs.R`) represents GIEL juvenile-to-adult (J->A) maturation with a single scalar `lpsi[sp] ~ dnorm(-2.4, sd=1)` — a species-level, size-blind, time-constant monthly logit-hazard (`psi_m <- ilogit(lpsi)`, gap-adjusted as `pst[t] <- 1-(1-psi_m)^g`). Every tagged juvenile matures at the same rate regardless of its actual TL at tagging/capture. BK asked (2026-09-14) for a design that replaces this with a hazard tied to size at tagging, ramping toward ~100% probability by the second growing season, using **the age-based, shared-K, release-anchored VBGF growth model** (`model/GIEL_VBGF_AgeLength.R`; `data/GIEL_VBGF_AgeLength.RData`; objects `al_final`/`samps_final`) as the authoritative growth source for this purpose. This choice was made explicitly, without first reconciling against the increment/Fabens model's own threshold curve (`model/GIEL_GrowthSeasonThreshold.R`) — the two models' smooth P(TL1 -> >=250mm after one growing season) curves diverge meaningfully (e.g. at TL1=150mm: 0.27 age-model vs. 0.49 increment-model; at TL1=249mm: 0.995 vs. 0.917; comparison computed in the 2026-09-14 conversation, not yet saved to a script). BK is separately gathering more growth-anchoring data before implementing any of this, so that reconciliation remains open.

**Step 1 -- precompute an external maturation-hazard lookup table (offline, outside NIMBLE).** Using `al_final`'s posterior (`samps_final`: `Linf`, `logK[1]`, `sigma_v`, `sigma`), compute a hazard surface over a grid of TL bins (e.g. 10-20mm width spanning ~100-249mm) and elapsed months since tagging (`delta_t = 1..24`, i.e. up to two growing seasons):
- For each TL bin's representative TL1 and each `delta_t`, project forward via the anchor-independent Fabens/VBGF identity `TL2 = Linf - (Linf - TL1) * exp(-K_i * delta_t/12)`, drawing a fresh individual/pond-year `K_i = exp(logK + N(0, sigma_v))` per posterior draw (the same marginalization used for the standalone smooth curve computed 2026-09-14), plus observation noise `sigma`.
- `F(TLbin, delta_t) = P(TL2 >= 250)`, a cumulative-maturation-probability surface.
- Convert to a discrete monthly hazard: `psi_lookup[TLbin, delta_t] = (F(delta_t) - F(delta_t - 1)) / (1 - F(delta_t - 1))`, with `F(0) = 0`.
- Cap `delta_t` at 24; beyond that, treat maturation as certain (`psi_lookup` -> ~1).
- Save as a new small, hand-inspectable artifact (e.g. `data/GIEL_MaturationHazard.RData` or `.csv`), in the same spirit as `BWDieOffEvents.csv`/`BWPondAreas.csv` -- an externally-derived, versioned input, **not** re-derived inside the NIMBLE fit itself. This lets BK regenerate the table independently once better growth-anchoring data exist, without touching the robust-design code.

**Step 2 -- data-build changes (`model/BWRobustDesign_data.R`).**
- Retain continuous `TL_entry` in the `Fish` table for GIEL juveniles (currently only the binary `stage0` is kept; `total_length` is used transiently around line ~161-165 and then dropped) and bin it into the same TL bins used in Step 1 (`TL_entry_bin`).
- Fish with missing/imputed TL at entry (existing `stage0_imputed` flag -- e.g. `origin == 3` established fish never physically measured at entry) fall back to the **old** scalar `lpsi[sp]` pathway; do not fabricate a TL for these.
- `make_rd_inputs()`'s history-collapsing step (which pools fish sharing identical `y`/`K`/`stobs`/`post`/`entry_t`/`end_t`/`origin`/`stage0`/`pond` to shrink ~2,900+ GIEL fish to a much smaller set of unique weighted rows) must add `TL_entry_bin` as an additional grouping key so collapsing still applies -- this is why TL is binned rather than kept fully continuous (a continuous per-fish covariate would break history-collapsing and force per-individual likelihood evaluation, a large performance cost).
- Add `data/GIEL_MaturationHazard.RData` to the `BUILD_INFO` mtime staleness guard, matching the convention already used for other externally-maintained inputs.
- This change is **GIEL-only** (species = 2 in the 7-pond joint model). XYTE's `lpsi[1]` in this same joint 2-stage model is untouched -- XYTE's primary model is the separate three-stage build, which already handles size-dependent growth via its own `a1`/`pLg` recruit-size split.

**Step 3 -- NIMBLE changes (`model/BWRobustDesign_defs.R`).**
- `rdPond()` (and the pooled per-history loop) gains fixed constants `psi_lookup` (TLbin x delta_t matrix) plus per-history `tlbin[i]` and `entry_t[i]`, used only when `sp[j] == GIEL` and the history's fish has a known (non-imputed) TL bin.
- Retain one free scalar per species, `lpsi_adj[sp] ~ dnorm(0, sd = 1)`, as a logit-scale offset: `psi_i[t] = ilogit(logit(psi_lookup[tlbin[i], t - entry_t[i]]) + lpsi_adj[sp])` -- so the model can still shift the whole external schedule if robust-design contact/stage data disagree with the growth model's implied timing, without re-estimating its shape. A posterior near 0 would indicate the external schedule needs no correction.
- Multi-month gap transitions (e.g. the 6-month Apr->Oct jump) currently use `pst[t] <- 1-(1-psi_m)^g` (a constant-hazard power); with a time-varying `psi_i[t]` this must become a cumulative product over the actual intervening months, `pst[t] <- 1 - prod_{k=1}^{g}(1 - psi_i[t0 + k])` -- the same kind of month-indexed generalization already used for the `dE`/`nEvt` die-off mechanism in this codebase, not a new pattern.
- Fish with imputed/missing TL_entry, and all XYTE histories in this joint model, keep the existing `psi_m <- ilogit(lpsi[sp])` scalar path unchanged.
- The untagged-pool annual maturation term `psiY[j,y]` (feeding `U`/`RecJ`, the never-tagged pool) is **not** changed by this plan -- there is no individual TL data for untagged fish, so it remains a constant species-level annual rate. This limits how far the improvement propagates: only the *tagged*-fish likelihood benefits from the size-tied schedule.

**Step 4 -- validation plan (post-implementation, once BK is ready to build this).**
- Confirm `N_tag >= known alive` still holds for every GIEL pond-month after the change.
- Check the `lpsi_adj[GIEL]` posterior: near 0 supports the external schedule as-is; a large offset would indicate the growth model and the robust-design contact data disagree on maturation timing.
- Compare model-implied age-at-maturation-by-TL-at-tagging against whatever direct stage-transition recaptures exist in the GIEL contact/stage data (expected to be sparse, similar to the 13 S + 10 L juvenile stage re-observations noted for XYTE at IP1 in the three-stage model; GIEL's own count has not yet been tabulated).
- Sensitivity run using the growth model's 2.5%/97.5% posterior-quantile schedules instead of the mean schedule, to see how much survival/detection estimates move -- this tests how much the still-open choice between the two candidate growth-model curves (age-based vs. increment) would matter for the robust-design results, without first having to fully reconcile the two growth models.

**Open risks flagged, not resolved, by this plan.**
- The two growth models' threshold curves diverge meaningfully (see Motivation); BK chose to proceed with the age-based model for this design but is separately gathering more anchoring data before implementing any of the above, and reconciliation between the two curves remains an open item.
- Bin width for `TL_entry_bin` trades resolution against history-collapsing performance; not yet chosen.
- No GIEL-specific count of direct stage-transition recaptures has been tabulated to gauge how informative Step 4's validation check will actually be.

**STATUS UPDATE (2026-09-14, implemented): this plan was executed as a simplified 3-track sensitivity sweep, not with a single committed track.** Plan file: `.posit/assistant/plans/2026-09-14-1444-giel-growth-model-tracks-feeding-robust-design-maturation-hazard.md`. Scope was narrowed to three growth-model tracks (Track A2, age-based + Cibola, is out of scope): **F1** = Fabens increment, IPCA-only (`GIEL_GrowthSeasonThreshold.R`, existing); **F2** = Fabens increment, IPCA + Cibola (`GIEL_GrowthSeasonThreshold_withCibola.R`, existing); **A1** = age-based VBGF, IPCA-only (`GIEL_VBGF_AgeLength.R`, existing fit, new P(TL>=250mm) section added). Confirmed default `MATURATION_TRACK` = `"F1"` (BK's choice) and TL bins = 20mm, 100-249mm (8 bins), both per the plan's proposed defaults.

- **Step 1 (A1's missing P(TL>=250mm) curve):** added to `model/GIEL_VBGF_AgeLength.R` (§5, before the final save), reusing `samps_final` without re-running the ~100k-iter MCMC. Result: 0.268 (150mm), 0.521 (175mm), 0.788 (200mm), 0.951 (225mm), 0.995 (249mm) -- notably below both Fabens tracks at low TL1 (F1 0.491, F2 0.643 at 150mm), matching the ad hoc estimate already in this file's Motivation section. Caveat: A1's `sigma` (17.0mm) was fit on absolute age-length residuals spanning ages up to ~10yr, not one-season increments, so likely overstates uncertainty here. See `GIEL_VBGF_AgeLength_Summary.md` "P(TL >= 250mm...) -- Track A1" section.
- **Step 2 (three-track comparison):** new `model/GIEL_GrowthTrackComparison.R` / `_Summary.md`, `data/GIEL_GrowthTrackComparison.RData`, figures `model/GIEL_GrowthTrackComparison.png` (posterior VBGF curves) and `_ProbCurves.png` (P(TL>=250mm) overlay). Tracks disagree most at low TL1 (spread 0.375 at 150mm) and converge by 249mm (spread 0.078); all three tracks' `Linf` sits at/below that track's own observed max TL (shared identifiability caveat).
- **Step 3 (hazard tables):** new `model/GIEL_MaturationHazard_Build.R`, run once per track (`TRACK <- "F1"/"F2"/"A1"; source(...)`), writing `data/GIEL_MaturationHazard_<track>.RData` (`psi_lookup`: 8 TL bins x 24 months, clipped to `[1e-6, 1-1e-6]`). Implementation note not in the original design: within a TL bin, `K_i`/`eps_i` are drawn **once per simulated individual and reused across the whole `delta_t` grid** (common random numbers), not redrawn independently at each `(bin, delta_t)` cell -- required so `F(bin, delta_t)` is genuinely monotone non-decreasing in `delta_t` (an individual's projected TL is monotonic in time for fixed K), otherwise Monte Carlo noise produces spurious negative discrete hazards. Verified via an exact reconstruction identity (`F` rebuilt from `psi_lookup` must reproduce the simulated `F`).
- **Step 4 (mechanism, implemented in full):** `model/BWRobustDesign_data.R` retains `TL_entry` (from the existing `StageAtEntry`/`TL_use` computation, renamed on select), adds `hasTL`/`TL_entry_bin` (`GIEL_TLBIN_EDGES`, hardcoded to match Step 3's bins so any track loads safely), a `MATURATION_TRACK` setting (default `"F1"`) that loads `data/GIEL_MaturationHazard_<track>.RData` and adds it to `BUILD_INFO$inputs`. Currently **0 of 936** GIEL juveniles fall back to imputed/missing-TL (`stage0_imputed`), so every GIEL juvenile in the current data gets the size-tied path. `model/BWRobustDesign_defs.R`: `make_rd_inputs()` adds `hasTL`/`TL_entry_bin` to the history-collapsing key (so fish with different TL bins are never merged) and passes `tlbin`/`hasTL`/`psi_lookup`/`N_TLBIN`/`DELTA_T_MAX` into `constants`; `rdPond()` gains `tlbin`, `hasTL`, `psi_lookup`, `lpsi_adj`, `dtmax` arguments and, in **both** the forward and backward passes (both independently recomputed `ps` from `pst[t]` before), computes a per-fish cumulative-hazard `ps` over the gap's intervening months (`1 - prod_k(1 - ilogit(logit(psi_lookup[tlbin,dt]) + lpsi_adj))`) when `hasTL[i]==1`, else falls back to the original scalar `pst[t]`; beyond `dtmax` (24) months, maturation this month is forced to certain. `rd_code` adds `lpsi_adj[s] ~ dnorm(0, sd=1)` per species (prior-only for XYTE, since XYTE never has `hasTL==1`) and passes `lpsi_adj = lpsi_adj[sp[j]]` into `rdPond()`; `make_inits()`/`rd_monitors`/`rd_configure()`'s survival-offset `RW_block` and `BWRobustDesign_NIMBLE.R`'s `core_pars` all updated to include `lpsi_adj`. **Also fixed in passing:** 5 unqualified `count()` calls in `model/BWRobustDesign_data.R` were silently resolving to `mclust::count()` (a totally different function) whenever `mclust` was loaded earlier in the same R session (as it is by `GIEL_VBGF_AgeLength.R`) -- all qualified to `dplyr::count()`.
- **Pre-launch validation (2026-09-14, before committing to the ~1hr fit):** rebuilt data with `MATURATION_TRACK <- "F1"`; built the uncompiled `make_rd_inputs(c(3,6,7))` model (2,938 fish / 2,297 histories / 49 netting rows, 706 of 2,297 histories `hasTL==1`, `tlbin` range 1-8, `psi_lookup` dim 8x24); `repair_inits()` reached a finite logProb (-67953.49); compiled and ran a 200-iteration smoke-test chain (~1.5 min) with `lpsi_adj[1]`/`dJ[1]`/`dE[1:3]` all present in the samples and no non-finite draws.
- **F1 validation fit launched 2026-09-14 (job `3D197187`, "GIEL hazard fit Track F1"), via `model/run_giel_hazard_fit_F1.R`, writes `data/BWRobustDesign_MCMC_giel_F1.RData` / `model/BWRobustDesign_traceplots_giel_F1.pdf`.**
- **F1 validation result (completed 2026-09-14 16:26, 69 min): PASSED, mechanism working as intended.** `N_tag >= known alive` verified for all 210 pond-months at the 2.5% posterior quantile (0 violations; checked by summing `N_tag[pond,t,1:2]` against a direct known-alive count from `entry_t`/`end_t`/`last_alive_t`/`count_from`). Convergence: 64 of 674 monitored nodes R-hat > 1.1, all in the known nuisance families (`N_tag`, `U`, `RecJ`, `r`, `Atot`, `Tal` — recruitment/pool quantities that mix poorly in every prior GIEL/XYTE fit); every core hyperparameter (survival, detection, `dJ`, `dE`, `lpsi`, `lpsi_adj`, `g_eff`) has R-hat <= 1.04. **`lpsi_adj[1] = 0.227 (0.044, 0.402)`** — modest, credibly nonzero positive logit offset on the Track-F1 hazard lookup table, meaning the robust-design contact/stage data want maturation slightly faster than the raw growth-model schedule predicts, not a pathological or extreme correction. Comparison to the pre-hazard baseline (`data/BWRobustDesign_MCMC_giel.RData`): overall survival hyperparameters essentially unchanged (`mu_phi[1]` 2.714 vs 2.711, `sigma_phi[1]` 0.848 vs 0.849, `dE[1:3]` all within 0.05 logit of baseline), confirming the size-tied maturation submodel is largely separable from survival/die-off estimation (the same pattern seen for XYTE's `a1` size-split submodel). Detection (`lp`) shifted modestly for the juvenile stage in ponds IP2/IP5 (`lp[1,1]` -2.03 vs -2.20; `lp[2,1]` 0.158 vs 0.009) since size-tied maturation changes how much calendar time tagged fish spend recorded as "juvenile" before transitioning, which reallocates their detections between stages -- expected, not a concern. `lpsi[1]` (the untagged-pool annual maturation rate, still scalar since there is no individual TL data for the untagged pool) moved from -1.697 (SD 0.081, baseline) to -2.233 (SD 0.362, F1) because it no longer does double duty informing tagged-fish maturation, and is now informed only via the netting join/untagged-pool likelihood -- expected under the plan's Step 3 design (untagged-pool maturation intentionally left as a constant rate). Job note: `dJ[1]` (juvenile survival offset relative to adult) is positive in both fits (F1 0.413, baseline 0.453) -- i.e., juvenile survival is estimated higher than adult on the logit scale in this model; this is a pre-existing feature of the GIEL 2-stage model, not introduced by the hazard mechanism.
- **IP2 (Pond 2) adult-survival investigation (2026-09-14, while F2/A1 fits ran).** BK asked whether the shared `dJ[GIEL] > 0` (juvenile survival modeled higher than adult) is a general GIEL pattern, driven by Pond 2 alone, by a single bad year, or by die-off event-timing miscoding. **Findings, using the F1 fit's per-pond `lphiA[pond,season]` posteriors plus the raw `Fish`/`TotalCountIPGIEL` data (model-independent checks):**
  1. **It is essentially an IP2-specific pattern.** Per-season modeled adult annual survival: IP6 stays in a stable, plausible 0.40-0.77 range every season (no crash); IP5 has one unflagged low season (FY2018, 0.156) and a low post-crash-recovery season (FY2021, 0.103) but is otherwise 0.35-0.82; **IP2 is chronically low in 6 of its 9 non-event seasons** (FY2018 0.23, FY2019 0.14, FY2020 0.035, FY2021 0.082, FY2024 0.23, FY2025 0.08), with only the stocking year (FY2017, 0.43), the already-flagged 2022 event year itself (0.49, i.e. NOT the lowest year), and the two post-restock years (FY2023 0.65, FY2026 0.68) higher. IP2 supplies 728 of 936 (78%) of all GIEL juveniles ever tagged, so a pond this dominant and this chronically low pulls the shared, pond-pooled `dJ[GIEL]` upward.
  2. **Not a single year.** The low-survival pattern spans 4 consecutive non-event seasons (FY2018-FY2021) plus 2 more later (FY2024-FY2025) -- a persistent, recurring pattern at this pond, not an isolated bad year.
  3. **Not a die-off event-timing/coding issue for the documented 2022 crash.** `Evt[IP2, ]`/`dE` correctly flags primary occasions t=39-42 (Jan-Apr 2022) plus the following Apr-Oct 2022 gap (so the discount applies to Jan-Oct 2022, 9 of 12 months), matching the confirmed real-data crash (Jan 2022 known-alive 66 -> Jul 2022 39 -> Aug 2022 0, from `TotalCountIPGIEL`) almost exactly; realized FY2022 model survival with the event adjustment (0.057, 95% CI 0.027-0.10) is very close to the pattern in the raw counts. **But this event code does nothing to explain the pre-2022 FY2018-FY2021 decline**, which is a separate, currently *unflagged* chronic problem: raw `TotalCountIPGIEL` for IP2 shows known-alive fish falling continuously from 237 (Mar 2017 stocking) to just 14 (Oct 2021) -- a ~94% cumulative loss over 4.5 years, essentially complete before the coded 2022 event window even starts. Scan effort at IP2 was strong and continuous throughout this decline (1,200-2,300 h/month every FY2018-FY2022 month, confirmed from `eff_hrs`), so this is not an unscanned-gap artifact; whether it reflects real sustained mortality, an unrecorded second die-off, or a data/husbandry issue at this pond is not resolved by the joint model and would need pond-specific water-quality/event records (none currently exist, per the GIEL growth-model die-off cross-reference notes above) to investigate further.
  4. **A structural/detection confound also likely contributes, and is a strong independent explanation for `dJ`'s sign specifically at IP2.** Baseline (median-effort) weekly detection by pond and stage from the F1 fit's `lp[pond,stage]`: IP2 juvenile 0.116 vs IP2 adult 0.302 (juveniles detected **less than half as often** as adults); IP5 juvenile 0.539 vs adult 0.366; IP6 juvenile 0.551 vs adult 0.337 (juveniles detected **more** often than adults at both other ponds). IP2 is the only pond where juvenile detection is much worse than adult detection -- the same "low-detection-as-a-survival-sink" failure mode already documented for the XYTE three-stage model's L-stage (`AGENTS.md`, XYTE section, "L-stage detection is implausibly low"): in a state-space model, a poorly-detected stage's true mortality can partly masquerade as unresolved "still alive, just not seen," inflating its apparent survival. Since IP2 supplies most of the juvenile sample, this pond-specific detection asymmetry is a plausible driver of the pooled `dJ[GIEL] > 0` finding independent of (and probably compounding with) the chronic-decline pattern in point 1. A raw, model-free cohort check (fraction of tagged fish re-sighted >=1 year after entry, no survival/detection separation) shows the *direction* of the juvenile-vs-adult gap actually differs by pond (IP2: adults re-sighted more, 7.7% vs 1.2%; IP5: adults more, 21.2% vs 7.5%; IP6: juveniles more, 42.6% vs 36.8%) -- consistent with detection differences dominating this raw metric rather than a single clean cross-pond survival story.
  **Suggested follow-ups (not yet implemented):** fit a pond-specific (or at least IP2-vs-rest) `dJ` to test whether the juvenile-over-adult gap survives once IP2 is isolated; consider a shared juvenile/adult detection intercept at IP2 (mirroring the XYTE fix) to test whether that alone resolves `dJ`'s sign; and/or add a second, unflagged chronic-mortality term (e.g. an `eps`-style deviation) for IP2 FY2018-FY2021 analogous to the XYTE `eps_J` mechanism, since the current `dE`/`Evt` machinery only covers the acute, already-documented 2022 event.
- **F2 and A1 fits launched 2026-09-14 following the F1 pass.** Data rebuilt with `MATURATION_TRACK <- "F2"` (writes `data/BWRobustDesign_data.RData`; reconciliation printout confirms `GIEL maturation hazard table loaded from data/GIEL_MaturationHazard_F2.RData`), then `model/run_giel_hazard_fit_F2.R` launched as background job `206BDEDF` ("GIEL hazard fit Track F2", confirmed running), writing `data/BWRobustDesign_MCMC_giel_F2.RData` / `model/BWRobustDesign_traceplots_giel_F2.pdf`. Data then rebuilt again with `MATURATION_TRACK <- "A1"` (safe to do immediately after the F2 job launch since NIMBLE jobs load `data/BWRobustDesign_data.RData` into their own process memory within seconds of starting, confirmed running before the second rebuild), then `model/run_giel_hazard_fit_A1.R` launched as background job `58C2CA9E` ("GIEL hazard fit Track A1", confirmed running), writing `data/BWRobustDesign_MCMC_giel_A1.RData` / `model/BWRobustDesign_traceplots_giel_A1.pdf`. Both jobs use the same settings as F1 (`RUN_PONDS <- c(3L, 6L, 7L)`, 3 chains, 30k/10k burn/thin 10); expect ~60-70 min each (F1 took 69 min), running concurrently. **`data/BWRobustDesign_data.RData` on disk is currently built with `MATURATION_TRACK = "A1"`** (the last rebuild) -- re-run with the desired track before any further ad hoc use of that file. F1, F2, and A1 all completed 2026-09-14 (69.1 / 100.0 / 100.3 min respectively).
- **Validation checklist run on all three tracks 2026-09-15 (`model/GIEL_MaturationHazard_Validation.R`, results in `model/GIEL_MaturationHazard_Validation_Summary.md`, `data/GIEL_MaturationHazard_Validation.RData`).** Note: a 2026-09-15 conversation initially (incorrectly) believed F1 had not yet been run; the file `data/BWRobustDesign_MCMC_giel_F1.RData` (69.1 min, matching the completion described above) was confirmed genuine and included in this validation rather than re-run. **All three tracks pass the known-alive constraint with 0 violations (183 pond-months checked each) and converge acceptably** (58 core hyperparameters, max Rhat 1.046/1.027/1.053 for F1/F2/A1, 0 core nodes > 1.1; the usual `N_tag`/`Tal`/`U`/`RecJ`/`r`/`Atot` nuisance-family Rhats up to 1.29 are the same pre-existing artifact seen in every prior GIEL/XYTE fit). `lpsi_adj[GIEL]`: **F1 +0.227 (0.044, 0.402)**, **F2 −0.645 (−0.852, −0.430)**, **A1 −0.643 (−1.081, −0.118)** — all credibly nonzero but opposite in sign; F1's schedule needs faster maturation, F2/A1's need slower, mirroring the F1-vs-{F2,A1} divergence already documented in the P(TL>=250mm) curves. Survival/die-off hyperparameters (`mu_phi`, `sigma_phi`, `dE[1:3]`) are essentially identical across tracks and the pre-hazard baseline (< 0.1 logit difference); FY2022 IP2 and FY2020 IP5 event-adjusted realized annual survival agree to within 0.01 across all three tracks (IP2 ~0.09-0.10, IP5 ~0.04-0.05) — the maturation-hazard track choice does not affect survival/die-off conclusions, only maturation-timing quantities (`lpsi`, juvenile `lp`, `dJ`). No track is disqualified; choosing among them (or reporting all three as a sensitivity range) is a modeling decision for BK. **Step 6 complete (2026-09-15): `model/GIEL_MaturationHazard_Sensitivity.qmd`** (self-contained HTML, `model/GIEL_MaturationHazard_Sensitivity.html`) loads the three sensitivity fits plus the baseline directly from their `.RData` files (not from `data/GIEL_MaturationHazard_Validation.RData`, which was never saved to disk despite the validation script defining a `save()` call — likely run interactively without reaching that line) and each track's `psi_lookup`/`F_mat` hazard table. Sections: (1) hazard-schedule comparison (monthly hazard by TL bin and the discretized P(TL>=250mm after 6 months) curve actually used inside the model, as opposed to each growth model's own smoother continuous-TL curve); (2) known-alive constraint (reproduces the validation script's 0-violations result); (3) convergence by parameter family; (4) `lpsi_adj[GIEL]` table; (5) core hyperparameter comparison (F1/F2/A1/baseline); (6) FY2022 (IP2)/FY2020 (IP5) event-adjusted realized survival, recomputed from posterior draws with the correct `nEvt` (7 and 9 months respectively, from `RD_constants$nEvt` in the 7-pond `data/BWRobustDesign_data.RData`, remapped to GIEL-only `pond_new` indices 1=IP2/2=IP5) rather than the validation script's own reference table (which used `nEvt=12` for IP5, a placeholder for a different, unused baseline-only printout); (7) conclusion. All findings reproduce `GIEL_MaturationHazard_Validation_Summary.md`'s numbers. Rendered cleanly on first attempt (`quarto render model/GIEL_MaturationHazard_Sensitivity.qmd`).

**Shared IP2 juvenile/adult detection intercept added and refit launched for all three tracks (2026-09-15, BK request).** Follow-up to the "IP2 (Pond 2) adult-survival investigation" bullet above, which found IP2 is the only GIEL pond where juvenile detection is much worse than adult detection (`lp[IP2,1] = -1.35` vs `lp[IP2,2] = +0.34` in the no-sharedLp four-pond XYTE... no, this is the GIEL model — see that bullet for the actual IP2 juvenile/adult `lp` gap) — the same low-detection-as-a-survival-sink failure mode already fixed for the XYTE three-stage model's L stage (`lp[j,2] <- lp[j,1]`), hypothesized as a driver of the pooled `dJ[GIEL] > 0` finding (juvenile survival modeled above adult). **Implementation (`model/BWRobustDesign_defs.R`):** the stochastic detection node is renamed `lp_raw[j, st] ~ dnorm(1, sd=1.5)` (unchanged prior/structure); a new deterministic `lp[j, 1] <- lp_raw[j, 1]` and `lp[j, 2] <- lp_raw[j,1]*sharedLp[j] + lp_raw[j,2]*(1-sharedLp[j])` ties juvenile and adult detection together only where `sharedLp[j] == 1`. `sharedLp` is a new per-pond-subset constant computed in `make_rd_inputs()` as `as.integer(PondTable$Backwater[ponds] == "IP2")` — 1 only at IP2, 0 everywhere else (a no-op for every other pond/fit, including XYTE-only subsets, which never include IP2; no data-build change needed since `PondTable` is already available in `make_rd_inputs()`). `rd_configure()`'s detection `RW_block` now blocks `lp_raw[j,1:2]` instead of `lp[j,1:2]`; `make_inits()`'s `lp` init renamed to `lp_raw`; `rd_monitors`/`core_pars` gained `"lp_raw"` (in addition to the existing `"lp"`, which still monitors the derived quantity). **Smoke-tested before launching full refits:** `make_rd_inputs(c(3,6,7))` gives `sharedLp = (1, 0, 0)` for (IP2, IP5, IP6); a compiled 200-iteration test chain confirmed `lp[1,1] == lp[1,2] == lp_raw[1,1]` exactly every iteration (IP2 shared) while IP5/IP6 keep independent `lp_raw[·,1]`/`lp_raw[·,2]`, and produced 0 non-finite draws. **Three new run scripts** (`model/run_giel_hazard_fit_{F1,F2,A1}_sharedLp.R`, same settings as the originals — 3 chains, 30k/10k burn/thin 10, seeds 4127/8351/2960 — but writing to `_sharedLp`-suffixed output files so the pre-sharedLp F1/F2/A1 fits remain archived unchanged) launched as background jobs 2026-09-15: **F1 job `ED897BDA`** -> `data/BWRobustDesign_MCMC_giel_F1_sharedLp.RData`; **F2 job `722B6929`** -> `data/BWRobustDesign_MCMC_giel_F2_sharedLp.RData`; **A1 job `03FED21D`** -> `data/BWRobustDesign_MCMC_giel_A1_sharedLp.RData` (data rebuilt with the matching `MATURATION_TRACK` immediately before each launch, per the established safe-sequencing pattern — NIMBLE jobs load `data/BWRobustDesign_data.RData` into their own process within seconds of starting). Expect ~70-100 min each (matching the original F1/F2/A1 run times), running concurrently; **`data/BWRobustDesign_data.RData` on disk is left built with `MATURATION_TRACK = "A1"`** after this launch sequence. **On completion check, per BK's request:** (1) whether `dJ[1]` (juvenile-vs-adult survival offset) moves toward/below 0 now that IP2's low juvenile detection no longer masquerades as inflated juvenile survival; (2) IP2's per-season `lphiA` (esp. FY2018-FY2021, the "chronically low" pattern) for any change; (3) whether survival/die-off estimates (`mu_phi`, `sigma_phi`, `dE[1:3]`, event-adjusted FY2022/FY2020 realized survival) are essentially unchanged across F1_sharedLp/F2_sharedLp/A1_sharedLp, matching the no-sharedLp tracks' finding that maturation-track choice doesn't affect survival — if so, per BK's standing instruction, **adopt Track F1 (Fabens increment, IPCA-only) as the preferred model, being the least complex of the three growth-model sources** (F2 adds an external historical pond and a site-comparison step; A1 adds an age-assignment mixture-model step); (4) known-alive constraint (`N_tag >= known alive`, 0 violations expected, as in every prior fit); (5) convergence, watching in particular whether `lp_raw[IP2,2]` (now prior-only at IP2) mixes fine as an unused node.

**SharedLp refit results, all three tracks (2026-09-15, F1/F2/A1 jobs `ED897BDA`/`722B6929`/`03FED21D`, completed ~11:07, ~124/~127/~127 min).** All checked with the corrected joint-posterior-quantile method (per-iteration sum across stage, then quantile — the marginal-per-stage shortcut used in an earlier attempt this session falsely flagged 50-65 of 210 pond-months as violations; re-verified the same bug would also falsely flag the already-validated baseline no-sharedLp F1 fit, confirming it was a check-script bug, not a real regression).
1. **Known-alive constraint: 0 violations in all three sharedLp fits** (210 pond-months each, matching baseline).
2. **Convergence: excellent, matching/better than the no-sharedLp tracks** — max core-hyperparameter Rhat 1.02 (F1), 1.01 (F2), 1.05 (A1); the usual `N_tag`/`U`/`RecJ`/`r`/`Atot`/`Tal` nuisance-family Rhats are the same pre-existing artifact. `lp_raw[IP2,2]` (now unused/prior-only, since `lp[IP2,2]` is tied to `lp_raw[IP2,1]`) mixes cleanly (Rhat 1.00, n.eff 600-970, posterior mean ~1.0-1.1 matching its `dnorm(1, sd=1.5)` prior) in all three fits — confirms the shared-detection mechanism didn't leave a poorly-identified orphan node. `lp[1,1] == lp[1,2]` exactly, confirming the tie works as coded.
3. **`dJ[GIEL]` moved toward zero but did NOT cross below it:** baseline (no sharedLp, Track F1) 0.413 (0.233-0.592) -> sharedLp 0.197 (F1), 0.218 (F2), 0.231 (A1) — roughly halved, consistent with the hypothesis that IP2's low juvenile detection was inflating apparent juvenile-vs-adult survival, but juvenile survival is still estimated modestly *higher* than adult after the fix (all three 95% CIs, e.g. F1 0.025-0.364, sit mostly or entirely above 0). **Partial confirmation only** — shared IP2 detection explains part but not all of the `dJ > 0` pattern; the chronic-decline mechanism flagged in the original IP2 investigation (point 1 there) likely still contributes.
4. **IP2 per-season `lphiA` (annualized) essentially unchanged** from the no-sharedLp baseline in every season (differences <=0.04 in realized annual survival): FY2018 0.233->0.234-0.236, FY2019 0.141->0.158-0.172, FY2020 0.036->0.039-0.044, FY2021 0.086->0.085-0.093, FY2024 0.231->0.220-0.226, FY2025 0.082->0.078-0.082 — the chronic FY2018-FY2021/FY2024-FY2025 low-survival pattern at IP2 persists after fixing the detection asymmetry, so detection was not the (or not the only) explanation for the chronic decline itself, only for part of the `dJ` bias.
5. **Survival/die-off hyperparameters essentially identical across F1_sharedLp/F2_sharedLp/A1_sharedLp** (mu_phi 2.73-2.74, sigma_phi 0.843-0.851, dE[IP2/IP5/IP6] all within 0.09 logit of each other and of baseline; event-adjusted realized survival FY2022 IP2 0.096-0.103 [vs baseline 0.090, vs the ~0.07 empirical floor noted earlier] and FY2020 IP5 0.043-0.047 [vs baseline 0.046]) — **confirms the maturation-track choice does not affect survival/die-off conclusions**, matching the no-sharedLp tracks' finding. `lpsi_adj[GIEL]` differs more between fits *and* shifted from the no-sharedLp values (F1 sharedLp -0.533 vs no-sharedLp +0.227; F2 sharedLp -1.377 vs no-sharedLp -0.645; A1 sharedLp -1.695 vs no-sharedLp -0.643) — sharing IP2 detection reallocates how much apparent maturation timing needs correcting, as expected, but this only affects `lpsi`/`lpsi_adj`/juvenile `lp`, not survival.
6. **Decision: per BK's standing instruction, adopt Track F1 (Fabens increment, IPCA-only) as the preferred maturation-hazard source**, with the shared-IP2-detection specification (`data/BWRobustDesign_MCMC_giel_F1_sharedLp.RData` / `model/BWRobustDesign_traceplots_giel_F1_sharedLp.pdf`) as the current reference GIEL joint robust-design fit — it is the least complex of the three growth-model sources (F2 adds an external historical Cibola pond and an underpowered site-comparison step; A1 adds an age-assignment mixture-model step), and all three are statistically indistinguishable on survival/die-off/convergence grounds. **Open item carried forward:** the IP2 chronic FY2018-FY2021/FY2024-FY2025 low-survival pattern remains unresolved and only partially attributable to the detection asymmetry now fixed; the two follow-ups suggested in the original investigation (pond-specific `dJ`, or an `eps`-style chronic-mortality deviation for IP2) are still open and not implemented.

**Joint robust-design model results added to `PopulationMonitoring.qmd` (2026-09-15).** A new "Joint Robust-Design Survival Model" subsection was added at the end of the "Imperial Ponds — Bonytail" section (after the mark-recapture narrative, before the `export-estimates` chunk), summarizing the adopted Track F1 sharedLp GIEL fit for report readers. This is the **first time** the Bayesian joint robust-design/NIMBLE model results have been surfaced in the rendered population monitoring report (previously documented only in `model/GIEL_ModelResults_Summary.md` and this file) — everything else in `PopulationMonitoring.qmd` is still live-computed from `ReportingData.RData` via glmmTMB/Chapman methods; this new subsection instead loads two pre-fit `.RData` files directly (`data/BWRobustDesign_MCMC_giel_F1_sharedLp.RData` and, for the `dJ` before/after comparison, `data/BWRobustDesign_MCMC_giel_F1.RData`), matching the pattern already used by `model/YCB3S_validation.qmd` and similar standalone reports. Content: a table of `dE` die-off offsets (IP2, IP5, IP6) with 95% CIs; prose (built as plain-string R objects `giel_narrative_dE`/`giel_narrative_dJ` in the hidden chunk, then referenced inline as `` `r giel_narrative_dE` ``/`` `r giel_narrative_dJ` `` — avoid inline `` `r paste0(...)` `` expressions with nested backtick-quoted column names for tables like `giel_dE_tab`, which produced unparseable escaping on the first attempt) covering the die-off event-adjusted survival for IP2 FY2022 (10%, 95% CI 5-17%) and IP5 FY2020 (5%, 95% CI 2-9%); a description of the maturation-hazard track selection (F1 chosen as simplest of three statistically-indistinguishable options); and the **`dJ[GIEL]` shared-detection finding**, i.e. the juvenile-vs-adult survival logit offset dropping from 0.41 (0.23-0.59) to 0.20 (0.03-0.36) — odds ratio ~1.51x to ~1.22x — after tying IP2's juvenile and adult detection together, explicitly noting this is a partial, not full, resolution (still modestly positive). Both `run_giel_rd`-gated chunks fall back gracefully (skip silently) if the two `.RData` files are not present. Verified: chunk logic runs standalone with correct numbers, full `quarto render PopulationMonitoring.qmd --to html` completes cleanly through the new chunks, 0 CRLF introduced, and the only blank lines inside the new chunks are intentional spacing (13 total, consistent with normal style elsewhere in the file — not the "blank line after every line" pathology).

---

## Open TODOs

- **UNM genetics-database collection table (requested 2026-09-17, not yet built).** The UNM/Dowling-lab bonytail parentage summary (`data/UNM_GIEL_ParentageSampling.csv`, from a 2026-09-17 email from Megan Osborne) only has coarse pond/year/stage/N counts, which produced an inconsistent, hard-to-interpret comparison against our NFWG-derived first-tagged-recruit counts (`output/UNM_vs_NFWG_GIEL_RecruitComparison.csv`) -- most likely because UNM samples larval/juvenile fish in the same season they are produced (seine/net sampling, no PIT tag required), while our NFWG database only gains a record once a fish is physically captured *and* large enough to tag, sometimes a year or more later. BK wants a **simplified table pulled from the genetics database itself** (which should hold every sample collection event, including larvae/juveniles with no PIT tag, with actual collection dates rather than just a summary year) so collection timing can be reconciled properly against NFWG/PIT records instead of guessing at a lag. Not yet started: no script, no file, no schema. When picked up, this should probably live alongside the other genetics reference data (see `Yuma_*_repro_counts_PIT.csv` / `IP_*_repro_counts_PIT.csv` in `Key Data Files`) and should get its own row in the `Key Data Files` table plus a `Key R Objects` entry once built.

---

## Chapman Validation Process

Both `ChapmanValidation.qmd` (YCB) and `ChapmanValidationIP.qmd` (IPCA Pond 1) follow the same validation recipe, used to test whether the Chapman estimator + `recapr` bootstrap CI (see `Analysis Rules and Caveats` below) can recover a **known, fixed true N** from a subset of confirmed long-term survivors. To build a similar validation for a new location:

1. **Define the true population.** Take a single stocking cohort at the location (e.g. `event == "stocking"`, one `release_year`). Classify fish as confirmed long-term survivors if their `MaxScanDate` falls on or after a cutoff date chosen well after the stocking year (YCB used +6 years; IPCA Pond 1 used +6 years). Because these are closed, continuously-scanned habitats, a contact on/after the cutoff proves the fish was alive throughout the entire intervening window. `TRUE_N` = count of these fish; it is fixed for every validation window.
2. **Restrict contacts.** Pull `StudyBWContacts` filtered to the location's `LID` and to only the confirmed survivor `PITIndex` set (`pits_true`). All M/C/R below are drawn exclusively from this restricted contact history — never from the full population.
3. **Annual analysis.** For each census year in the validation window (the years strictly between the stocking year and the true-N cutoff), compute M (Jan–Feb marks), C (Oct–Apr captures), and R (intersection). Apply `ciChapman(n1 = M, n2 = C, m2 = R, method = "boot", bootreps = 10000)`, filtering to R > 3, and check whether `TRUE_N` falls inside the bootstrap CI.
4. **Sub-annual analysis.** Roll 30-day mark / 30-day capture windows (back-to-back, stepping one month at a time) across the same span; apply the same Chapman + bootstrap CI logic per window.
5. **Watch for the R = C special case.** If detection is high enough that every captured fish was also marked in a given window, R = C and the Chapman formula algebraically simplifies to N̂ = M with a degenerate (zero-width) bootstrap CI — this is expected, not a bug (see `Analysis Rules and Caveats`). Note any such years explicitly rather than treating them as a meaningful CI-coverage test; the informative years/windows are ones where R < C.
6. **Report caveats.** Always state that `TRUE_N` is the confirmed-survivor subset of a single stocking cohort, not the location's total population (which may include recruits or supplemental stockings), and flag any scanner gaps that explain excluded or missed-coverage windows.

---

## Reference Document Lookup Guide

> **Standing instruction:** Whenever a document in the `documents/` folder is converted to a markdown summary at the user's request, automatically: (1) add a row to the **Key Reference Documents** table below with the PDF/DOCX filename, summary filename, and a brief description, and (2) add a row to this **Reference Document Lookup Guide** table with one or more trigger topics and the summary filename. Both updates must be made together before ending the response.

When a request touches one of the topics below, **read the corresponding markdown file before responding**. These summaries contain the key facts, formulas, and citations needed to give accurate answers without having to re-parse the original PDFs or DOCX files.

| Topic / Request Type | File(s) to read |
|---|---|
| Chapman estimator formula, mark-recapture methods, population estimates, wildlife abundance estimation | `documents/PierceEtAl_ChapterAbundance_Summary.md` |
| Bootstrap confidence intervals for Chapman, `ciChapman()` behavior, CI coverage at small R | `documents/Sangnawakij_et_al_2026_Summary.md` |
| Both of the above together (population estimate methods section of report) | Both `PierceEtAl_ChapterAbundance_Summary.md` and `Sangnawakij_et_al_2026_Summary.md` |
| OCH concept, IPCA site history, BONY pond sizes, Marsh et al. 2024 citation | `documents/Marsh_et_al_2024_Summary.md` |
| Pond/backwater surface areas (all 7 sites), YCB or IPCA site dimensions/origins/substrate/water source | `documents/Maturation of an off-channel.pdf` (Appendix 1.2–1.3); also see `Study Locations` and `Site Background` sections above and `data/BWPondAreas.csv` |
| YCB population narrative, sex-biased survival history, 2013 stocking cohort | `documents/2025Report_YCB_PopulationMonitoring_Summary.md` |
| IPCA population narrative, recruit counts by pond, IP RASU or BONY narrative text | `documents/2025Report_IP_PopulationMonitoring.md` |
| IPCA site design rationale, 2005 design workshop, original pond failures, six-pond layout | `documents/IP_DesignWorkshop2005_Summary.md` |
| 2014–2015 renovation, gap before 2017 stocking, well-water conversion | `documents/IPRenovationPlan2014_Summary.md` |
| FY2018 annual report, 2017 stocking results, first-year survival at IPCA | `documents/IPCA_AnnualReport2018_Summary.md` |
| YCB stocking history, harvest events, fish returned or removed by year | `documents/YumaBW_SummaryOfEvents_Summary.md` |
| GT-seq panel, CKMR methods, genetic database (lcrgenetics.net), MSB tissue archive | `documents/2025FinalReport_Summary.md` |
| Proposed BONY IPM structure (Sound Science LLC), comparison against our GIEL joint model, environmental-covariate relevance critique | `documents/BONY_Proposed_IPM_Summary.md` (factual summary) and `model/BONY_ProposedIPM_Critique.md` (structure outline + critique, to revisit once the GIEL fit returns) |
| Lake Havasu / open-water bonytail telemetry study, mainstem post-stocking survival, avian/fish predation evidence, failed Barker/CJS mark-recapture attempt | `documents/C64_2016_LakeHavasu_Summary.md` |
| 2019/2020 genetics report, microsatellite methods, AJ/DAN ephemeral ponds, early IPCA data | `documents/BackwaterFinalReport2020_Summary.md` |
| Pierce et al. full citation or page range | `documents/PierceEtAl_ChapterAbundance_Summary.md` |
| Sangnawakij et al. full citation or DOI | `documents/Sangnawakij_et_al_2026_Summary.md` |
| Bayesian VBGM methodology, ageing-error/observation-vs-process-error separation, sex-specific growth, `Linf`/`k` identifiability issues, growth model improvement ideas for the GIEL growth-season threshold model | `documents/Chamberlin2025_GrayTriggerfishVBGM_Summary.md` |

---

## Key Reference Documents

| File | Description |
|---|---|
| `documents/2025FinalReport.docx` | Full 2025 Dowling lab genetics and demographic final report for BONY and RASU (mainstem and OCH). GT-seq panel, CKMR methods, genetic database, tissue archive. See `2025FinalReport_Summary.md`. |
| `documents/2025FinalReport_Summary.md` | Summary of `2025FinalReport.docx` — GT-seq panel results for BONY (238 loci) and RASU, CKMR abundance estimates, database (http://lcrgenetics.net), and MSB tissue archive through 2025. |
| `documents/BONY Proposed IPM - Model Description 9-2-26.docx` | Sound Science LLC proposed integrated population model for bonytail (LCR MSCP Phase 3, Sub-Task 2), draft 10 July 2026. Off-channel state-space IPM (CHLP, IPCA Ponds 2/5/6) + open-water CJS; reproduction (F) from genetic parentage; environmental/release covariates. See `BONY_Proposed_IPM_Summary.md` and the critique in `model/BONY_ProposedIPM_Critique.md`. |
| `documents/BONY_Proposed_IPM_Summary.md` | Factual summary of the proposed BONY IPM document (structure, data streams, covariates, assumptions). See `model/BONY_ProposedIPM_Critique.md` for the structural critique against our own GIEL findings. |
| `documents/C64_2016_Lake Havasu Final Project Report.docx` | Humphrey, Kesner, and Marsh (2016). Lake Havasu (LCR MSCP Reach 3) open-water bonytail acoustic/radio telemetry (6 iterations) and PIT-scanning (5 iterations) study, 2013–2016. Documents near-total, rapid (within weeks) post-stocking mortality dominated by avian and fish predation, and an explicit failed attempt to fit CJS/Barker mark-recapture models to the telemetry+PIT data. See `C64_2016_LakeHavasu_Summary.md`; cited in the proposed-BONY-IPM critique (`model/BONY_ProposedIPM_Critique.md`) as direct precedent against the feasibility of a covariate-rich open-water φ³ submodel. |
| `documents/C64_2016_LakeHavasu_Summary.md` | Summary of the Lake Havasu final report above. |
| `documents/2025Report_YCB_PopulationMonitoring.docx` | Population monitoring narrative section for Yuma Cove Backwater RASU through 2025. Covers post-stocking survival (sex-biased), known survivors, mark-recapture estimates, and reproductive contribution. See `2025Report_YCB_PopulationMonitoring_Summary.md`. |
| `documents/2025Report_YCB_PopulationMonitoring_Summary.md` | Summary of `2025Report_YCB_PopulationMonitoring.docx`. Key facts: 24M vs 80F survived 2013 stocking; 2018 recruit cohort die-off preceded management shift to harvesting; majority of current YCB population is naturally recruited. |
| `documents/2025Report_IP_PopulationMonitoring.md` | Draft population monitoring narrative for Imperial Ponds RASU (Ponds 1, 3, 4) and BONY (Ponds 2, 5, 6). Includes recruit counts by pond and year. |
| `documents/Maturation of an off-channel.pdf` | Full text of Marsh et al. (2024) (see `Marsh_et_al_2024_Summary.md` for citation and synthesis). Appendix 1 ("Off-channel habitats") has the primary source dimensions for YCB (Appendix 1.3) and all six IPCA ponds (Appendix 1.2), used to populate `data/BWPondAreas.csv` on 2026-09-09. |
| `documents/Marsh_et_al_2024_Summary.md` | Summary of Marsh et al. (2024) — the primary reference for Imperial Ponds site history, design rationale, OCH concept, and BONY pond sizes. See file for full citation. |
| `documents/backwater final report draft 16 Jan 2020.docx` | Dowling lab 2019 final report (draft Jan 2020) on RASU genetics and demographics in OCH including ephemeral ponds (AJ, DAN), YCB, and early IPCA data. Used 14-locus microsatellites. Predecessor to `2025FinalReport.docx`. See `BackwaterFinalReport2020_Summary.md`. |
| `documents/BackwaterFinalReport2020_Summary.md` | Summary of `backwater final report draft 16 Jan 2020.docx`. Covers AJ and DAN ephemeral ponds (not in current project), YCB, and early IPCA. Established sex-biased survival pattern; identified need for SNP parentage. |
| `documents/IP Design Workshop 2005.pdf` | Bureau of Reclamation design workshop report for reconstructing the DU2 Ponds at INWR into what became the IPCA. Documents why original ponds failed and six-pond layout design goals. See `IP_DesignWorkshop2005_Summary.md`. |
| `documents/IP_DesignWorkshop2005_Summary.md` | Summary of `IP Design Workshop 2005.pdf`. Foundational document for IPCA site design and rationale for river-disconnected, well-water-supplied ponds. |
| `documents/Pierce et al. - Chpt 11 Estimating Animal Abundance.pdf` | Chapter 11 from *The Wildlife Techniques Manual* (7th ed., 2012), Pierce, Lopez, and Silvy. Covers the full range of abundance estimation methods including the Chapman (1951) mark-recapture estimator. Primary reference for Chapman estimator context and formula in wildlife practice. See `PierceEtAl_ChapterAbundance_Summary.md`. |
| `documents/PierceEtAl_ChapterAbundance_Summary.md` | Summary of Pierce et al. Chapter 11. Includes the Chapman estimator formula N̂ = (M+1)(C+1)/(R+1) − 1, its variance, assumptions, and notation mapping to the backwater project. |
| `documents/Sanwnawakij et al 2026 - Two-way capture-recapture methods.pdf` | Sangnawakij et al. (2026), *Statistical Methods & Applications* 35:1–22. Primary reference supporting bootstrap confidence intervals for the Chapman estimator; compares imputed bootstrap (preferred), double bootstrap, and simple bootstrap methods. See `Sangnawakij_et_al_2026_Summary.md`. |
| `documents/Sangnawakij_et_al_2026_Summary.md` | Summary of Sangnawakij et al. (2026). Covers Chapman estimator formula and bias, three bootstrap CI methods, simulation findings (imputed bootstrap best), and relevance to `recapr::ciChapman()`. Includes note on the special case R = C where Chapman formula simplifies to M. |
| `documents/LCR MSCP 2021 - Imperial Ponds Conservation Area Annual Report 2018.pdf` | FY2018 IPCA annual report (Swatzell et al. 2020). First full monitoring year after 2017 initial stocking. Contains stocking table, FY18 population estimates (60–66% RASU survival at 21 months), and mass BONY spawning observation. See `IPCA_AnnualReport2018_Summary.md`. |
| `documents/IPCA_AnnualReport2018_Summary.md` | Summary of the FY2018 IPCA annual report. Key reference for 2017 stocking conditions and first-year survival. |
| `documents/LCR MSCP Imperial Ponds Renovation Plan 2014.pdf` | LCR MSCP plan (Finnegan 2014) for rotenone renovation of all six IPCA ponds in Dec 2014–Jan 2015 prior to 2017 stocking. See `IPRenovationPlan2014_Summary.md`. |
| `documents/IPRenovationPlan2014_Summary.md` | Summary of the 2014 renovation plan. Explains the IPCA gap before 2017 stocking and the conversion to 100% well-water supply. |
| `documents/Yuma BW summary of events.xlsx` | Reference spreadsheet of major management events at YCB 2013–2025: stockings (2013, 2014, 2015, 2020, 2025) and netting/harvest events with fish counts. See `YumaBW_SummaryOfEvents_Summary.md`. |
| `documents/YumaBW_SummaryOfEvents_Summary.md` | Summary of `Yuma BW summary of events.xlsx`. Stocking and netting events at YCB with counts of tagged/untagged fish returned and harvested. |
| `documents/bayesian-estimation-of-von-bertalanffy-growth-parameters-for-gray-triggerfish-balistes-capriscus-incorporating-multiple.pdf` | Chamberlin et al. (2025), *Can. J. Fish. Aquat. Sci.* 82:1-12. Bayesian hierarchical VBGM for gray triggerfish separating multi-reader ageing/observation error from growth process error, comparing 3 ageing protocols and testing sex-specific growth. See `Chamberlin2025_GrayTriggerfishVBGM_Summary.md`. |
| `documents/Chamberlin2025_GrayTriggerfishVBGM_Summary.md` | Summary of the gray triggerfish VBGM paper above, with a section applying its findings to the GIEL growth-season threshold model: confirms our `Linf`/`K` non-identifiability is a generic, cited VBGF failure mode (Gwinn et al. 2010; Wilson et al. 2015) tied to lack of fish near the size asymptote (not to our lack of age data); supports reporting derived P(TL>=250mm) predictions rather than raw `Linf`/`K`; suggests testing sex as a covariate (76% of the growth-model fish are sexed, ~equal M/F split) as an untested, low-cost extension; and notes a possible future extension to separate measurement noise from process error, given the 11 flagged negative-growth records. |

> **Marsh et al. (2024)** is the canonical reference for the OCH program context, sex-biased survival patterns, and Imperial Ponds history. Cite as: Marsh, P.C., T.E. Dowling, T.F. Turner, M.J. Osborne, and B.R. Kesner. 2024. Monographs of the Western North American Naturalist, Vol. 15, Article 1.

---

## Key Data Files
| File | Contents |
|---|---|
| `data/BWScanningIndex.RData` | PIT scanner contacts (`BWContacts`, `BWEffort`); downloaded from ncreased.net |
| `data/NFWGAnalysis.RData` | NFWG database captures, stockings, transfers, mortality; downloaded from ncreased.net |
| `data/ReportingData.RData` | All processed analysis-ready objects (created by DataWrangling.R) |
| `data/ReportingData.RData` | All processed analysis-ready objects including known survival datasets (created by DataWrangling.R) |
| `data/Yuma_larvae_mothers_repro_counts_PIT.csv` | Yuma larval parentage – females |
| `data/Yuma_larvae_fathers_repro_counts_PIT.csv` | Yuma larval parentage – males |
| `data/Yuma_recruits_mothers_repro_counts_PIT.csv` | Yuma recruit parentage – females |
| `data/Yuma_recruits_fathers_repro_counts_PIT.csv` | Yuma recruit parentage – males |
| `data/IP_larvae_mothers_repro_counts_PIT.csv` | IP larval parentage – females |
| `data/IP_larvae_fathers_repro_counts_PIT.csv` | IP larval parentage – males |
| `data/IP_recruits_mothers_repro_counts_PIT.csv` | IP recruit parentage – females |
| `data/IP_recruits_fathers_repro_counts_PIT.csv` | IP recruit parentage – males |
| `data/BWNettingEvents.csv` | Hand-maintained untagged netting counts for the joint robust-design model (see `Joint Robust-Design Model`) |
| `data/BWRobustDesign_data.RData` | NIMBLE inputs for the joint robust-design model (created by `model/BWRobustDesign_data.R`) |
| `data/BWRobustDesign_MCMC.RData` | Posterior samples and diagnostics from `model/BWRobustDesign_NIMBLE.R` |
| `data/GIEL_IP5_StockedOnlyBayesianSensitivity.RData` | Stocked-only Bayesian sensitivity check for IP5 GIEL (`annual_surv_stocked_wholeSeason`, `bpost_stocked`, summary tables); see `model/GIEL_ClassicCJS_RobustDesign_Annual_Summary.md` §7.1. Not produced by any script. |
| `data/GIEL_GrowthSeasonThreshold.RData` | Side analysis: recapture pairs, growing-season exposure data, NIMBLE growth-increment fits, and derived P(TL >= 250mm after one growth season) for bonytail at IP2/IP5/IP6. Produced by `model/GIEL_GrowthSeasonThreshold.R`; see `GIEL Growth-to-250mm-Threshold Side Analysis` section above. |

---

## Key R Objects (loaded via `data/ReportingData.RData`)
| Object | Description |
|---|---|
| `StudyBWAnalysis` | One row per PIT-tagged fish (first record in each backwater). Contains: `location`, `location_id`, `species`, `sex`, `total_length`, `first_date`, `event` (stocking/capture), `release_year`, `release_month`, `MaxScanDate`, `MaxDAL`, `Survived` (0/1), `SurvivedFY24`, `SurvivedFY25`, `Transfer` (0/1) |
| `StudyBWNFWG` | All NFWG records for study backwaters. Contains `FirstRecord` flag, `event`, `collection_date`, `PITIndex`, `disposition`, `species`, `sex`, `total_length` |
| `StudyBWContacts` | PIT scanner contacts summarized per PIT-Date-Hour. Contains `Backwater`, `LID`, `Species`, `PITIndex`, `Date`, `ScanHr`, `ScanMonth`, `ScanFY`, `available_date` |
| `StudyBWTransfersAnalysis` | Transfer records with associated capture data |
| `StudyBWCaptureUniques` | Unique capture records per fish per month per backwater |
| `StudyBWGrowth` | Growth data for fish with ≥1 year between tagging and recapture |
| `SurvivalDAL`, `SizeClass2`, `SizeClass3` | Analysis parameters |

## Key R Objects (loaded via `data/ReportingData.RData` — Known Survival additions)
| Object | Description |
|---|---|
| `KnownSurvivalYuma` | Daily known survivor records for Yuma Cove XYTE. Columns: `Date`, `PITIndex`, `first_date`, `sex`, `total_length`, `event`, `MaxScanDate`, `MaxDAL` |
| `KnownSurvivalIPXYTE` | Daily known survivor records for IP XYTE ponds. Adds `location` column |
| `KnownSurvivalIPGIEL` | Daily known survivor records for IP GIEL ponds. Adds `location` column |
| `TotalCountYuma` | Daily total known survivor count (Yuma, all sex combined) |
| `TotalCountIPXYTE` | Daily total known survivor count by IP XYTE pond |
| `TotalCountIPGIEL` | Daily total known survivor count by IP GIEL pond |
| `SuspectTransfers` | Fish with NFWG transfer records flagged as suspect because PIT scanner contacts at the same backwater continued across >1 calendar month after the listed transfer date, suggesting the listed transfer date is inaccurate. Columns: `PITIndex`, `Backwater`, `TransferDate`, `TransferLocation`, `TransferYear`, `PostTransferMonths`, `LastPostTransferContact`. Purely diagnostic — no longer affects inclusion in the known survival analysis (see `Transferred fish` rule below). Also written to `output/TransfersWithContinuedScanning.csv` by the QMD. |
| `KnownSurvivorTransfers` | One row per known-survivor fish (`MaxDAL > SurvivalDAL`) that was transferred out of a backwater (`Transfer == 1`). Columns: `PITIndex`, `Backwater`, `location`, `location_id`, `species`, `sex`, `total_length`, `first_date`, `MaxScanDate`, `MaxDAL`, `TransferDate`, `TransferLocation`. |
| `TransferReductionSummary` | Aggregated from `KnownSurvivorTransfers`: one row per `Backwater`/`species`/`TransferDate`/`TransferLocation` with a count `n` of known survivors removed on that date. Used by `PopulationMonitoring.qmd` to annotate known-survival plots (dotted vertical lines + `-n` labels via `transfer_layers()`) and to generate prose callouts (`describe_transfers()`). |

---

## Output PNG Figures (in `output/`)
| File | Script | Description |
|---|---|---|
| `KnownSurvivalPlotYuma.png` | PopulationMonitoring.qmd | Known survivors over time – Yuma, by sex. Dotted vertical lines with `-n` labels mark dates when known survivors were removed via transfer (see `TransferReductionSummary`). |
| `KnownSurvivalPlotIPXYTE.png` | PopulationMonitoring.qmd | Known survivors over time – IP XYTE, faceted by pond. Same transfer-date annotations as above, per panel. |
| `KnownSurvivalPlotIPGIEL.png` | PopulationMonitoring.qmd | Known survivors over time – IP GIEL, faceted by pond. Same transfer-date annotations as above, per panel. |
| `YumaEstimatePlot.png` | BWMarkRecaptureEstimates.R | Annual mark-recapture estimates – Yuma |
| `IPXYTEEstimatePlot.png` | BWMarkRecaptureEstimates.R | Annual mark-recapture estimates – IP XYTE, faceted |
| `IPGIELEstimatePlot.png` | BWMarkRecaptureEstimates.R | Annual mark-recapture estimates – IP GIEL, faceted |
| `YumaCoveSurvival90DaysPostStocking.png` | PostStockingSurvival.R / PopulationMonitoring.qmd | Post-stocking survival curves – Yuma; exported at 6×4 in |
| `IPXYTESurvival90DaysPostStocking.png` | PostStockingSurvival.R / PopulationMonitoring.qmd | Post-stocking survival curves – IP XYTE; exported at 6×4 in |
| `IPGIEL2017Survival90DaysPostStocking.png` | PostStockingSurvival.R / PopulationMonitoring.qmd | Post-stocking survival curves – IP GIEL (2017); exported at 6×4 in |
| `YumaOffspringPlot.png` | GeneticsAndSurvival.R | Reproductive contribution by sex/year – Yuma XYTE |
| `IPCA1OffSpringPlot.png` | GeneticsAndSurvival.R | Reproductive contribution – IP Pond 1 XYTE |
| `CohortComparisonXYTEPlot.png` | PopulationMonitoring.qmd | Cross-site initial vs supplemental stocking survivors — Razorback Sucker |
| `CohortComparisonGIELPlot.png` | PopulationMonitoring.qmd | Cross-site initial vs supplemental stocking survivors — Bonytail |
| `SexSurvivalXYTEPlot.png` | PopulationMonitoring.qmd | Sex-based survival comparison — Razorback Sucker |
| `SexSurvivalGIELPlot.png` | PopulationMonitoring.qmd | Sex-based survival comparison — Bonytail |
| `TransfersWithContinuedScanning.csv` | PopulationMonitoring.qmd | Records for fish with suspect transfer entries retained in the known survival analysis |
| `../IPGIELScannerDiagnostics.html` | IPGIELScannerDiagnostics.qmd | Self-contained (embed-resources) HTML diagnostics report for Bonytail ponds; written to the project root, not `output/`. Re-render with `quarto render IPGIELScannerDiagnostics.qmd`. |
| `../XYTEScannerDiagnostics.html` | XYTEScannerDiagnostics.qmd | Self-contained HTML diagnostics report for Razorback Sucker sites (YCB, IPCA Ponds 1, 3, 4); written to the project root. Re-render with `quarto render XYTEScannerDiagnostics.qmd`. |
| `PopulationEstimates_<date>.xlsx` | PopulationMonitoring.qmd | All-site mark-recapture estimates (M, C, R, N̂, 95% bootstrap CI from `recapr::ciChapman(method = "boot")`) exported each time the QMD is rendered (`export-estimates` chunk, ~line 1138). Filename includes the render date (e.g. `PopulationEstimates_14Aug2026.xlsx`); re-rendering on the same day overwrites that day's file. **Multi-sheet layout** (mirrors the legacy `output/PopulationEstimates.xlsx` design, minus its mixed header colors): sheets are `Summary (Latest)` (most recent census year per backwater), `All Backwaters` (full history, all locations), then one sheet per backwater named `<Backwater> (RASU|BONY)` — e.g. `YCB (RASU)`, `IP1 (RASU)`, `IP2 (BONY)`. Per-backwater sheets drop the redundant `Backwater` column. Columns: `Backwater` (Summary/All Backwaters sheets only), `Species`, `CensusYear`, `M`, `C`, `R`, `Estimate`, `Lower95CI`, `Upper95CI`. Every sheet uses the **same** `openxlsx::writeDataTable(tableStyle = "TableStyleMedium2", bandedRows = TRUE)` — one consistent table style across all sheets; do not reintroduce per-sheet custom header colors. |

---

## Analysis Rules and Caveats
- **Post-stocking survival model:** Requires ≥5 stocked fish with known sex and total length. Excludes Yuma 2020 (no sex data) and Yuma 2015 (no male contacts). IP GIEL model only uses 2017 stocking cohort. Sex "J" (juvenile) is recoded to "U" before analysis.
- **Mark-recapture estimates:** Computed only when recaptures R > 3. Marking period = Jan–Feb, capture period = Oct–Apr. Fish must be tagged ≥6 months before marking period. **Chapman (1951) estimator:** N̂ = (M+1)(C+1)/(R+1) − 1, the nearly unbiased modification of the Lincoln-Petersen estimator (see `PierceEtAl_ChapterAbundance_Summary.md`). **95% CI method:** Parametric bootstrap via `ciChapman(n1 = M, n2 = C, m2 = R, method = "boot", bootreps = 10000)` from the `recapr` R package (Tyers 2021). Recaptures are resampled from Binomial(n = C, p = R/C); bootstrap CIs outperform normal-approximation methods especially at low R. See `Sangnawakij_et_al_2026_Summary.md` for the theoretical basis and comparison of bootstrap methods. **Special case:** When R = C (all captured fish were marked), the Chapman formula simplifies algebraically to M, identical to the simple ratio MC/R = M — the −1 is present and correct; it is a mathematical identity. Applied consistently in `BWMarkRecaptureEstimates.R` and `PopulationMonitoring.qmd`.
- **Known survivors:** A fish is counted as "alive" on a date if it was tagged before that date AND was scanned at least once ≥90 DAL.
- **Genetics/offspring:** Only Yuma Cove XYTE and IP Pond 1 XYTE currently have parentage data from the Dowling lab. Skip this section for all other ponds.
- **CJS model:** Only implemented for IP Pond 1 in BackwaterMCR.R. Requires MARK software (RMark package).
- **Pond-to-pond transfers:** One known transfer (003C06F28D) has been documented; `FirstRecord` is tracked per backwater. Its transfer-origin backwater is recorded in the source data under a legacy name, `"Imperial NWR Pond 3"`, which `DataWrangling.R` normalizes to `"IPCA (Pond 3)"` before joining transfer info onto `StudyBWAnalysis` (see `Fixed Code Issues`) so the departure event attaches only to the Pond 3 record, not the Pond 5 arrival record.
- **Transferred fish are retained, not excluded:** `DataWrangling.R` no longer filters out fish with `Transfer == 1` from the known survivor groups (`StudyBWXYTEIP`/`StudyBWGIELIP`/`StudyBWYuma`) — only `MaxDAL > SurvivalDAL` is required. This works because `MaxScanDate`/`MaxDAL` in `StudyBWAnalysis` are already scoped to contacts within the fish's original backwater (joined via `ContactsSummary` on `PITIndex` + `location`), so a transferred fish is counted as a known survivor only through its last local contact date — at or near `TransferDate` — and then drops out of the daily `KnownSurvival*`/`TotalCount*` counts on its own; no special-case cutoff logic is needed. `KnownSurvivorTransfers` and `TransferReductionSummary` (see `Key R Objects` above) capture the resulting reductions for reporting: `PopulationMonitoring.qmd` marks each transfer date with a dotted vertical line and `-n` label on the known-survival plots (`transfer_layers()`) and lists dates/counts/destinations in prose immediately below each figure (`describe_transfers()`). The cohort comparison figure additionally excludes any captured-and-tagged fish via `filter(event == "stocking")`. `SuspectTransfers` remains purely diagnostic (see above) and no longer affects which fish are retained.

---

## Utility Functions (`LabFunctions.R`)
- `packages(pkg)` — auto-installs and loads a package
- `no_na(x)` / `no_na_df(x)` — replaces NA with 0 (scalar / vector)
- `download_backwater("data")` / `download_nfwg("data")` — refresh data from ncreased.net
- `euclid(p1, q1, p2, q2)` — Euclidean distance between UTM coordinates
- `split_hourly(df, id, start, end)` — expands start/end records to hourly rows

---

## Known Code Issues
- **Joint robust-design model — stale-data pitfall (2026-09-03/04):** `model/BWRobustDesign_NIMBLE.R` loads `data/BWRobustDesign_data.RData` and does **not** rebuild it. The known-alive constraint (`stobs = 3` from `MaxScanDate`, any contact incl. May–Sep) was added to `BWRobustDesign_data.R` after the data had been built, so the dev fit and first final fit were run on data with **zero** code-3 entries and underestimated tagged fish alive (e.g. IP1 Dec 2021: model 60 vs 86 known alive). **Before every fit:** re-source `model/BWRobustDesign_data.R` if it, `ReportingData.RData`, or `BWNettingEvents.csv` is newer than `BWRobustDesign_data.RData`, and confirm `table(stobs_mat)` includes code 3. A guard comparing file mtimes at the top of the NIMBLE script would prevent a recurrence (not yet added).
- `BackwaterMCR.R` references `StudyBWNFWGAnalysis` which does not exist in ReportingData.RData; likely should be `StudyBWAnalysis`. Also references column `Location` which should be `Backwater`. This script is incomplete/experimental.

## Line Ending Policy

All `.R` and `.qmd` files in this project **must use LF-only line endings** (Unix-style, `\n`). The files originated with Windows CRLF (`\r\n`) endings and were fully normalized to LF on 2026-08-07. The RStudio edit tool can silently reintroduce CRLF on Windows — if a string-replacement edit fails with "String not found" on any `.R` or `.qmd` file, CRLF re-contamination is the most likely cause.

**After any editing session**, run this one-liner from the project root to re-normalize all `.R` and `.qmd` files:

```python
# Run in a bash or terminal pane from C:\GIT\BackwaterGenetics
python3 -c "
import glob
for ext in ('*.R', '*.qmd'):
    for path in glob.glob(ext):
        data = open(path, 'rb').read()
        if b'\r\n' in data:
            open(path, 'wb').write(data.replace(b'\r\n', b'\n'))
            print('Normalized:', path)
"
```

To verify a single file is clean:
```python
python3 -c "data=open('FILE.R','rb').read(); print('CRLF count:', data.count(b'\r\n'))"
```

---

## Post-Edit File Format Checks

After editing any `.qmd` or `.R` file in this project, verify:

1. **No blank lines within code chunks** — Blank lines between every line of code is a known artifact in this project. After making edits, check that no blank line between every code line was inadvertently introduced. Quick diagnostic in R:
   ```r
   lines <- readLines("PopulationMonitoring.qmd")  # or other file
   in_chunk <- FALSE
   blank_in_chunk <- 0
   for (line in lines) {
     if (grepl("^```\\{r", line)) in_chunk <- TRUE
     else if (in_chunk && grepl("^```\\s*$", line)) in_chunk <- FALSE
     else if (in_chunk && trimws(line) == "") blank_in_chunk <- blank_in_chunk + 1
   }
   cat("Blank lines inside code chunks:", blank_in_chunk, "\n")
   ```
   If the count is high (more than a handful of intentional separators), strip them with the loop in `Known Code Issues` below or re-apply the cleanup used on 2026-07-24.

2. **YAML front matter has no blank lines** — Blank lines inside nested YAML blocks (`format:`, `execute:`) break parsing and cause the front matter to print as raw text. The YAML in `.qmd` files must be compact with no blank lines between keys.

3. **Global chunk options present** — `PopulationMonitoring.qmd` and `ScanningEffectiveness.qmd` rely on `execute: echo: false / warning: false / message: false` in the YAML. Confirm these are intact after edits.

---

## Fixed Code Issues (resolved)
- `PopulationMonitoring.qmd` (ip-pond1-offspring chunk, line ~863): `filter(Pond == "IPCA ( Pond 1)", ...)` had an extra space before "Pond", returning 0 rows and causing `facet_wrap` to error with "Faceting variables must have at least one value." Fixed to `"IPCA (Pond 1)"`.
- `DataWrangling.R` (recent, formerly `KnownSurvival.R`): Added `SuspectTransfers` detection — fish flagged as transferred but with >1 month of post-transfer PIT contacts at the same backwater are retained in the known survival analysis. Group filters use `Transfer == 0 | PITIndex %in% SuspectTransfers$PITIndex`.
- `PopulationMonitoring.qmd` cohort comparison (recent): Cohort comparison now correctly excludes transferred fish because `KnownSurvival*` objects upstream exclude them; the existing `filter(event == "stocking")` additionally ensures captured-and-tagged fish never appear in either cohort line.
- `PopulationMonitoring.qmd` (recent): Added `suspect-transfers` chunk that writes `output/TransfersWithContinuedScanning.csv` and emits a bold note paragraph listing flagged tags when `SuspectTransfers` is non-empty.
- `ScanningEffectiveness.qmd` (recent): Added "Tagged Fish Never Contacted by Passive Antennas" section with a summary table of never-scanned fish by location and a long-absence table listing any fish physically re-encountered ≥365 days after tagging with zero intervening passive contacts.
- `GeneticsAndSurvival.R` / `PopulationMonitoring.qmd` (recent): Cohort comparison `filter(event == "stocking")` confirmed present and correct; no captured-and-tagged fish flow into the Supplemental/Restock cohort lines.
- `KnownSurvival.R` line 144: `ax is.text` typo fixed to `axis.text`. *(Script is deprecated from pipeline; plotting now done in PopulationMonitoring.qmd.)*
- `DataWrangling.R` line 313: `writeData(..., ContactSummaryDuplicateLocations)` would error when no pond-hoppers exist (variable was `rm()`'d). Wrapped in `if (exists(...))` guard.
- `GeneticsAndSurvival.R` lines 133/155/175: `TagYear = as.factor(year(first_date))` caused numeric-vs-factor comparison in `filter(Year != TagYear)` to fail silently — stocking-year records were never excluded, leading to double-counting in `PropOffspring` for stocking years. Fixed by removing `as.factor()`.
- `GeneticsAndSurvival.R` lines 65/99: `suffix = c("_Recruits", "_Larvae")` was backwards on `full_join(LarvalParents, RecruitParents)`. Results were numerically correct due to symmetric logic, but column names were misleading. Fixed to `c("_Larvae", "_Recruits")`.
- `PopulationMonitoring.qmd` (Yuma offspring chunk line ~420, IP XYTE offspring chunk line ~719): same `TagYear = as.factor()` bug copied from original script. Fixed to `TagYear = year(first_date)` (integer comparison).
- `DataWrangling.R` / `KnownSurvival.R` (recent): All known survival data wrangling (`SuspectTransfers`, `KnownSurvival*`, `TotalCount*`, Additions, Removals) moved from `KnownSurvival.R` into `DataWrangling.R` and saved in `ReportingData.RData`. `KnownSurvivalCounts.RData` is no longer generated or loaded by any script. `KnownSurvival.R` is retained but deprecated from the pipeline.
- `DataWrangling.R` (2026-08-14): Reworked transfer handling for the known survival analysis. Previously, fish with `Transfer == 1` were excluded entirely from `StudyBWXYTEIP`/`StudyBWGIELIP`/`StudyBWYuma` (with `SuspectTransfers` fish carved out as an exception). Now all fish are retained regardless of `Transfer` status — they naturally stop counting as known survivors at their last local contact date (already scoped per-backwater in `ContactsSummary`), which falls at or near `TransferDate`. Added `KnownSurvivorTransfers` and `TransferReductionSummary` objects to report the resulting reductions. `PopulationMonitoring.qmd` updated to annotate the three known-survival plots with dotted vertical lines/count labels at transfer dates (`transfer_layers()`) and to print a prose summary of dates/counts/destinations below each figure (`describe_transfers()`); the `suspect-transfers` note was reworded to reflect that `SuspectTransfers` is now purely diagnostic.
- `DataWrangling.R` (2026-08-14, transfer reporting validation): Verified all 70 YCB fish in `TransferReductionSummary` have a matching NFWG `capture`/`retained` record at Yuma Cove on or within 1 day of `TransferDate` (the expected "capture-retained + transfer-elsewhere" profile) and, after excluding `SuspectTransfers` fish, none are scanned at YCB more than 4 days after their listed transfer date. This surfaced two bugs, both fixed: (1) `KnownSurvivorTransfers` now excludes `SuspectTransfers` fish (fish `003BE8F1E1`, transferred 2024-10-22 to "Yuma Cove (lakeside)", continued to be scanned at YCB through mid-2026 — a spurious transfer record that was already flagged diagnostically but wasn't excluded from the new reduction-reporting table). (2) The `Transfer`/`TransferDate`/`TransferLocation` join onto `StudyBWAnalysis` (building the per-fish, per-backwater records) previously matched on `PITIndex` alone, so a fish with records in *two* backwaters could have a transfer event attached to the wrong one. This affected the documented pond-hopper `003C06F28D`: its arrival record at IPCA Pond 5 was incorrectly tagged as transferred out *on its arrival date* (falsely appearing as a "-1" reduction in `TransferReductionSummary`), because the join ignored which backwater the fish actually left. Fixed by joining on `PITIndex` + backwater (`location` == `Backwater`), after normalizing the legacy transfer-origin name `"Imperial NWR Pond 3"` to the current `"IPCA (Pond 3)"` label so the real departure event still attaches to the correct (Pond 3) row. "Yuma Cove (lakeside)" as a transfer destination was confirmed to be a genuine off-site relocation, not a mislabeled in-backwater move — of 97 total YCB transfer records to that destination, only the one already-flagged `SuspectTransfers` fish showed continued on-site scanning.
- `PopulationMonitoring.qmd` (2026-08-14, docx render): `quarto render` failed for the `docx` format with "Functions that produce HTML output found in document targeting docx output," because the three stocking-table chunks (`yuma-stocking-table`, `ip-xyte-stocking-table`, `ip-giel-stocking-table`) always piped through `kableExtra::kable_styling()` + `add_header_above()`, which emit raw HTML regardless of target format. Fixed by adding an `apply_table_style(kbl_obj, header_above = NULL)` helper (defined in the `setup` chunk, alongside `bw_label`/`transfer_layers`) that applies that kableExtra styling only when `knitr::is_html_output()` is true; for docx it returns the plain `knitr::kable()` object unstyled (no striping or grouped header — docx would need flextable/officer for that, out of scope). The three chunks now end with `apply_table_style(c(" " = n, "Stocked" = 4, "Survived (≥90 DAL)" = 4))` instead of the raw `kable_styling()`/`add_header_above()` pipeline. Both `html` and `docx` targets render cleanly after this fix.
- `PopulationMonitoring.qmd` (2026-08-14, label overlap): On the Yuma known-survival plot, the transfer-count labels added by `transfer_layers()` overlapped illegibly when two transfer dates fell close together (e.g. Nov 6 and Nov 7, 2019 — labels "-6" and "-22" stacked on top of each other since the dates are indistinguishable at a multi-year x-axis scale). Fixed by staggering `vjust` for consecutive dates (`arrange(TransferDate)` then alternate `vjust = -0.3` / `-1.7` via `label_vjust = -0.3 - 1.4 * ((row_number() - 1) %% 2)`, mapped with `aes(vjust = label_vjust)`), so adjacent labels render at different heights even when their vertical reference lines nearly coincide.
